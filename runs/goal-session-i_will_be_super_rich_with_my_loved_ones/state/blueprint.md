# App Blueprint — i_will_be_super_rich_with_my_loved_ones

> **Tapeology — research evolution.** Status: APPROVED (human-approved 10-06-2026; `blueprint.approved` marker present).
> Carries forward the APPROVED contract of sessions `i_will_be_rich` + `i_will_be_super_rich`
> (J-01–J-37 built & in force: rows 1–13 below, unchanged) and registers the research evolution
> (J-38–J-68: theses, verdicts, journal, review, excursions, analytics, studies, then cues).
> Source: `docs/goal.md` — Product Shape, Canonical values, Key Capabilities 20–34.
>
> **Governing principles (unchanged):** single-ticker tape cockpit; every value computed exactly
> once and read identically by REST/WS/UI; provider-agnostic engine, one vendor adapter;
> price impact, not raw aggression; honest failure states, never fabricated data; one focused chart.
> **New for this session:** the research layer attaches ONLY via the engine's snapshot observers
> (capability 20) and is read-only over the engine — byte-identical engine outputs with or without
> it (equivalence-tested); journal-scoped SQLite persists research records only (no tape data);
> every research record is stamped with bound source + `data_feed` + `config_fingerprint`;
> **build order is binding: cues (J-63–J-67) only after evidence (J-58–J-62) passes.**

## Information Architecture

**Layout shell:** dark instrument-panel; persistent top bar gains the first multi-page nav:
**Cockpit · Journal · Studies**. The cockpit stays the home and stays one screen.

```
Tapeology (top bar: Cockpit · Journal · Studies)
├── Cockpit  /              — the tape cockpit (HOME; all J-01–J-37 surfaces unchanged)
│     + price chart now renders in ALL modes incl. live, carrying thesis geometry
│     + thesis strip (between chart and panel grid): declare affordance → active thesis
│       (setup, direction, invalidation, statement statuses, verdict + evidence, risk-flag
│       chips, resolve / mark-entry / mark-exit) → later the checklist/stance (cue layer)
│     + hint dock (under the tape-state panel): current setup-forming hint when active
├── Journal  /journal       — filterable thesis table · hint log · analytics view
│   └── /journal/[id]       — review detail: frozen statements + verdict timeline (true clock
│                             time), risk flags, action marks, execution checks, excursions,
│                             outcome × process quadrant, mistake-tag picker, "re-watch window"
└── Studies  /studies       — create/monitor replay studies (job status/progress/cancel);
                              results: occurrences + aggregates vs seeded null baseline,
                              hindsight_level / truncation labels, feed + fingerprint stamps
```

> **Iter-12 (build-out note, additive — no skeleton change):** the persistent top bar ships with
> the **Cockpit · Journal** entries and the `/journal` thesis-table page; the **Studies** entry
> lands together with the `/studies` page (J-60) so the approved skeleton never carries a dead
> link. `/journal/[id]`, the hint log, and the analytics view land with their own journeys
> (J-54/J-55, J-65, J-59) under the same Journal home.
>
> **Iter-13 (build-out note, additive — no skeleton change):** the `/journal/[id]` review-detail
> page ships (J-54/J-55) at its already-approved home under Journal; `/journal` rows become links
> (the iter-12 "deliberately not links" placeholder resolves). The page's mistake-tag picker
> renders the machine-SUGGESTED tags pre-selected + toggleable with a **disabled Save** carrying
> honest copy — the review SAVE flow (`POST …/review`, the `reviewed` flip) lands with J-57
> (same no-dead-control pattern as the disabled Studies entry). Excursions, the outcome × process
> quadrant, and "re-watch window" land with their own journeys (J-58, J-56, later).
>
> **Iter-14 (build-out note, additive — no skeleton change):** the review pillar completes at the
> same Journal home — `/journal/[id]` gains per-statement FINAL-status badges (J-55 completion),
> the **outcome × process quadrant** (J-56), and the now-ENABLED Save (the iter-13 disabled-Save
> placeholder resolves via `POST /research/thesis/{id}/review`, J-57); `/journal` rows gain
> additive grade/reviewed columns. Excursions and "re-watch window" still land with their own
> journeys (J-58, later).
>
> **Iter-15 (build-out note, additive — no skeleton change):** the **evidence layer begins** at
> the same Journal home — `/journal/[id]` gains the **excursion section** (J-58, row 20 build-out):
> two visually separate population blocks (confirmation-anchored / entry-anchored, never pooled),
> per-horizon MFE/MAE in R + ternary outcome + truncation flags + spread-at-anchor. No new routes.
> Analytics (J-59), studies (J-60–J-62), and "re-watch window" still land with their own journeys.

> **Iter-16 (build-out note, additive — no skeleton change):** the **analytics view ships** at its
> already-approved home — `/journal` gains the analytics view (J-59, row 21 aggregates half) as an
> in-page view beside the thesis table (table stays the default; no new route, no nav change; the
> Studies entry stays disabled). The hint log portion of `/journal` still lands with J-65 (cue layer);
> studies (J-60–J-62) and "re-watch window" still land with their own journeys.

> **Iter-17 (build-out note, additive — no skeleton change, no UI change):** the **capability-34
> engine performance gate ships** as its own byte-identity-pinned step (the prerequisite goal.md
> sets for studies): rolling-feature refresh maintenance in `app/engine/features.py` becomes truly
> incremental across evictions with values BYTE-IDENTICAL to before — rows 1–2 keep their owners,
> endpoints, and outputs unchanged (oracle-equivalence + pinned-anchor + observer-equivalence
> tested). Test/CI assets registered: a committed ≈10-minute moderate-density real **SIP** fixture
> (also the capability-32 reference-study window, reused by J-60–J-62 next) and a CI timing gate
> whose budget is the config-owned `dense_replay_time_budget_seconds` — serving/CI-only, EXCLUDED
> from `config_fingerprint` with documented rationale + stability test + counter-test (the
> iter-12/iter-16 precedent). Schema stays v7; the Studies nav entry stays disabled; the `/studies`
> page (row 23) still lands with J-60.

> **Iter-18 (build-out note — NAV-SKELETON CHANGE, re-approval requested):** the **studies layer
> ships** (J-60/J-61/J-62) at its pre-registered home: the `/studies` page lands and the top bar's
> **Studies entry flips from disabled to ENABLED** — the one skeleton change this session, planned
> in the approved IA from day one (the entry was registered disabled so the skeleton never carried
> a dead link); see `state/blueprint.reapproval-requested`. The page = create form (reference-window
> quick-pick over the committed PG SIP fixture · seeded sims · arbitrary symbol+window via the
> existing historical seam) + job list (status/progress/cancel) + stored-results view (occurrence
> rows, aggregates side-by-side with the seeded random-arm-time null baseline, `hindsight_level` /
> truncation labels, feed + fingerprint stamps, n + caveats). Row 23 is built out (see Data
> Contract); the cue layer (J-53, J-63–J-67) remains strictly last.

> **Iter-20 (build-out note, additive — no skeleton change):** the **cue layer begins** at its
> pre-registered home — the `/` thesis strip gains the **holding-period management stance** (J-53,
> the stance half of row 25): `thesis_intact | thesis_weakening | thesis_invalidated` + evidence,
> with live distance-to-invalidation ($ and R) and open R, shown ONLY while a thesis is
> entry-marked and unresolved with a live monitor. No new routes, no nav change; the entry
> checklist (J-63/J-64), hint dock (J-65), and live feed badge (J-67) still land with their own
> journeys.

> **Iter-21 (build-out note, additive — no skeleton change):** the cue layer continues at the same
> pre-registered home — the `/` thesis strip gains the **entry checklist** (J-63, the checklist
> half of row 25): eight named checks with live margins in their own units, the aggregate stance
> (`conditions_met | conditions_not_met | tape_against | no_fresh_tape`) through its own dwell, and
> the nearest-counterevidence line, shown ONLY on an active, monitor-evaluated, NOT-entry-marked
> thesis (mutually exclusive with the iter-20 management stance). Row 14 (`delivery_lag_seconds`)
> ships as its feeder-owned prerequisite. No new routes, no nav change; the J-64 freshness journey,
> hint dock (J-65), and live feed badge (J-67) still land with their own journeys.

> **Iter-22 (build-out note, additive — no skeleton change):** the **J-64 freshness guarantee**
> completes at the same pre-registered home — the row-25 owner's wiring is fixed so the served
> checklist always reflects the CURRENT canonical row-6 `stream_status` / row-14
> `delivery_lag_seconds` across status flips (paused/stale → immediate `no_fresh_tape`; resume →
> honest re-evaluation; closed/failed stay honest-by-removal) — and the `/` cockpit status area
> gains row 14's pre-registered **lag readout** (served value rendered verbatim). No new routes,
> no nav change; the hint dock (J-65) and live feed badge (J-67) still land with their own journeys.

> **Iter-23 (build-out note, additive — no skeleton change):** the cue layer completes its last
> unbuilt surface at its pre-registered homes — the `/` cockpit gains the **hint dock** (J-65,
> row 22 build-out) under the tape-state panel (visible only when a hint is active; declare
> affordance prefills the thesis-strip form, never creates a thesis), and `/journal` gains the
> **hint log** as a third in-page view (theses | analytics | hints — no new route, no nav change).
> The J-66 sweep (+ sound cue), J-67 feed badge, and the J-68 backlog still land with their own
> journeys.

> **Iter-24 (build-out note, additive — no skeleton change):** the **J-67 feed-basis surface
> ships** at its pre-registered homes — the `/` cockpit status area gains the **feed-basis badge**
> (row 29: the served `sim | iex | sip` basis rendered verbatim, with the live IEX-vs-SIP
> disclosure line from row 24), and `/journal`'s existing hints view gains each stored row's
> `data_feed` stamp (row 22, additive column — no new route, no nav change). The J-66 sweep
> (sound cue + copy-lint + all-surface walk) and the J-68 backlog still land with their own
> journeys.

> **Iter-25 (build-out note, additive — no skeleton change):** the **J-66 cue-discipline sweep
> ships** at its pre-registered homes ("all research surfaces") — the `/` cockpit cue area gains
> the **optional sound-cue toggle** (explicit, default OFF, fires only on stance/verdict
> transitions with the served cooldown, visible fired-indicator; the toggle state is a
> client-local UI preference, never served or persisted), every research surface's copy is
> audited against the no-imperative/no-prediction register with fixes applied at the taxonomy
> source, the study-creation feed pre-stamp consolidates to the row-26 mapping (the last
> hardcoded feed literals fall), and a comprehensive **copy-lint test** (full taxonomy payload +
> served copy + frontend-string scan) becomes the permanent backstop. No new route, no nav
> change; the J-68 backlog lands with its own journeys.

**Feature / journey homes** (≤2 clicks from the nav):

| Feature / journey | Canonical home (route) | Nav section |
|---|---|---|
| J-01–J-37 (cockpit, chart, watch lifecycle, real data) | `/` | Cockpit |
| J-38–J-46, J-49, J-50, J-52, J-53 (declare, verdicts, flags, resolve, marks, stance) | `/` thesis strip | Cockpit |
| J-47 (source binding / survives interruption) | `/` thesis strip + `/journal` row | Cockpit / Journal |
| J-48 (thesis geometry) | `/` chart pane | Cockpit |
| J-51, J-55–J-57 (persistence, review, grades, tags) | `/journal` → `/journal/[id]` | Journal |
| J-54, J-58 (execution checks, excursions) | `/journal/[id]` | Journal |
| J-59 (segregated analytics) | `/journal` analytics view | Journal |
| J-60–J-62 (replay studies + CI reference study) | `/studies` | Studies |
| J-63, J-64 (entry checklist / stance + freshness) — built LAST | `/` thesis strip | Cockpit |
| J-65 (hints, logged) | `/` hint dock + `/journal` hint log | Cockpit / Journal |
| J-66 (copy discipline), J-67 (feed labels) | all research surfaces / live feed badge + stamps | all |
| J-68 (regression sentinel) | `/` cockpit unchanged with no thesis | Cockpit |

No watchlist grid, no multi-symbol view, no execution/order affordance anywhere.

## Data Contract

Rows 1–13 are **built & in force** (approved in prior sessions; owners unchanged — listed compactly).
Rows 14–27 are the research evolution, to be built by this session. `…/summary` and `WS /stream`
re-expose the snapshot and never recompute; no page recomputes or re-fetches any row from elsewhere.

| # | Value / entity | Computed by (single owner) | Served by (single endpoint) | Notes |
|---|---|---|---|---|
| 1 | Tape state + confidence | `TapeStateClassifier` (relative spread/impact gates; spread = graded factor, not veto; SIP historical / IEX live feed inside the one adapter) | `GET /tape/{t}/state` | re-exposed by `/summary`, WS |
| 2 | 14 core features × 5 windows | `FeatureEngine` | `GET /tape/{t}/features` | re-exposed by `/summary`, WS |
| 3 | bid / ask / spread / last | `MarketState` (spread = ask − bid) | `GET /tape/{t}/summary` | re-exposed by WS |
| 4 | Recent trades (price/size/side) | Aggressor classifier (quote rule + tick-test fallback) | `GET /tape/{t}/events` | re-exposed by WS |
| 5 | Observations + event-log messages | Engine transition emitter | `GET /tape/{t}/events` | re-exposed by WS |
| 6 | Watched source + stream status (connecting/waiting/live/stale/paused/failed/closed) | Engine/feeder — one writer | `GET /tape/{t}/summary` | re-exposed by WS; lifecycle via `/watch` + pause/resume/speed |
| 7 | Symbol search results | Vendor adapter (warmed `_ASSET_UNIVERSE`) | `GET /symbols/search?q=` | hiccup ⇒ empty list |
| 8 | Market clock | Vendor adapter clock module | `GET /market/clock` | live status indicator |
| 9 | Real-data failure states (unavailable / unknown symbol / no data / closed / provider_timeout) | Providers + adapter (real call-level deadline; backend bound < frontend timeout) | `POST /watch/{t}` error path | UI non-cockpit states |
| 10 | OHLC bars + tape-state markers | Engine history buffer | `GET /tape/{t}/history?bar=` | chart reads verbatim, all modes |
| 11 | Paused state | Engine/feeder | `/summary` (set via pause/resume) | re-exposed by WS |
| 12 | Resolved historical window (local tz, quick-picks) | `apps/frontend/lib/datetime.ts` resolver (once, pre-fetch) | `POST /watch/{t}` body | no second tz conversion |
| 13 | Display/epoch anchor (true-clock axis; live = first record's epoch) | Engine/feeder (display metadata only) | `GET /tape/{t}/history` projection + snapshot | chart stamps axis verbatim |
| 14 | **`delivery_lag_seconds`** | Feeder (latest record epoch vs wall clock) | snapshot via `GET /tape/{t}/summary` + WS | UI lag readout AND the `tape_lag_ok` check read this same value. **Iter-21 (build-out, additive):** ships — feeder-owned in `app/watch_manager.py`, an ADDITIVE snapshot/projection metadata field (the iter-9 `end_reason` precedent: lifecycle/display data only, never read by classification; observer signature unchanged, equivalence suite green, zero re-pins). Per-mode honest semantics documented: live = latest record's epoch vs wall clock; paced replay (sim/historical) = the feeder's processing backlog against its own pacing schedule (a replay deliberately behind wall clock is NOT lagging; a healthy sim reads ≈0). The bound is a config-owned documented research default; row 25's `tape_lag_ok` margin reads this same served value. **Iter-22 (build-out, additive):** the pre-registered UI lag readout ships in the `/` cockpit status area — it renders this same served snapshot value verbatim (display rounding only; honest absence when null, never a fabricated 0); the feeder stays the one owner, no second computation anywhere |
| 15 | **Thesis projection** (fields, frozen expected-behaviour statements + live statuses, verdict + evidence, risk flags, monitor_status) | Research monitor (engine snapshot observer; exception-isolated) | `GET /research/thesis/active?ticker=` | WS `thesis` key MUST equal it verbatim; thesis strip + chart geometry read it; `thesis: null` is normal. **Iter-9 (additive):** a surviving entry-marked active thesis with no live monitor (ticker unwatched, or watched on a mismatched source) is served by this SAME endpoint via the SAME projection builder from the persisted record, flagged with an explicit not-evaluated `monitor_status` + bound-source notice — never a second computation path or endpoint. **Iter-10 (additive):** the projection gains a **`geometry`** key — chart-ready thesis geometry (labeled invalidation/level price-lines at the declared prices; verdict-transition, entry/exit, and first-confirmation markers with their logical + wall timestamps) — computed ONLY inside the same single `build_projection` as a pure projection of the thesis fields + the row-16 append-only timeline + row-18 marks (current evaluation segment only: events before the latest `watch_restarted` gap are omitted from geometry, never misplaced on a foreign axis); served by this same endpoint + the WS `thesis` key; the chart draws it verbatim on the row-13 epoch anchor and computes no state/side/price/time basis of its own. **Iter-11 (additive):** the projection gains a **`risk_flags`** key — the row-17 entry risk flags (flag name + measured plain-language evidence values), computed ONCE at declaration by the research monitor's single flag function inside `POST /research/thesis`, frozen on the persisted thesis (schema v4 migration, never backfilled), and re-exposed VERBATIM by this same single `build_projection` (live and surviving paths alike) + the WS `thesis` key + `GET /research/journal/{id}`; chip labels/evidence copy from row-24 taxonomy; key ABSENT = never assessed (pre-migration row), empty list = assessed-and-clean; never recomputed at read, never a second computation or serving path |
| 16 | **Published verdict timeline** (append-only, with gap events; `rule_first_true` + `published_at`) | Verdict engine → journal repository (single writer queue; repo exposes no update/delete) | `GET /research/journal/{id}` | rendered verbatim; never recomputed at read. **Iter-9 (additive):** gap events (e.g. `watch_restarted` on matching-source re-attach) are appended timeline rows written by the same single writer — never edits or backfill |
| 17 | **Entry risk flags** | Computed once at declaration by the research monitor; frozen on the thesis | `POST /research/thesis` response → row 15 / journal | advisory, never blocking; incoherent input = 422, never a flag |
| 18 | **Action marks** (entry/exit, verbatim price + logical & wall time + **spread-at-mark stamped once at recording** from the current snapshot — a moment value, never recomputed) | `POST /research/thesis/{id}/action` (recorded as stated, never inferred) | row 15 projection + `GET /research/journal/{id}` | chart marks + strip read it; entry-marked ⇒ no Abandon |
| 19 | **Thesis resolution + execution checks + outcome × process grades** | Computed once at resolution (`POST /research/thesis/{id}/resolve`); persisted | `GET /research/journal/{id}` | user resolutions = `played_out \| abandoned` ONLY (system owns `invalidated`/`expired` → 422); 409 already-resolved; entry-marked refuses abandon; the resolution is an APPENDED timeline event + status flip (logical + wall timestamps), never an edit; enum labels from evidence-backed checks; never numeric scores. **Iter-13 (additive):** the **execution-checks half ships** — the four named checks (`entered_before_confirmation`; `chased_entry` anchored at the recorded `rule_first_true` price + the config chase threshold, never the post-dwell publish; `exited_beyond_invalidation`; `cut_confirming_early`) + their machine-SUGGESTED mistake tags (backend-owned check→tag mapping, labels from row 24) are computed ONCE at every terminal resolution (user resolve, invalidation auto-resolve, stream-end expiry, restart-expiry sweep) by ONE function (single owner module under `app/research/`) from recorded row-18 marks + the row-16 append-only timeline + frozen thesis fields ONLY, persisted on the thesis (**schema v5** versioned migration), and served VERBATIM only by this same `GET /research/journal/{id}`; enum statuses + plain-language evidence, never numeric scores; no marks ⇒ explicit `not_applicable`; pre-v5 resolved theses carry the key ABSENT (honest omission, never backfilled, never computed at read). The grades half remains for J-56; suggestions are never system-recorded tags — the user confirms via the J-57 review flow. **Iter-14 (additive):** the **grades half ships** — outcome (`thesis_held \| thesis_failed \| no_read`, 1:1 from the resolution) × process (`clean \| flagged \| violated`, a config-owned rule over the frozen row-17 risk flags + the persisted execution checks; being invalidated is never itself a process failure), enum labels + plain-language evidence naming the driving checks, never numeric — AND **per-statement FINAL statuses** persist at the same terminal-resolution moment (an additive field; the frozen statements JSON is never mutated; no-monitor terminal paths record an explicit honest `unknown`/`not_evaluated`). Both computed once at the same four terminal paths, persisted (**schema v6** versioned migration, proven vs a committed v5 fixture), served VERBATIM only by this same `GET /research/journal/{id}`; pre-v6 resolved theses carry the keys ABSENT (honest omission, never backfilled, never computed at read) |
| 20 | **Excursion outcomes** (R-unit ternary per horizon; spread-at-mark; truncation flags; confirmation- vs entry-anchored populations never pooled) | Excursion calculator at marks / first confirmation / stream end; persisted | `GET /research/journal/{id}` | analytics aggregates persisted rows only. **Iter-15 (build-out, additive):** ships — single owner `apps/backend/app/research/excursions.py`, an in-memory tracker fed only by the research-monitor observer (read-only over the engine): the **confirmation population** arms once at the first published `confirming` timeline event (reference = that event's persisted `last`; spread-at-anchor stamped once at arming, the row-18 moment-value pattern); the **entry population** arms once at the recorded row-18 entry mark (verbatim price; reuses its stamped `spread_at_mark`); **R basis = the same single helper row 27 uses** — never a second formula; ternary outcome per config horizon (`excursion_horizons_seconds`, documented research defaults, in `config_fingerprint`) resolved by **first touch** in logical time; horizons truncated (flagged, never extrapolated, never bridged) at stream end or row-16 gap events; persisted ONCE via the proven `compute_and_persist_*` seam (four terminal paths + the stream-end survival path for entry-marked surviving theses; tracker-unavailable paths persist an explicit not-tracked marker, never fabricated numbers) on **schema v7** (versioned migration, proven vs a committed v6 fixture + persistent-DB check); pre-v7 theses carry the key ABSENT (honest omission, never backfilled, never computed at read); served VERBATIM only by this same `GET /research/journal/{id}`; the `/journal/[id]` excursion section renders the two populations as separate blocks and computes nothing |
| 21 | **Journal rows + analytics aggregates** (segregated by `data_feed` + `config_fingerprint`; abandonment bucket always visible; "insufficient sample" under min-n) | Analytics module over persisted rows ONLY | `GET /research/journal`, `GET /research/analytics` | `/journal` renders; no pooling across feeds/fingerprints. **Iter-12 (additive):** the journal-rows half ships — ONE row-projection function over the persisted theses rows (id, ticker, bound source, `data_feed`, `config_fingerprint`, setup, direction, declared logical + wall timestamps, status, resolution incl. the VERBATIM persisted expired/interruption reason, entry/exit-mark presence), computed once server-side from persisted records only (nothing recomputed at read) and served ONLY by `GET /research/journal` (filter/pagination params per goal.md; default/max page size config-owned, serving-only — excluded from `config_fingerprint` with documented rationale); `/journal` renders rows verbatim (dates dd-MM-yyyy via the one shared formatter; setup/direction/resolution display labels from row-24 taxonomy, frontend hardcodes none). Grade/reviewed fields land as additive keys with rows 19/24 journeys (J-56/J-57) — honest omission until then. The aggregates half (`GET /research/analytics`, J-59) remains to be built. **Iter-16 (build-out, additive):** the aggregates half ships — single owner `apps/backend/app/research/analytics.py`, computed at read over persisted rows ONLY (theses, verdict_events, actions, row-28 saved reviews, row-20 excursion records; nothing underlying is ever recomputed) and served ONLY by `GET /research/analytics`; payload is structurally partitioned by (`data_feed`, `config_fingerprint`) — no pooled rollup exists anywhere — with per setup × direction groups: n + always-visible abandonment bucket, ternary excursion distribution per horizon from the confirmation-anchored population (truncated counted separately, never pooled/extrapolated), median time-to-confirm (logical time, from the persisted timeline; honest omission when no confirmation), row-28 tag frequencies (user-confirmed only), the acted-trade R distribution kept structurally apart (values via the row-27 `marks.py` function — never a second formula), and median spread/R (persisted `spread_at_anchor`/`r_basis`) beside every +1R figure; groups under the config `analytics_min_sample_size` serve an explicit insufficient-sample marker with n still shown (the key is serving/presentation-only and excluded from `config_fingerprint` with documented rationale + a fingerprint-stability test — the iter-12 page-size precedent); `/journal`'s analytics view renders the payload verbatim (display rounding only) with copy from row 24; no currency, no equity curve, no win-rate-as-edge anywhere. **Iter-14 (additive):** the pre-announced `grades` + `reviewed` keys ship on the same single row projection (read from the row-19/row-28 persisted values, never recomputed; honest omission for pre-grade/unreviewed rows) |
| 22 | **Hints** (pattern, evidence, baseline citation or "no studied baseline") | Hint engine — produced once when shown; every shown hint logged | hint log (journal store; read via `/journal` hint log) | hint dock + log read the same record; sustain-dwell + cooldown gated. **Iter-23 (build-out, additive):** ships — single owner `apps/backend/app/research/hints.py`, a pure logical-time evaluator driven by the research monitor's observer seam (engine untouched, byte-identity preserved; exception-isolated → `monitor_status: failed`); state-native patterns ONLY (sustained bid/ask_absorption → absorption_reversal long/short; sustained buyer/seller_control → trend_continuation long/short; unclear never); `hint_sustain_dwell_seconds` + `hint_cooldown_seconds` are config-owned documented research defaults, IN `config_fingerprint` (they shape persisted records — the study-arming precedent); the record (pattern, measured plain-language evidence, setup context + direction, baseline citation, row-26 stamps, logical + wall ts) is produced ONCE at fire and persisted to the existing v7 `hints` table via the single writer queue; the baseline citation reads PERSISTED row-23 `done` study aggregates for that setup+feed+fingerprint (hindsight_level excluded) verbatim, else exactly "no studied baseline — unvalidated pattern"; the active hint clears on state exit, watch stop, and any non-live status flip (J-64 freshness discipline) — the log record is unaffected; served by **`GET /research/hints/active?ticker=`** (`hint: null` normal) == the additive **WS `hint` key** VERBATIM (the row-15 precedent) for the dock, and **`GET /research/hints?ticker=&limit=&offset=`** (persisted rows verbatim; journal serving-only page-size pattern) for the log; `POST /research/thesis` gains an optional additive `declared_from_hint_id` (valid → thesis links + hint marked declared-from via the writer queue; unknown → 422; a click alone never creates a thesis). **Iter-24 (additive):** the `/journal` hints view displays each stored row's `data_feed` stamp — the persisted row-26 stamp rendered verbatim (labels from row 24), never recomputed |
| 23 | **Study results** (occurrence rows, aggregates, seeded null baseline, status/progress) | Study runner (cancellable background job); persisted | `POST/GET /research/studies`, `GET /research/studies/{id}`, `POST …/cancel` | `/studies` renders stored results; deterministic re-runs. **Iter-18 (build-out, additive):** ships — single owner = the study-runner module under `app/research/` (an unpaced fresh-`TapeEngine` replay over the chosen source via the existing observer seam ONLY; engine/classifier/providers untouched, byte-identity preserved); sources = the committed PG SIP reference window (credential-free) · seeded sims · arbitrary symbol+window via the existing historical seam; state-native auto-arming for absorption_reversal/trend_continuation (config-owned thresholds, IN `config_fingerprint`); level setups require a user-supplied level → stamped **`hindsight_level`**, excluded from cross-study aggregates; per-occurrence verdict summaries via the EXISTING per-setup rule tables + excursions via the row-20/row-27 single helpers (the study is a registered consumer, never a second formula; occurrence R basis = a config-owned deterministic rule at arm time, identical for setup and null arms); the **null-baseline seed is recorded on the study record** (`study_null_arm_count` config, IN fingerprint) so results are byte-deterministic per (source, `config_fingerprint`, seed); status enum `queued \| running \| done \| cancelled \| failed` with progress, cancelled = explicit + partial-marked, failed = explicit error; stamps (bound source, `data_feed`, `config_fingerprint`, seed) persist once at creation, results persist once at completion/cancellation — never recomputed at read; a committed CI test pins the reference study's exact rows + aggregates (J-62) |
| 24 | **Taxonomies + research display copy** (setups, flags, tags, verdict/stance enums) | Backend taxonomy module | `GET /research/taxonomy` | frontend hardcodes none of them. **Iter-13 (additive):** the **mistake-tag catalog** ships (`chased`, `entered_before_confirmation`, `ignored_rejection`, `ignored_risk_flags`, `moved_invalidation` *(self-assessed)*, `no_clear_setup`, `wrong_setup_type`, `overstayed`, `other` + required note) with display copy, served by this same endpoint; the review SAVE flow (`POST …/review`) lands with J-57. **Iter-14 (additive):** the review SAVE flow ships as row 28; grade/final-statement-status display copy is served additively by this same endpoint (frontend hardcodes none). **Iter-15 (additive):** excursion display copy ships via this same endpoint — ternary-outcome labels (`+1R_first \| −1R_first \| neither_within_horizon`), the `truncated` label, not-tracked / not-applicable copy, and the two population titles; the frontend hardcodes none. **Iter-16 (additive):** analytics display copy ships via this same endpoint — partition/group titles, abandonment-bucket label, insufficient-sample copy, truncated-label reuse, the spread/R caption, the journaled-measurements framing line, and the split honest-absence fallbacks (“not yet resolved” vs “predates the feature”). **Iter-18 (additive):** studies display copy ships via this same endpoint — study status labels (each status its own explicit copy), the `hindsight_level` label ("level chosen with hindsight — illustrative"), truncated-label reuse, the null-baseline caption, and the journaled-measurements framing for `/studies`; the frontend hardcodes none. **Iter-20 (additive):** management-stance display copy ships via this same endpoint — the three stance labels + evidence templates (present-tense, factual, never imperative or predictive), the journaled-measurement register for distance/open-R, and the two distinct honest-absence copies (“no entry mark yet” vs “not currently evaluated”); the frontend hardcodes none. **Iter-21 (additive):** entry-checklist display copy ships via this same endpoint — the eight check labels + per-check margin captions, the four checklist-stance labels (`conditions_met \| conditions_not_met \| tape_against \| no_fresh_tape`) + factual evidence templates ("N/8 checks pass" register), the nearest-counterevidence template, and the checklist honest-absence copy; ThesisStrip's three hardcoded `journaled measurement…` caption literals consolidate to the already-served `stance_readout_caption` (closing the iter-20 coherence advisory); the frontend hardcodes none. **Iter-23 (additive):** hint display copy ships via this same endpoint — pattern labels, present-tense descriptive evidence templates (never imperative or predictive), the exact "no studied baseline — unvalidated pattern" string, the baseline-citation template, the dock title + "Descriptive only — not trading advice" register line, the declared-from label, and the hint-log column labels + honest empty-state copy; the frontend hardcodes none. **Iter-24 (additive):** feed-basis display copy ships via this same endpoint — per-feed badge labels (sim \| iex \| sip) and the live IEX-vs-SIP disclosure line (“live verdicts read the single-venue IEX feed; historical replay and studies use SIP — spreads and prints differ”); the frontend hardcodes none. **Iter-25 (additive):** sound-cue display copy ships via this same endpoint — the explicit toggle label, the off-by-default / transition-only description, and the served `sound_cue_cooldown_seconds` value, reusing the existing “Descriptive only — not trading advice” register; any copy correction surfaced by the J-66 all-surface walk is fixed at this taxonomy source; the frontend hardcodes none |
| 25 | **Entry checklist + management stance** (named checks with live margins; `conditions_met/…/no_fresh_tape`; `thesis_intact/…`) — **cue layer, built LAST (after J-58–J-62 pass)** | Stance evaluator, computed once server-side; publishes through its own dwell | row 15 projection (additive keys) | UI renders margins verbatim, derives nothing. **Iter-20 (build-out, additive):** the **management-stance half ships** (J-53) — single owner = the stance evaluator module under `app/research/`, driven by the research monitor (observer-only; engine untouched), derived EXCLUSIVELY from the latest row-16 published verdict of an entry-marked, unresolved thesis (`confirming → thesis_intact`; decay → `thesis_weakening` carrying the verdict's evidence; `invalidated → thesis_invalidated`, terminal; the pending case never reads intact); publishes through its own config-owned logical-time dwell with `thesis_invalidated` dwell-exempt; served as additive keys (`management_stance`, `distance_to_invalidation` in $ and R, `open_r`) on row 15's single `build_projection` (REST + WS verbatim); NEVER persisted (schema stays v7, `verdict_events` untouched); keys absent without an entry mark and on the surviving not-evaluated path (two distinct honest-absence copies). The entry-checklist half (named checks + live margins + `no_fresh_tape` freshness + row 14 `delivery_lag_seconds`) remains for J-63/J-64. **Iter-21 (build-out, additive):** the **entry-checklist half ships** (J-63) — same single owner module (`app/research/stance.py`), driven by the research monitor (observer-only; engine untouched); eight named checks composed ONLY of registered canonical values (row-16 published verdict; warm-up vs the classifier's own `warmup_min_events`; row-6 `stream_status` for `feed_live`; row-14 `delivery_lag_seconds` vs its config bound for `tape_lag_ok`; row-3 spread vs the classifier's own stability gates in bps; trade speed vs the classifier's own floor; invalidation distance in spread-multiples vs `invalidation_too_tight_spread_multiple`; chase return anchored at the recorded `rule_first_true` price vs `chase_return_threshold`), each serving its live measured margin in its own units — never a bare boolean; aggregate checklist stance publishes through its OWN config-owned dwell (`tape_against` on rejecting; `no_fresh_tape` whenever `feed_live`/`tape_lag_ok` fail — a prior green never persists over non-live data); a server-computed **nearest-counterevidence** line names the closest condition that would flip the read; shown ONLY for an active, monitor-evaluated, NOT-entry-marked thesis (mutually exclusive with the management stance; keys absent on no-thesis / entry-marked / not-evaluated paths); served as additive keys on row 15's single `build_projection` (REST + WS verbatim); NEVER persisted (schema stays v7). The J-64 freshness journey verification (pause/closed/stale forcing legs + the visible lag readout) lands next. **Iter-22 (build-out, additive — J-64):** the freshness WIRING is fixed in the same single owner seam (research monitor + its `build_checklist` call sites): every `on_status` flip (incl. non-terminal paused/stale and the resume restore) brings the served checklist into agreement with the CURRENT canonical row-6 `stream_status` / row-14 `delivery_lag_seconds` — a READ of the registered owners, never a second computation — so `no_fresh_tape` publishes immediately (dwell-exempt) and a prior green never persists over non-live data; resume re-greens only through the existing dwell on fresh evidence; closed/failed keep their honest-by-removal terminal paths; the checklist stays NEVER persisted (schema stays v7), still served only as additive keys on row 15's single `build_projection` |
| 26 | **Source / `data_feed` / `config_fingerprint` stamps** | Assigned once at record creation (fingerprint hashed over the ENTIRE frozen config) | stored on every research record; shown wherever the record is shown | a thesis is never evaluated against a different source than declared. **Iter-24 (additive):** the scenario→`data_feed` stamp mapping consolidates to ONE shared function (the `hints.py` local copy folds into it — single owner, placed to avoid the monitor↔hints import cycle) and becomes config-aligned: it reads the config-owned per-mode feed keys (`live_feed` / `historical_feed`; sim stays `sim`) instead of hardcoded literals, so a SIP-entitled operator upgrading live is ONE config value with no relabeling code (defaults unchanged ⇒ all existing stamps and pinned tests byte-identical). **Iter-25 (additive):** the LAST hardcoded feed literals fall — the study-creation pre-stamp sites (`routes.py` reference/historical kinds, previously literal `"sip"`) read this same single config-aligned mapping; defaults unchanged ⇒ stamps and the pinned reference-study results stay byte-identical |
| 27 | **Realized move in R + R basis + spread-at-mark display** (R = \|entry − invalidation\|; realized move signed by direction; present ONLY when the marks exist — no marks ⇒ no realized metric, no dishonest zero) | ONE research projection function, computed once server-side from row-18 recorded marks + the thesis invalidation (no client-side arithmetic) | row 15 projection + `GET /research/journal/{id}` | strip renders verbatim, labeled a journaled measurement with spread-at-mark beside it — never currency P&L; distinct from row 20 (excursion horizon populations); feeds row 21's acted-trade R distribution later via the same computed values, never a second path. **Iter-15 (additive):** the R-basis helper is now shared with row 20's excursion calculator — one function, one formula, two registered consumers. **Iter-16 (additive):** row 21's acted-trade R distribution becomes the third registered consumer of the same row-27 function — never a second path. **Iter-18 (additive):** the study runner (row 23) becomes the fourth registered consumer of the same row-27 function — never a second path. **Iter-20 (additive):** the management-stance live readouts (distance-to-invalidation in R, open R signed by the same convention as the realized move; row 25) become the fifth registered consumer of the same row-27 `r_basis` function — never a second formula. **Iter-21 (additive):** the entry checklist's invalidation-distance check (row 25) becomes the sixth registered consumer of the same row-27 `r_basis`/distance basis where it needs the entry-reference distance — never a second formula |
| 28 | **Saved review** (user-confirmed mistake tags + note + `reviewed` flip) — *iter-14* | Recorded ONCE by `POST /research/thesis/{id}/review` (tags validated against row-24 taxonomy; `other` requires the note → 422; 409 unless resolved; persisted verbatim; the append-only `verdict_events` surface untouched) | `GET /research/journal/{id}` (+ the `reviewed` key on row 21 rows) | distinct from row 19's machine-SUGGESTED tags — the user confirms, the system never tags on its own; tag labels render from row 24 everywhere they appear |
| 29 | **Current-watch feed basis** (sim \| iex \| sip) shown as the cockpit feed badge — *iter-24, J-67* | The ONE consolidated scenario→`data_feed` mapping (row 26's function), a pure projection of the row-6 watched-source descriptor — computed once server-side, never client-derived | additive metadata field on the row-6 snapshot projection (`GET /tape/{t}/summary`, re-exposed by WS verbatim) | the badge renders the served value verbatim with row-24 copy (disclosure line on the live IEX basis); the iter-9 `end_reason` / iter-21 `delivery_lag_seconds` precedent — projection/display metadata only, never read by classification; observer signature unchanged, equivalence suite green, zero re-pins |

**Persistence (scoped).** Journal store = stdlib `sqlite3` (WAL, `busy_timeout`, `BEGIN IMMEDIATE`,
one writer queue — never written from event processing or the WS serialization path). Tables:
theses, verdict_events (append-only), hints, actions, studies, study_occurrences, schema_version.
Env-configured DB path; tests inject a temp path. No tape data persisted (committed fixtures excepted).
**Schema evolution ships a versioned migration** (bump `journal_schema_version` + in-place `ALTER`
of older DBs in one writer transaction, never backfilling append-only rows), proven by a test against
a committed old-schema fixture — `CREATE TABLE IF NOT EXISTS` alone is never a migration (iter-4 lesson).

**New sim scenarios.** `SIM-SHIFT` and `SIM-REVERSAL` (capability 21) are provider-level only —
seeded, documented like the existing five; the engine is untouched.

**Config.** Every new research value (per-setup dwell, stance dwell, chase threshold, invalidation
ε / k robustness, hint sustain + cooldown, excursion horizons, timeline cap, min sample size,
delivery-lag bound, study null-arm count) lives in config as a documented research default —
no magic numbers in research code. *Iter-18 (additive):* the study arming thresholds and the
occurrence R-definition rule join this list — all IN `config_fingerprint` (they shape persisted
study results); only genuinely serving-only keys may be excluded, with the documented
rationale + stability-test + counter-test pattern. *Iter-20 (additive):* the management-stance dwell joins this list as a documented research default; the stance is never persisted, so the key is serving-only — any exclusion from `config_fingerprint` follows the same codified rationale + stability-test + counter-test pattern. *Iter-21 (additive):* the entry-checklist stance dwell and the delivery-lag bound join this list as documented research defaults; the checklist is never persisted, so both are serving-only-exclusion candidates — any exclusion from `config_fingerprint` follows the same codified rationale + stability-test + counter-test pattern, and the default remains IN the fingerprint otherwise. *Iter-23 (additive):* the hint sustain dwell + cooldown (already named in this list) ship as documented research defaults and are IN `config_fingerprint` — they shape persisted hint records (the study-arming precedent); no exclusion is requested. *Iter-24 (additive):* the `hint_log_max` serving-only exclusion gains its codified assurance pair (fingerprint-stability test + real-threshold counter-test — the `study_list_max` precedent), making the existing config comment's claim true; and the per-mode feed keys (`live_feed` / `historical_feed`) — already IN `config_fingerprint` — become the single source the row-26 stamp mapping reads. *Iter-25 (additive):* `sound_cue_cooldown_seconds` joins this list as a documented research default served via the row-24 taxonomy payload; the cue is never persisted, so the key is serving-only — any `config_fingerprint` exclusion ships the codified rationale + stability-test + counter-test pair in the same commit.
