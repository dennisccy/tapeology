# Assumption Ledger — session tradable_wall

## iter-0 — goal-evaluator

**Ambiguity:** The iteration spec instructs recording credential-gated J-03 and J-06 as `blocked` (Alpaca env unset, not simulated), but the journey-history status vocabulary (`passing`/`failing`/`partial`/`already_passing`/`regressed`/`unknown`) has no `blocked` value.
**We chose:** `failing` for both — there is positive evidence their features are entirely absent at baseline (setups.py/recorder path for J-03; PriceChart overlay+chip for J-06), so they are definitively not-passing, not merely untested. The credential gate is preserved as a `note` field on each journey rather than as the primary status. `unknown` was rejected because it means "not tested this iteration; carry over" — but both were exercised and found absent.
**Reversible:** yes

## iter-3 — goal-evaluator

**Ambiguity:** J-03's acceptance says ">=10 event-window datasets EXIST ... the pinned event's drill-in SHOWS the five-state timeline at the 300-test." Alpaca creds turned out present (unexpected), so the credentialed recording genuinely ran — but the process was interrupted, producing 15 real datasets in an ephemeral pytest temp dir (GC-eligible, NOT the persistent store), with only a JPM proxy timeline shown and the pinned-AAPL drill-in never demonstrated. Does "exist"/"shows" require durable persistence in the canonical store + the specific pinned-AAPL drill-in, or is a demonstrated-but-ephemeral recording run enough to score the credentialed headline met?
**We chose:** The stricter reading — the credentialed headline is met only when the datasets PERSIST in apps/backend/.data/datasets/ (append-only/checksummed/split-frozen, re-openable) AND the pinned-AAPL 06-22 drill-in five-state timeline is demonstrated end-to-end. Under this bar the credentialed portion is not-yet-met, so J-03 = partial (keyless substrate passing), matching the auditor's explicit recommendation and the spec's own stated expectation ("land partial, not full passing"). The dev/QA "MET" framing was not accepted. A human who considers a demonstrated-but-ephemeral real run sufficient could reverse this to passing.
**Reversible:** yes

## iter-4 — goal-decomposer

**Ambiguity:** J-04's acceptance ("the report compares all three strategies on identical data ... each cell either has n>=5 or is labelled insufficient_sample ... an all-insufficient report is a valid outcome") plus the tag "(Keyless via the committed fixture; full run credentialed)" leaves open whether J-04 can be scored passing on the KEYLESS committed-fixture run alone (one recorded window -> cells honestly all insufficient_sample), or whether the credentialed >=10-window recorded data (operator-gated, tied to J-03's still-blocked credentialed portion) is required before J-04 can pass.
**We chose:** The keyless reading — this iteration builds the full 3-way report machinery (structure_tape_map registration + additive arming path + edge_report.py 3-way extension + GET /research/edge-report + MCP proxy) and verifies it end-to-end over the committed datasets_j03/ fixture, treating a correct, gate-honoring, all-insufficient_sample report as J-04's passing core (success criterion 5 and the acceptance line both state an all-insufficient / empty-survivor report is a valid outcome). The credentialed >=10-window enrichment (richer n>=5 cells) is an operator-gated carry parallel to J-03's blocked recording, not a blocker for J-04's core acceptance. A human who requires credentialed multi-window data before J-04 passes could reverse this to partial.
**Reversible:** yes

## iter-4 — goal-evaluator

**Ambiguity:** J-04's acceptance ("the report compares all three strategies on identical data ... each cell either has n>=5 or is labelled insufficient_sample ... an all-insufficient report is a valid outcome") tagged "(Keyless via the committed fixture; full run credentialed)" leaves open whether the KEYLESS committed-fixture run must produce a POPULATED (non-empty) all-`insufficient_sample` report, or whether a vacuously-EMPTY report (`cells: []`) on the literal fixture — plus a synthetic-panel proof of the populated cell structure — satisfies J-04's passing bar. The literal committed `datasets_j03/` fixture (symbol PG, not a config-owned panel symbol) yields `cells: []` under the real panel; the populated cell shape is proven only by a test-local panel override.
**We chose:** The empty-is-valid reading → J-04 = passing on its keyless core. The goal explicitly and repeatedly names an empty / all-`insufficient_sample` report a valid, publishable outcome (Success Criterion 5, the J-04 acceptance line, the "No gate bending for a headline" anti-goal), and DoD item 1's "exact cell structure" assertion is met by `test_synthetic_scan_join_produces_real_cells_all_insufficient_sample` (real PG ticks + test-local panel). Every REQUIRED acceptance element (3-way, full register, no train/holdout pooling, no feed pooling, n>=5-or-insufficient, no gate weakened, champion untouched, MCP byte-identical) is delivered and independently verified; the credentialed populated-cell enrichment is treated as the operator-gated carry parallel to J-03, not a J-04 blocker. A human who requires the committed fixture (or a credentialed panel-symbol recording) to produce a genuinely NON-EMPTY report before J-04 passes could reverse this to `partial`.
**Reversible:** yes

## iter-5 — goal-decomposer

**Ambiguity:** The goal (J-02/J-05 acceptance, Data Contract) is silent on how a touch event in the most-recent stored session should be presented when its reaction is computed from a truncated sub-horizon (`setups.py` caps the reaction read at `min(touch_index + horizons[0], len(all_bars)-1)`) while its horizon-0 forward return honestly reports `None` — the audit-B1 case, 13/801 live events. Should such a boundary event surface its (shorter) effective horizon, have its reaction flag-suppressed, or be excluded entirely from the registry/edge-report?
**We chose:** Additive disclosure — the event KEEPS its existing `reaction` label and `forward_returns`, and additively carries the effective reaction horizon + a boundary flag so the iter-6 UI can render it honestly as truncated-horizon. No label mutation, no exclusion. This preserves byte-identity of every non-boundary event (J-02 pinned AAPL 06-22 unchanged; J-04 edge-report cells unchanged) and keeps the gate-honesty rails intact, while resolving the "definitive label beside None returns" inconsistency by disclosing it rather than hiding it. A human who prefers suppressing the boundary reaction or excluding boundary events from the registry could reverse this.
**Reversible:** yes

## iter-6 — goal-decomposer

**Ambiguity:** J-05 says "`/structure` decluttered" and the Vision says `/structure` "defaults to the clean map + case browser + edge report" — leaving open whether the era-5 **Registry** and **Comparison** sections (era-5 J-02/J-03, currently on the page below the levels/zones) must be REMOVED to declutter, or whether "declutter" means only that the raw all-levels rendering moves behind a toggle while everything else stays.
**We chose:** The non-regressing reading — "declutter" applies to the raw levels/zones rendering only (it moves behind an explicit off-by-default "raw levels" toggle, restored byte-identically). The era-5 Fetch control, provenance badge, Registry, and Comparison sections are PRESERVED intact (Foundation invariant: "the existing surfaces … `/structure` stay intact"; J-07 sentinel), positioned below the new Tradable Map (default) · Case Studies · Edge Report sections so the map still leads the page. A human who reads "declutter" as stripping `/structure` down to only map+cases+edge-report could reverse this.
**Reversible:** yes

## iter-6 — goal-evaluator

**Ambiguity:** J-05's acceptance says the pinned AAPL 06-22 drill-in "shows `rejected` with its forward returns (and the tape timeline once J-03 ran)". J-03's credentialed >=10-window recording has not durably landed (still `partial`, operator-gated), so the pinned drill-in's tape timeline renders the honest empty-state "No recorded tape for this event." rather than a populated five-state timeline. Is J-05 fully `passing` with an empty tape timeline on the pinned case, or only `partial` until J-03 populates it?
**We chose:** `passing`. The J-05 acceptance parenthetical explicitly conditions the tape timeline on "once J-03 ran", and the phase spec's DoD + TESTING REQUIREMENTS name the honest `tape_timeline` empty-state a sanctioned pass condition ("the drill-in shows `tape_timeline` when a recorded dataset exists and an honest empty-state when it does not"). Every J-05-owned element — Tradable Map default with <=10 bands, raw-levels toggle, Case Studies registry+filters+drill-in, boundary-event truncation disclosure, honest-empty Edge Report, era-5 fetch/badge, zero client recomputation — is verified present; the populated-tape-timeline dimension is J-03's operator-gated deliverable, tracked separately as its own `partial` journey, not a J-05 blocker. A human who requires the pinned case to display a populated tape timeline before calling J-05 complete could reverse this to `partial`.
**Reversible:** yes

## iter-7 — goal-decomposer

**Ambiguity:** J-06 says the chip mapping and labels are "read from GET /research/strategies, never hardcoded", but that endpoint serves the config-owned rejection/breakthrough state-by-direction mapping (structure_tape_map entries.rejection_states / entries.breakthrough_states, config.py:1357-1362) and NOT a separate human-readable display-label field. Does honoring "labels ... never hardcoded" require a backend change to add a served label field, or is it satisfied by reading the served mapping plus rendering the served tape-state token?
**We chose:** The no-backend-change reading — the served mapping IS the confluence vocabulary: the chip decides which state confirms a band side from entries.rejection_states / entries.breakthrough_states and renders the tape-state token served by /tape/{ticker}/history (cosmetic title-casing of a served token is an allowed re-format of an endpoint value, not hardcoded vocabulary). No new served label field is added, so J-06 stays pure-frontend and config_fingerprint stays 4d665603569b9dbf. A human who requires /research/strategies to serve explicit display labels before J-06 passes could reverse this.
**Reversible:** yes
