# Diff scan report

**Result:** CRITICAL — 1 critical, 0 warn

- **CRITICAL** `aws-access-key` in `bdocs/phases/goal-yahoo_fetch-iter-6.md`: fixtures contain **deliberately-planted fake secrets** (e.g. AWS's example key `AKIAIOSFODNN7EXAMPLE`)

_Generated by lib/scan_diff.py over the FULL iteration diff (including paths the bounded view excludes)._
