{
  "phase": "goal-referee-iter-4",
  "status": "blocked",
  "current_step": "closure_failed",
  "updated_at": "2026-08-15T05:23:10.114285Z",
  "started_at": "2026-08-14T21:16:31.363940Z",
  "cli": "claude",
  "blockers": [],
  "note": "J-03 fix implemented: permutation_test's exact-enumeration branch computed each combination's group-2 sum via total - g1_sum (a separately math.fsum-accumulated session total) instead of a direct math.fsum accumulation over the combination's own complement values -- disagreeing with _t_statistic's own direct math.fsum(group2) in the last representable digit, letting the TRUE observed grouping narrowly fail its own self-comparison and drop from the extreme count, so p could fall to HALF its own floor 2/(draws_used+1). Fixed: direct complement accumulation, matching _t_statistic's method. Empirically verified (20,000 seeded multi-session cases) that this fix alone still left ~7% of 3+-session cases able to violate the floor (naive cross-session += accumulation vs _t_statistic's math.fsum numerator) -- extended the fix to also combine per-session terms via math.fsum, closing violations to zero across 20,000+ cases; this second change is grounded in the spec's own blanket math.fsum-class-accumulation determinism clause and is disclosed explicitly in the dev handoff as an extension beyond the plan's literal text. Seeded (non-enumeration) branch byte-unchanged (out of scope, confirmed via diff). STATS_CORE_VERSION bumped referee-stats-v1->v2; attestation re-captured against the fixed build (values byte-identical for this specific 3-session fixture -- an honest empirical fact, not evidence of no change; re-verified by running run_oracle_attestation() live, not assumed). New tests/test_referee_stats.py (iter-4 TC-1/TC-2/TC-5/TC-6/TC-7/TC-8, labeled distinctly from this file's own pre-existing iter-3 TC-1..TC-19 to avoid ambiguity): the evaluator's exact minimal repro now returns p==2/7 not 1/7; a 3,000-case seeded property test (1-4 sessions, 2v2/1v4/4v1 shapes, all sidedness) finds zero floor violations (verified this generator/seed finds 12 violations against a faithful pre-fix reproduction, proving the test is meaningful); _draw_indices_without_replacement direct coverage; the n1>1,n2==1 seeded fast path verified via brute-force full-enumeration ground truth plus an independent general-Fisher-Yates reference (a literal same-keyed-stream match is mathematically impossible here -- the fast path consumes 1 randrange call/draw vs the general algorithm's n1 calls/draw, empirically confirmed to diverge -- so both estimators are checked against ground truth within a 6-SE tolerance instead); version-bump + stale-version-rejection test. New tests/test_referee_oracles.py (iter-4 TC-3/TC-4): a calibration case (S=5,n1=1,K=4) that genuinely enters the enumeration branch for the first time in this suite (every prior case uses S>=16, never triggering it) -- checked for both calibration and the exact floor on every one of 400 replications; a second, ANTI-conservative mutation fixture (independently reproducing the actual pre-fix subtraction bug) detected via floor violations on a 3,000-fixture batch, pairing with the existing over-cautious-only mutant. Whole oracle file: 83.3s (was 78.2s), within the 120s budget. Lead 1 closed additively in app/research/referee_evidence.py: one shared _is_stale_basis(...) predicate replaces playbook_occurrence_readiness()'s and playbook_observations()'s two independent copies of the identical (detector_basis, config_fingerprint) staleness check; both now additionally serve stale_basis_dates (empty on every fixture with no stale-basis record, including the real corpus); zero change to any other served field's value (tests/test_referee_evidence.py: extended the existing D3 fixture test (TC-9) + one new sibling test for playbook_observations (TC-10); every other pre-existing assertion unchanged). Lead 2 (_strategy_observation's epoch_anchor fallback) explicitly NOT touched, confirmed via git diff showing zero lines changed in _strategy_observation/strategy_observations/edge_report.py. Full backend suite: 2504 passed, 8 skipped, 0 failed, 0 errors in 257.0s -- exceeds the >=2,495 pass/8 skip floor. Config().config_fingerprint() unchanged at 08e471b10130e1e2; EXPECTED_TOOLS unchanged at 20; import-ban guard (iter-3) passes unmodified; git diff --stat confirms zero diff to every named frozen module (app/config.py, app/main.py, desk_playbook*.py, desk_forward.py, levels.py, tradability.py, setups.py, edge_report*.py, backtests.py, pnl_scan.py, any route file, docs/referee-statistical-spec.md) and zero new Config field/runtime dependency; git status shows exactly the 5 planned files changed, no store file, no secrets. No live-server/browser verification performed this iteration (backend-only, unconsumed by any route -- matches iter-3's own precedent); J-10's browser regression sentinel is this iteration's Required-still-passing item but is a QA-stage responsibility, not covered in this dev pass.",
  "changed_files": [
    "apps/backend/app/research/referee_stats.py",
    "apps/backend/app/research/referee_evidence.py",
    "apps/backend/tests/test_referee_stats.py",
    "apps/backend/tests/test_referee_oracles.py",
    "apps/backend/tests/test_referee_evidence.py",
    "docs/handoffs/goal-referee-iter-4-dev.md",
    "reports/phase-goal-referee-iter-4-implementation-summary.md"
  ],
  "tests_run": true,
  "browser_checks_run": true,
  "next_action": "ready_to_merge"
}
