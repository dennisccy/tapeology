{
  "phase": "goal-referee-iter-5",
  "status": "in_progress",
  "current_step": "dev_complete",
  "updated_at": "2026-08-15T07:55:00.000000Z",
  "started_at": "2026-08-15T06:38:00.000000Z",
  "cli": "claude",
  "blockers": [],
  "note": "J-04 implemented: new app/research/referee_null.py building both matched-null variants per docs/referee-statistical-spec.md Sec4. referee-null-tod-v1 draws K=REFEREE_NULL_ANCHORS_PER_OCCURRENCE (4) seeded anchors per eligible J-02 observation (referee_evidence.playbook_observations -- eligibility holds by construction since excluded/truncated leaves never become observations), same symbol/measurement-series (reconstructed by locating the RTH session bar carrying the signal's own recorded forward['at_utc'] epoch exactly -- finest 1m series first, then 5m, never re-deriving desk_playbook._measurement_anchor's own detection-adjacent logic), same ToD bucket, remaining-time-matched for fixed horizons (literal wall-clock distance to the session's own 16:00 ET close -- TC-4 hand-verified: 60 min remain at 15:00 ET, 55 min at 15:05 ET), ToD-bucket-only for to_close-family measures, excluding the trigger bar, without replacement via the IMPORTED desk_forward._draw_anchor_indices; measured through the IMPORTED desk_forward._measure_from at entry_kind='close' with the occurrence's own side sign via desk_playbook_features.side_sign (NOT desk_forward._side_sign -- that helper's support/resistance vocabulary would silently mis-sign every short-side playbook occurrence, a real bug caught and avoided, documented in the module docstring). referee-null-context-v1 additionally filters candidate anchors through the IMPORTED desk_playbook_context.BandMapResolver/band_context_block over the recorded band map (never re-derived locally, per anti-goal 6); an unresolvable map excludes the WHOLE occurrence, never falls back to the unfiltered ToD population; room_r borrows the paired occurrence's own risk distance (risk_source='paired_signal'). Minted all three named signature-bearing spec ids (referee-null-tod-v1, referee-null-context-v1, referee-test-perm-v1), each hashing its own full parameter blob read at call time. Built the append-only RefereeNullStore (keyed (observation_id, null_spec_signature); duplicate raises; no update/delete method anywhere, source-scan guard-tested), the durable terminal-state-only RefereeNullRunStore (its state enum genuinely includes 'cancelled', unlike desk_playbook_log.py's exclude-cancel convention -- matches this ledger's own pinned Data-Contract shape), RefereeNullComputeManager (single-flight PER null-spec, not process-global -- two DIFFERENT null-specs may run concurrently, the SAME one never does), run_null_build_and_record (resumable/idempotent -- an already-recorded key is skipped, never rewritten), and a CLI warmer. Non-finite anchor measurements are excluded-and-counted (non_finite_excluded_count), never propagate and never exclude the whole occurrence (T-5) -- deliberately different from referee_stats.py's new fail-loud door guard, documented in the module docstring's own paragraph on why. Extended referee_routes.py: GET /research/desk/referee/nulls (?id=), POST/GET/POST-cancel .../nulls/compute, GET .../nulls/runs -- GET-never-computes verified (manager snapshot stays idle after a GET); unknown/malformed null_spec_id refused 422 at every entry point. Three riders in referee_stats.py: min_attainable_p now 2/(draws_used+1) in exact-enumeration mode (was the unreachable 1/(draws_used+1)), unchanged at 1/(draws_used+1) in the seeded branch -- proven by a fresh >=1,000-case tail-regime sweep asserting the SERVED FIELD (not just p) equals the true floor with >=100 exact-floor hits (can-fail guard); one pre-existing iter-3 test's hand-fixture assertion corrected (0.25->0.5, this fixture's own math makes it identical to p) with an explanatory comment -- the one place this iteration edits a pre-existing assertion, a direct spec-mandated consequence of the fix, not scope drift; touches none of _ATTESTATION_EXPECTED's four pinned fields, no STATS_CORE_VERSION bump. Non-finite (NaN/inf) fail-loud guard added at _t_statistic (covering permutation_test/sign_flip_result/equal_weight_t, which all call it first) and explicitly at bootstrap_ci_occurrence/bootstrap_ci_cluster (checked BEFORE the min_clusters floor short-circuit so insufficient_sample can never mask a bad input) -- one shared helper pair, checked once per entry point. TC-8 tightened 6.0->3.5 SE (empirically verified during development: real fast path ~0.30 SE, independent general-algorithm reference ~0.17 SE off ground truth on the pinned seed); a new companion mutation test reintroduces the realistic 'dropped total- complement' n2==1 fast-path bug (entirely inside the test file) and proves its p deviates ~0.12 from ground truth, vastly outside the tightened band -- proving the tightened band actually discriminates a real regression. Import-topology guard corrected in test_referee_guards.py: split the combined detect-or-context ban into (a) an UNCHANGED blanket desk_playbook_detect ban and (b) a narrower desk_playbook_context ban that exempts ONLY referee_null.py, citing docs/goal.md's exact Read-side-law sentence in the code comment; the reverse-direction guard is byte-unchanged; added a can-fail counter-test for the new narrower rule; referee_stats.py's own separate stricter ban (TC-23) untouched. Full backend suite: 2553 collected, 2545 passed, 8 skipped, 0 failed, 0 errors (~252s, via --junit-xml since -q prints no summary line under this project's addopts=-q, the established practice every prior goal-referee-iter-* handoff records) -- exceeds the iteration-4 floor (2513/2505/8) by exactly +40 (29 new tests in test_referee_null.py, incl. a CLI smoke test with an idempotent-second-run check and an unknown-spec-id argparse-rejection check, + 9 in test_referee_stats.py + net +2 in test_referee_guards.py), confirming no stray/uncounted test collection (a draft bug where referee_null.py's test_perm_spec_parameters/test_perm_spec_signature functions were imported under their literal names and picked up by pytest's own test_* collection convention was caught via a PytestReturnNotNoneWarning during development and fixed by aliased imports). Config().config_fingerprint() unchanged at 08e471b10130e1e2 (verified live); test_mcp_server.EXPECTED_TOOLS unchanged at 20; test_copy_discipline.py/test_no_execution_path.py green; git diff --stat scoped to exactly referee_null.py (new)/referee_stats.py/referee_routes.py/test_referee_null.py (new)/test_referee_stats.py/test_referee_guards.py -- zero diff to desk_forward.py, desk_playbook.py, desk_playbook_context.py, levels.py, tradability.py, setups.py, edge_report*.py, backtests.py, pnl_scan.py, app/config.py, app/main.py, docs/referee-statistical-spec.md; zero new Config field. Required-still-passing check: test_referee_evidence.py (J-01/J-02) and test_referee_oracles.py (J-03) both re-run directly, green, unmodified. Live-verified against the real corpus (scripts/dev.sh, :8301/:3301): GET /research/desk/referee/nulls serves the honest empty state ({'records': [], 'integrity_errors': []}) -- no real-corpus null-build was run (explicitly OUT OF SCOPE this iteration); GET /research/desk/referee/evidence (J-01, unmodified) still serves the same real-corpus numbers every prior iteration recorded (210 records/156 sessions/3222 signals at current basis); all 5 new routes present in the OpenAPI schema. scripts/dev.sh started/stopped/restarted/stopped cleanly on :8301/:3301, full process trees (uvicorn --reload's WatchFiles worker child, next dev's npm-exec/sh-c/node/next-server chain) verified gone by exact PID both times (never a pattern-based pkill, per the host's own safety rule -- confirmed no impact on any other project's process), no port conflicts on the second start. J-10's browser regression sentinel (the full kept-product browser walk with fresh screenshots) was NOT performed in this dev pass, matching every prior goal-referee-iter-* dev handoff's own precedent that this is a QA-stage responsibility; J-04 itself carries no browser acceptance per its own spec text. Zero frontend files changed (no frontend handoff written). Known judgment calls flagged in the dev handoff's Known Issues for reviewer triage: the remaining-time boundary uses literal wall-clock distance to the 16:00 ET close (not bar-count-equivalent minutes) -- TC-4 hand-verifies this reading, simpler and more robust than an array-index formula; the seeded stream's hypothesis_id slot is filled with the null-spec id itself (no hypothesis exists yet at J-04) -- documented as a reversible pre-registration analogue; referee-test-perm-v1's minting function lives in referee_null.py per the iteration spec's own named-open implementation choice; window_overlap_fraction's exact formula is an original design (spec names the disclosure, not a formula) -- not gate-tested by any TC number.",
  "changed_files": [
    "apps/backend/app/research/referee_null.py",
    "apps/backend/app/research/referee_stats.py",
    "apps/backend/app/research/referee_routes.py",
    "apps/backend/tests/test_referee_null.py",
    "apps/backend/tests/test_referee_stats.py",
    "apps/backend/tests/test_referee_guards.py",
    "docs/handoffs/goal-referee-iter-5-dev.md"
  ],
  "tests_run": true,
  "browser_checks_run": false,
  "next_action": "review"
}
