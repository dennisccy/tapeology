{
  "schema_version": 1,
  "journey": "J-06",
  "name": "MCP contract v3 — 17 read-only tools",
  "default_timeout_ms": 8000,
  "notes": [
    "J-06 has NO browser-observable acceptance surface (goal.md marks it '(Keyless; automated.)') -- its real acceptance is entirely backend/MCP: exactly 17 mcp__tapeology__* tools advertised, desk_universe/desk_screen byte-identical to their curl equivalents in both empty AND populated fixture states, get_endpoint's /research/ allowlist proxying /research/desk/screen verbatim (plus its honest-error passthrough), and tests/test_mcp_server.py green. None of that is expressible as a goto/click/fill/expect browser script, so demo_runner.py CANNOT re-verify J-06's real contract -- only a human/LLM QA pass calling the actual mcp__tapeology__* tools (or curl) can. This script is therefore a best-effort, honest proxy only: it checks that the /desk route (the ONE UI surface whose existence the new desk_universe/desk_screen tools and the ui_route_map's 3-row contract both depend on) still renders with its nav intact -- a real but PARTIAL regression guard, not a substitute for the tool-level check. Re-run the actual MCP verification every iteration regardless of this script's verdict.",
    "Verified 2026-07-30 by browser-qa-agent (goal-desk-iter-26): 17 tools enumerated live (backtests, bars, datasets, desk_screen, desk_universe, edge_report, get_endpoint, levels, pnl_ledger, setups, strategies, tape_features, tape_history, tape_state, taxonomy, tradability, ui_route_map); desk_universe/desk_screen/get_endpoint('/research/desk/universe'|'/research/desk/screen'|'/research/desk/topup/runs') each matched curl byte-for-byte in BOTH an empty fixture-scoped rig and after populating it (3-symbol universe + a real top-up + a real screen compute, all on a fixture-scoped backend at :8000, never the ambient store); get_endpoint's honest-error passthrough confirmed via a real 404 ('Ticker AAPL is not being watched') proxied verbatim; tests/test_mcp_server.py: 38 passed.",
    "IMPORTANT environment finding (not a J-06/J-17 regression): at verification time the ambient frontend (:3301) was serving a STALE .next build whose client bundle has 'http://localhost:8000' baked in for NEXT_PUBLIC_API_URL, even though its own process environment correctly holds http://localhost:8301 -- confirmed live: /desk rendered 'Backend unreachable -- is the API running?' against the correctly-running :8301 backend the instant nothing was listening on :8000 (this browser-qa session had transiently occupied :8000 for its own fixture-scoped rig). This is exactly the T-9 stale-build trap goal.md warns about; a J-05.json note earlier in this same iteration already hit and remediated a DIFFERENT symptom of the same trap (a 500 crash). Fixing it (rm -rf apps/frontend/.next + restart) was attempted but blocked by the session's own permission classifier, so it is left unresolved -- this script's own replay, and every OTHER J-01..J-16 golden targeting :3301, will show a false FAIL until an operator/dev pass does the clean rebuild."
  ],
  "steps": [
    {"n": 1, "journey": "J-06", "action": {"type": "goto", "url": "/desk"}, "expect": {"text": "Desk"}},
    {"n": 2, "journey": "J-06", "action": {"type": "expect", "target": {"role": "link", "name": "Cockpit"}}},
    {"n": 3, "journey": "J-06", "action": {"type": "expect", "target": {"role": "link", "name": "Structure"}}}
  ]
}
