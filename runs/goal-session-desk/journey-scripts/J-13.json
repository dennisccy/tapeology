{
  "schema_version": 1,
  "journey": "J-13",
  "name": "Every ranked briefing row states the price its wall sits at and the close it was measured from",
  "default_timeout_ms": 15000,
  "notes": [
    "Verified goal-desk-iter-17 by browser-qa-agent (2026-07-29) live against the running rig at backend :8301 / frontend :3301, serving the real ambient apps/backend/.data/screen store. The live latest snapshot (screen-2026-07-28-ac07c9581a4f, 63 ranked rows) entirely predates this iteration's code, so every one of its rows honestly shows the new band column's fallback text 'close not recorded in this snapshot' -- confirmed via DOM eval over all 63 ranked rows (0 populated). The ranked table's header row was confirmed to carry exactly 10 cells ending in 'band' (symbol, side, class, distance, score, coverage, tick evidence, basis, history, band). The row's composite hover-tooltip title attribute was confirmed to append the close/band segment immediately after the history segment and before the coverage segments, with no separate title on the band <td> itself.",
    "The POPULATED case (an in-band row and an out-of-band row, both legible in one screenshot) was independently verified this same run on a fixture-scoped rig (backend :8392 / frontend :3392, TAPEOLOGY_BAR_DIR/TAPEOLOGY_DESK_UNIVERSE_DIR/TAPEOLOGY_DESK_SCREEN_DIR/TAPEOLOGY_DATASET_DIR all pointed at a temp copy of the real bar/universe stores, never apps/backend/.data) by computing a NEW screen for screen_date=2026-07-28 against that scoped store: BRK-B came back in-band (band 488.50-490.85 . close 490.85, distance 0.00 bps) and LIN out-of-band (band 506.33-509.61 . close 506.32, distance 0.20 bps) -- see reports/qa/goal-desk-iter-17-evidence/UT-05-result.png. That scoped rig's data is NOT reachable at future replay time (a one-off temp copy), so this golden intentionally pins to the PERMANENT legacy state instead, which is guaranteed stable forever by the append-only rail.",
    "This script clicks a Screen History row, which is SAFE to replay repeatedly: selecting a row only drives GET /research/desk/screen?id=<id>, a plain read that recomputes nothing and writes nothing. The pinned id (screen-2026-06-22-3ecd45c062c7) is the era's earliest recorded screen (iter-3) -- permanently a legacy row under the append-only rail, so its band cells will read the exact fallback text forever, making this the most future-proof anchor for a regression replay of this journey.",
    "Step 4 is a post-match liveness check (this era's iter-4/iter-5 lesson): a further /desk navigation AFTER the key assertion matches, proving the page is still alive and rendering correctly."
  ],
  "steps": [
    {"n": 1, "journey": "J-13", "action": {"type": "goto", "url": "/desk"}, "expect": {"text": "Desk"}},
    {"n": 2, "journey": "J-13", "action": {"type": "click", "target": {"css": "tr[data-screen-id=\"screen-2026-06-22-3ecd45c062c7\"]"}}, "expect": {"text": "screen-2026-06-22-3ecd45c062c7"}, "timeout_ms": 10000},
    {"n": 3, "journey": "J-13", "action": {"type": "expect", "text": "close not recorded in this snapshot"}, "timeout_ms": 8000},
    {"n": 4, "journey": "J-13", "action": {"type": "goto", "url": "/desk"}, "expect": {"text": "Desk"}}
  ]
}
