{
  "schema_version": 1,
  "journey": "J-03",
  "name": "The screen \u2014 pinned inputs, append-only snapshot, deterministic rank",
  "default_timeout_ms": 15000,
  "notes": [
    "UPDATED (one snapshot per date) by developer: Screen History is now a year-at-a-glance calendar, not a table -- the backend settles exactly one snapshot per screen date (app/research/desk_screen_decision.py), so the near-duplicate same-date rows the table existed to tell apart no longer exist. This step's `desk-history-table` / 'date' header pin retargets to the calendar's own container and its statically-rendered 'Jan' month label -- a shell string present in the first paint, deliberately NOT async-list or <option> text (the era-5 headless-matcher lesson).",
    "UPDATED (desk layout reflow) by developer: step 2's page-wide 'Class A' would now first resolve inside the Forward Returns table, which renders its own `Class <x>` chip per ranked row and moved above the briefing in this reflow. The step would have stayed green via the wrong element -- it would have stopped proving the chip renders in the briefing at all. Now scoped to a desk-row-band-class cell inside desk-screen-rows-table: same assertion, honest resolution.",
    "UPDATED (desk section collapse) by developer: six /desk sections now render COLLAPSED -- Screen Runs, Screen Comparison, Provenance, Index Reconciliation, Top-up Runs and Playbook Evidence are on the page and named, but closed by default, and each one's own GET is deferred until its first expand. Their headings stay VISIBLE while closed, so a step that pins a section TITLE still resolves untouched; only a step reading a section's CONTENTS needs the expand click inserted before it, because the replay runner resolves every target with state=\"visible\". This is a deliberate product-shape change, not a regression."
  ],
  "steps": [
    {
      "n": 1,
      "journey": "J-03",
      "action": {
        "type": "goto",
        "url": "/desk"
      },
      "expect": {
        "text": "Desk"
      }
    },
    {
      "n": 2,
      "journey": "J-03",
      "action": {
        "type": "expect",
        "target": {
          "css": "[data-testid=\"desk-screen-rows-table\"] td[data-testid=\"desk-row-band-class\"]"
        }
      },
      "expect": {
        "text": "Class A"
      },
      "timeout_ms": 15000
    },
    {
      "n": 3,
      "journey": "J-03",
      "action": {
        "type": "expect",
        "target": {
          "testid": "desk-history-calendar"
        }
      },
      "expect": {
        "text": "Jan"
      },
      "timeout_ms": 10000
    },
    {
      "n": 4,
      "journey": "J-03",
      "action": {
        "type": "click",
        "target": {
          "testid": "desk-section-expand-provenance"
        }
      },
      "timeout_ms": 10000
    },
    {
      "n": 5,
      "journey": "J-03",
      "action": {
        "type": "expect",
        "target": {
          "testid": "desk-provenance"
        }
      },
      "expect": {
        "text": "Config fingerprint"
      },
      "timeout_ms": 10000
    }
  ]
}
