{
  "schema_version": 1,
  "journey": "J-14",
  "name": "Every ranked briefing row states where the nearest wall on the OTHER side of price sits",
  "default_timeout_ms": 15000,
  "notes": [
    "Verified goal-desk-iter-18 by browser-qa-agent (2026-07-29) live against the running rig at backend :8301 / frontend :3301, serving the real ambient apps/backend/.data/screen store. The live latest snapshot (screen-2026-07-28-ac07c9581a4f, 63 ranked rows) entirely predates this iteration's code, so every one of its rows honestly shows the new opposite column's fallback text 'opposite wall not recorded in this snapshot' -- confirmed via DOM eval over all 63 ranked rows (0 populated) plus the 4 named rows (BRK-B, CRM, ISRG, CMCSA). The ranked table's header row was confirmed to carry exactly 11 cells ending in 'opposite' (symbol, side, class, distance, score, coverage, tick evidence, basis, history, band, opposite). The row's composite hover-tooltip title attribute was confirmed to append the 'bands by class not recorded in this snapshot' segment immediately after the band/close segment and before the coverage segments, with no separate title on the opposite <td> itself.",
    "The POPULATED case (a near opposite-wall row <=25 bps and a far opposite-wall row >1,000 bps, both legible in one screenshot, plus an unclassified-class opposite band and a populated bands_by_class tooltip line) was independently verified this same run on a fixture-scoped rig (backend :8395 / frontend :3395; TAPEOLOGY_JOURNAL_DB/TAPEOLOGY_DATASET_DIR/TAPEOLOGY_BAR_DIR/TAPEOLOGY_BAR_INDEX_DB/TAPEOLOGY_DESK_UNIVERSE_DIR/TAPEOLOGY_DESK_SCREEN_DIR all pointed at a fresh temp dir seeded with the committed fixture universe plus the AAPL/MSFT fixture bars and four REAL keyless-Yahoo-fetched symbols -- BRK-B, CRM, ISRG, CMCSA -- never apps/backend/.data) by computing a NEW screen for screen_date=2026-06-22 against that scoped store: CMCSA and CRM both came back with an opposite_band 0.00 bps away (class null, rendered 'unclassified'), AAPL came back 1208.73 bps away -- see reports/qa/goal-desk-iter-18-evidence/UT-03-result.png (near+far together), UT-05-result.png (populated bands_by_class tooltip line), UT-06-result.png (unclassified opposite band). That scoped rig's data is NOT reachable at future replay time (a one-off temp copy, torn down after this run), so this golden intentionally pins to the PERMANENT legacy state instead, which is guaranteed stable forever by the append-only rail -- matching the J-13 precedent exactly.",
    "This script clicks a Screen History row, which is SAFE to replay repeatedly: selecting a row only drives GET /research/desk/screen?id=<id>, a plain read that recomputes nothing and writes nothing. The pinned id (screen-2026-06-22-3ecd45c062c7) is the era's earliest recorded screen (iter-3) -- permanently a legacy row under the append-only rail, so its opposite cells will read the exact fallback text forever, making this the most future-proof anchor for a regression replay of this journey.",
    "Step 4 is a post-match liveness check (this era's iter-4/iter-5 lesson): a further /desk navigation AFTER the key assertion matches, proving the page is still alive and rendering correctly."
  ],
  "steps": [
    {"n": 1, "journey": "J-14", "action": {"type": "goto", "url": "/desk"}, "expect": {"text": "Desk"}},
    {"n": 2, "journey": "J-14", "action": {"type": "click", "target": {"css": "tr[data-screen-id=\"screen-2026-06-22-3ecd45c062c7\"]"}}, "expect": {"text": "screen-2026-06-22-3ecd45c062c7"}, "timeout_ms": 10000},
    {"n": 3, "journey": "J-14", "action": {"type": "expect", "text": "opposite wall not recorded in this snapshot"}, "timeout_ms": 8000},
    {"n": 4, "journey": "J-14", "action": {"type": "goto", "url": "/desk"}, "expect": {"text": "Desk"}}
  ]
}
