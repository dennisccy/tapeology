{
  "phase": "goal-referee-iter-3",
  "status": "in_progress",
  "current_step": "dev_complete",
  "updated_at": "2026-08-14T19:45:00.000000Z",
  "started_at": "2026-08-14T18:31:17.000000Z",
  "cli": "claude",
  "blockers": [],
  "note": "J-03 implemented: new app/research/referee_stats.py (the calibrated statistics core) implementing docs/referee-statistical-spec.md verbatim -- referee_stream() (the pinned seeded-stream recipe), _draw_indices_without_replacement/_draw_indices_with_replacement (the hand-coded Fisher-Yates/randrange discipline, matched from desk_forward._draw_anchor_indices rather than imported -- import-ban guard proves it), bootstrap_ci_occurrence/bootstrap_ci_cluster (percentile bootstrap CIs at both clustering levels; the clustered CI returns the literal insufficient_sample below REFEREE_MIN_CLUSTERS_FOR_CI=8, else a real interval + MDE), permutation_test (the primary test, referee-test-perm-v1's algorithm: combined statistic T via the single _t_statistic helper both weight forms A/C-harmonic and B reduce to; full enumeration <=8192 else REFEREE_B seeded per-session-substream draws; p=(1+extreme)/(draws+1)), sign_flip_result/equal_weight_t (the two named robustness disclosures, served alongside but never substituting for the primary), benjamini_hochberg (+BY disclosure; caller folds unevaluated candidates in as p=1, m never drops), run_oracle_attestation/verify_oracle_attestation (fail-closed: verify re-derives live expected/tolerance from the current build, never trusts a stored passed flag). New tests/test_referee_oracles.py: all 6 spec Sec6 oracle cases (size iid-skewed lognormal, size heavy-tailed Student-t3, the unclustered-pooled-permutation-foil-over-rejects demonstrated failure, the sign-flip-mis-sizes demonstrated failure, power at S=40 matching a pinned golden 0.8950, the 20-null+1-positive BH sweep matching pinned goldens 0.0114/0.9375, CI coverage at S=40 + the S=6 insufficient_sample case) PLUS a deliberately mis-implemented mutation fixture (rejection rate exactly 0.0, proving the suite catches a wrong implementation) -- every foil/mutant independently implemented from scratch in the test file, never by flag-flipping the primary code path. A module-scoped autouse budget-guard fixture self-times the file against REFEREE_ORACLE_BUDGET_SECONDS=120s; measured twice in isolation at 74.7s and 76.7s. One real bug was found and fixed during this session's own verification (not left for review): the budget guard originally captured its start time at MODULE IMPORT (pytest collection), which wrongly charged every OTHER test file's runtime against the 120s budget when running as part of the full suite -- fixed by moving the time.perf_counter() capture into the fixture's own lazy setup (before yield). New tests/test_referee_stats.py: 32 fast mechanics tests (TC-1..TC-7, TC-16, TC-17, TC-19) including a degenerate-fixture CI test hand-derivable to an exact point, an independently-reimplemented-from-scratch reference bootstrap matching the module's own CI bounds exactly, a hand-enumerated 3-permutation fixture with a hand-derived p=0.5, a from-scratch reference permutation loop cross-checking the seeded B-draw branch's exact extreme count, and BH's k* on a hand-computed 5-candidate family plus the unevaluated-folds-as-p=1 case (m grows 5->6, never drops). Carried rider 1: test_referee_evidence.py gains real assertions for _signal_reaches_session_complete (a fixture signal engineered to land exactly at/-1s/+1s of the REFEREE_SESSION_COMPLETE_ET=15:55 boundary -- True at/after, False strictly before -- plus the no-forward-block case and a named test for the disclosed bar-gap-blind limitation). Carried rider 2: same file gains real assertions for resolve_referee_obs_cache_db_path (env-var override returns verbatim; unset defaults to a sibling of resolve_desk_playbook_dir's own resolved directory). Carried rider 3: one clarifying sentence added to docs/referee-statistical-spec.md Sec2 (provenance.detector_basis is None for strategy-family observations by design) -- documentation-only, zero .py diff, ratifying the already-recorded iter-3 assumptions-ledger entry. tests/test_referee_guards.py extended with a referee_stats.py-scoped AST import-ban guard (zero imports of desk_playbook_detect/desk_playbook_context/desk_forward/levels/tradability) + its can-fail counter-test. Full backend suite: 2503 collected, 2495 passed, 0 failed, 0 errors, 8 skipped (via --junit-xml; runtime 240.5s) -- exceeds the >=2446 pass/8 skip floor exactly (2446 + 49 new tests = 2495). Config().config_fingerprint() unchanged at 08e471b10130e1e2; test_mcp_server.EXPECTED_TOOLS unchanged at 20 tools; git diff --stat scoped to exactly test_referee_evidence.py/test_referee_guards.py/referee-statistical-spec.md -- zero diff to referee_evidence.py's own source, desk_playbook*.py, desk_forward.py, levels.py, tradability.py, setups.py, pnl_scan.py, app/config.py, app/main.py, or any route file; zero new Config field; zero new runtime dependency (referee_stats.py imports only itertools/math/random/statistics, verified by an AST source-scan test -- never scipy, never numpy). No live-server or browser verification performed: J-03 is backend-only and unconsumed by any route/page/MCP tool this iteration (goal.md's own '(Keyless; automated.)' marker) -- the entire acceptance is the seeded oracle suite; the already-running pinned backend/frontend were left untouched, no process started or stopped. J-10's browser regression sentinel is this iteration's Required-still-passing item but is a QA-stage responsibility, not covered in this dev pass.",
  "changed_files": [
    "apps/backend/app/research/referee_stats.py",
    "apps/backend/tests/test_referee_oracles.py",
    "apps/backend/tests/test_referee_stats.py",
    "apps/backend/tests/test_referee_evidence.py",
    "apps/backend/tests/test_referee_guards.py",
    "docs/referee-statistical-spec.md",
    "docs/handoffs/goal-referee-iter-3-dev.md"
  ],
  "tests_run": true,
  "browser_checks_run": false,
  "next_action": "review"
}
