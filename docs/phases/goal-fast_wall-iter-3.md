# Goal Iteration 3 — J-03 The arm memo (per-tick levels recompute becomes ~100 memo hits per session)

<!-- machine-readable goal-mode metadata -->
## Goal Mode Metadata

- **Session ID:** fast_wall
- **Iteration:** 3
- **Mode:** next
- **Depth:** full
- **Frontend Present:** no
- **Target journeys:** J-03
- **Required-still-passing journeys:** J-01, J-02, J-07 (these are the ONLY three `passing` journeys in this session's `journey-history.json`, so this list is already this iteration's full regression set — mirroring iter-2's identical reasoning. J-01 and J-07 are the two journeys the applied lesson below names explicitly: this iteration touches `levels.py`/`tradability.py`, the canonical owners `/structure`'s Tradable Map and Case Studies sections read directly, and `Frontend Present: no` means browser-qa/golden-replay is SKIPPED again this iteration, so a mechanically-proven byte-identity assertion — TC-15 — is the required stand-in gate for their non-regression, not a nicety. J-02 is included as the smoke-set/canonical-home neighbor: `bars.py`/`datasets.py`/`dataset_index.py` are untouched by this diff (zero git diff), so its carry-forward is trivially mechanical, but it is listed for completeness since it shares this iteration's cross-cutting Structure/Studies home in `blueprint.md`.)
- **Anti-goal reminders (verbatim from `docs/goal.md`):**

  *Immutable rails — the identity of the project:*
  1. **No execution path, ever** — no brokerage/trading API, no order tickets, no live OR paper trading, no "just to test" exceptions. (`apps/backend/tests/test_no_execution_path.py` is the tier-1 guard; new research code adds matching guard tests, never weakens them.) *(critical)*
  2. **No profit claims and no advice** — every $ figure is a simulated measurement carrying R, n, fee/slippage assumptions, and its train/hold-out/forward basis. No prediction language, no imperative trading cues. *(critical)*
  3. **Frozen foundations** — the `v1` strategy, the `default` profile, the tape engine's five states and thresholds, the frozen structure computations, the JSON `BarStore`, and archived-era behaviour stay byte-identical. New work is additive and versioned beside them, never a mutation of them. *(critical)*
  4. **Hold-out-only promotion** — the champion pointer moves only on a genuine hold-out survival through the sweep gate (plus the era-6 statistical gates once they exist). Train-only wins are labeled overfit. Never lower a minimum sample size, widen a gate, or pool across feeds/fingerprints to manufacture a survivor. *(critical)*
  5. **No lookahead** — every value computed as-of T uses only events/bars fully completed at T. *(critical)*
  6. **Single source of truth** — each shared value is computed once, owned by one canonical endpoint, and read verbatim by REST/WS/UI/MCP/reports. The coherence-auditor hard-fails violations. *(critical)*
  7. **Deterministic and seeded** — every random draw uses a config-owned recorded seed; identical requests reproduce byte-identical results; no wall-clock, no unseeded randomness in any research artifact.
  8. **Read-only MCP** — MCP tools remain byte-identical proxies of GET endpoints; nothing on the MCP surface can change state. *(critical)*
  9. **Immutable data** — registered datasets and bar series are append-only, checksummed, never re-tagged, never deleted, never content-perturbed. Splits are frozen at registration. *(critical)*
  10. **Persistence stays scoped** — no ambient recording of live streams; recording/fetching is an explicit, logged act. *(critical)*

  *Interlude-specific anti-goals (added, not weakening any rail above):*
  - **Accelerators are never sources of truth.** Every cache/index/memo this interlude adds is a rebuildable derived value: deleting it loses nothing and fabricates nothing; a miss recomputes byte-identically through the one canonical owner; no research value is ever read FROM a cache that could not be re-derived identically without it. *(critical)*
  - **No compute on page load — operator-run only.** No GET (page, REST, or MCP proxy) ever starts, resumes, or extends the backtest sweep; the only compute entry points are the explicit POST trigger and the CLI warmer. No scheduled, ambient, or retry-driven compute either. *(critical)*
  - **The verification trust boundary never weakens.** Stat-keyed serving applies only to content already fully verified in this process's lifetime, keyed by `(path, size, mtime_ns)` with the racy-write guard; ANY stat change re-verifies fully; integrity failures are never cached; `DatasetStore.load_events()`/`replay()` — the paths that feed research values — verify fully on every load, forever. *(critical)*
  - **No divergent accelerator output.** An accelerated read (cached, memoized, resumed, or parallel) whose bytes differ from the fresh sequential compute of the same inputs is a veto-class defect, never a tolerable approximation; no accelerator ships without a passing determinism/equivalence test proving that byte-identity. *(critical)*
  - **No gate, register, or vocabulary drift.** The PnL register, `insufficient_sample` labeling, train/hold-out separation, feed separation, and the "simulated — not indicative of live results" language are untouched; the not-computed state introduces no prediction/advice/imperative phrasing. *(critical)*
  - **No source-guard weakening.** The existing source-introspection tests (forbidden substrings, single rebind, pinned dependency wiring) are respected as written — never edited, renamed, or loosened to make a change fit. *(critical)*

## GOAL

`structure_tape` and `structure_tape_map` backtests stop re-running the full levels/tradability pipeline on every confirming tick: a small per-run `_StructureArmMemo` in `backtests.py`, keyed by new `levels.py`/`tradability.py` helpers, serves each arming check from the handful of real level/tradability states a session actually has — byte-identically to today's unmemoized output.

## BACKGROUND

Iter-2's `eval.md` explicitly recommends building **J-03 next** ("per goal.md's stated dependency order (J-01 → J-02 → J-03 → J-04 → J-05), now unblocked by J-02"), echoing iter-1's own recommendation of the same order; iter-2's `coherence.md` was `COHERENCE-PASS` (rule 2 N/A — no consolidation owed). No journey has regressed (rule 1 N/A). J-03 is the priority-rubric unblocker (rule 3): the Vision's own measured baseline shows `structure_tape` on a 14,241-event dataset could not finish in 9.3 minutes (≥400× v1's 1.37s) because the arming check re-runs the FULL levels pipeline per confirming tick, even though levels are a constant function of `as_of` between the ~100 real bar-close instants a session actually has — so shipping J-04's compute button/manager next, before this fix, would still leave every triggered sweep catastrophically slow, defeating the interlude's own purpose. J-06 (`setups_scan_cache.py`) is ALSO technically unblocked by J-02's new `BarStore.root`, but is deliberately NOT co-picked this iteration: it touches a second, different frozen-foundation file (`setups.py`) under the same critical anti-goals, and rule 5 (never bundle two risky journeys) forbids pairing it with J-03's three-file change in one diff — a joint failure would be undiagnosable. This is therefore the ONE risky journey this iteration; J-04, J-05, J-06 stay out of scope; rule 4 (smallest spec wins ties) does not apply since J-03 has no smaller-scoped sibling ready to build in its place per the dependency order.

**Depth = full**, citing the eval.md iter-2 recommendation directly plus the "Picking depth" rubric's own triggers (the prior verdict was `CONTINUE`, not `ESCALATE`, so full is not mandatory by that rule alone — it is independently justified here): J-03 touches the data model of THREE frozen-foundation research-computation files (`levels.py`, `tradability.py`, `backtests.py`) at once — "touches data model" / "risky, structural" — under the interlude's CRITICAL "Frozen foundations" and "No divergent accelerator output" anti-goals, where a memo silently serving a stale level/tradability state would corrupt every downstream backtest result, a veto-class defect. It also "requires new tests beyond browser smoke": byte-identity determinism tests for BOTH memo-bust legs goal.md itself enumerates (a daily period closing between bar epochs; a run spanning a UTC date boundary) plus counting-spy proof, guarded by the two existing source-introspection tests (`test_backtests.py:1500-1508`, `:932-943`) that this diff must leave byte-unmodified. The audit + coherence lanes are the backstop a lean reviewer-only cycle cannot provide. J-03 is keyless/automated per goal.md's own tag ("(Keyless; automated.)"); **Frontend Present: no** — no browser-qa dispatch this iteration.

**Lesson applied** (`lessons.md` iter-2, naming J-03 explicitly): "any future `Frontend Present: no` iter that touches an endpoint or shared value a `passing` browser journey depends on (**J-03**, J-05, J-06 all qualify — each accelerates a value the `/structure` surfaces read)... treat a mechanically-proven byte-identity assertion as a *required* gate, not a nicety — it is the only guardrail left for those UI journeys when replay does not run." This iteration directly touches `levels.py` and `tradability.py` — the canonical owners of `/structure`'s Tradable Map and Case Studies sections, which J-07 (the regression sentinel) guards. TC-15 below (every existing `test_levels.py`/`test_tradability.py` pinned-value test passes with byte-unmodified source) is that required gate: `compute_levels`'s and `compute_tradability`'s own function bodies are untouched by this diff (only new standalone helpers are added beside them), so their served bytes for J-01/J-02/J-07's dependents cannot have moved.

## IN SCOPE

### Backend

- [ ] `apps/backend/app/research/levels.py`: add `level_change_points(store: BarStore, symbol: str) -> tuple[float, ...]` — mirror `compute_levels`'s own healthy-series enumeration (`store.list()`'s healthy `records` half, `_select_one_series_per_timeframe`, `PRIOR_PERIOD_TIMEFRAMES`/`_PERIOD_SECONDS`) to build the sorted, deduplicated union of every healthy series' own bar epochs for `symbol` plus, for each prior-period-timeframe series' bar, its `epoch + period_seconds` close instant. Document the contract directly on the function: between two consecutive returned change points, `compute_levels(store, symbol, as_of, config)` is a constant function of `as_of` for that `symbol` (a SUPERSET of true change points is always safe; a subset is never safe) — no change to `compute_levels`'s or `compute_confluence_zones`'s existing bodies.
- [ ] `apps/backend/app/research/tradability.py`: add `basis_day_key(as_of_epoch: float) -> str` — the UTC session-date key, reusing the existing `_session_date` date-resolution helper (never a second date derivation), documenting that `_resolve_basis`'s chosen prior session is constant for every `as_of_epoch` sharing the same UTC calendar date. No change to `compute_tradability`'s or `_resolve_basis`'s existing bodies.
- [ ] `apps/backend/app/research/backtests.py`: add a small per-run `_StructureArmMemo` class — `levels_at(as_of_epoch)` keyed by `bisect.bisect_right` position into a `level_change_points(...)` tuple resolved ONCE at memo construction (a miss/out-of-range falls back to calling `compute_levels(` directly, the existing owner call); `tradability_at(as_of_epoch)` keyed by `basis_day_key(as_of_epoch)` (a miss calls `compute_tradability(` directly). Build exactly ONE memo instance inside `_structure_tape_trades` (for `STRATEGY_TAPE_ID` runs) and exactly ONE inside `_structure_tape_map_trades` (for `STRATEGY_TAPE_MAP_ID` runs) — once per `BacktestRunner.run()` call, never shared across runs, never persisted to disk or any store. Thread it into `_structure_tape_arm` (currently calling `compute_levels(bar_store, symbol, as_of_epoch, config)` at line ~695) and `_structure_tape_map_arm` (currently calling `compute_tradability(bar_store, symbol, as_of_epoch, config)` at line ~807) as a new keyword-only parameter defaulting to `None`; `memo=None` (every EXISTING caller and test) preserves today's exact direct-call behavior byte-for-byte. `run()`'s own public signature is unchanged — the memo is built and consumed entirely inside the two structure branches of `_strategy_trades`; v1's branch (unchanged) never sees a memo. Both source-introspection guard tests (`test_backtests.py:1500-1508`, `:932-943`) must pass unmodified: the literal `compute_levels(`/`compute_tradability(` owner-call substrings stay present (in each function's fallback branch), and no forbidden level-internal substring (`_swing_pivots`, `_prior_period_extremes`, `_cluster_levels`, `_grade_zone`) is introduced anywhere in `backtests.py` or `_structure_tape_map_arm`'s source.
- [ ] `apps/backend/tests/test_levels.py`: add the `level_change_points` contract tests (TC-1, TC-2 below).
- [ ] `apps/backend/tests/test_tradability.py`: add the `basis_day_key` contract tests (TC-3, TC-4 below).
- [ ] `apps/backend/tests/test_backtests.py`: add the memo determinism/equivalence, both memo-bust legs, both counting-spy, and interactive-budget tests (TC-5 through TC-11 below), reusing the existing `confluence_bar_store` fixture and `_record_structure_tape_dataset` helper where the fixture shape already fits, adding new fixtures only where goal.md's acceptance requires a scenario the existing fixtures don't cover (the two memo-bust legs; the multi-interval budget fixture).

### Frontend

None — J-03 is a backend-only accelerator (`blueprint.md`'s Information Architecture table: "no dedicated UI panel — accelerates the backtest sweep the J-04 button triggers"). No frontend file is touched this iteration.

### New user-facing capability

None directly this iteration. The memo is inert to every CURRENT UI surface — J-04's compute trigger, the only thing that will actually exercise `structure_tape`/`structure_tape_map` at scale, does not exist yet. Once J-04 ships, a real edge-report sweep's structure-strategy backtests will draw levels/tradability from the ~100 real per-session states this memo now serves, instead of recomputing per tick — the latent throughput fix this iteration lays down for J-04/J-05 to expose.

### New information displayed

None — no response shape changes. `compute_levels`'s and `compute_tradability`'s served JSON is byte-identical before and after this diff (TC-5, TC-6, TC-15).

### New user actions

None.

### UI surface changes

None — no page, panel, or component is added or modified.

### Product surface delta

No visible product surface changes this iteration. The only observable delta is INTERNAL to backtest execution speed for the two structure strategies (proven by the counting-spy TCs, TC-9/TC-10) — not yet operator-visible on `/structure` since no trigger to run a real sweep exists until J-04.

### Blueprint conformance

No new surfaces — J-03 is a cross-cutting backend accelerator with no dedicated UI panel, exactly as `blueprint.md`'s Information Architecture table already registers it: "J-03 Per-run structure arm memo | no dedicated UI panel — accelerates the backtest sweep the J-04 button triggers | Structure (cross-cutting)." No nav change; no `blueprint.reapproval-requested` needed.

### Data-contract additions

None. The per-run `_StructureArmMemo` this iteration ships is already pre-registered VERBATIM in `runs/goal-session-fast_wall/state/blueprint.md`'s "Rebuildable accelerators" list ("the per-run `_StructureArmMemo` (in-memory, one instance per backtest run — never persisted)", drafted at baseline from the interlude's own Product Shape section). `level_change_points` and `basis_day_key` are internal helper functions that feed the memo — they are not separately displayed values, are served by no endpoint, and are not new Data-Contract entries. This iteration introduces no NEW displayed value, no second computation path, and no second serving endpoint for any existing Data Contract row: `GET /research/levels` and `GET /research/tradability` stay the ONE endpoints, `levels.py`/`tradability.py` stay the ONE owners. Verified against the current `blueprint.md` field-for-field; no edit made this iteration (nothing to add).

## OUT OF SCOPE

- J-04 (`edge_report_compute.py`, the compute routes, the CLI warmer, the "Compute edge report" button) — next per the dependency order, not started here.
- J-05 (`EdgeReportBacktestCache`, the `run_pair` provider seam, the process pool) — depends on J-04's manager plumbing.
- J-06 (`setups_scan_cache.py`, `compute_setups`'s cache-key change from `id(config)` to a content hash) — technically unblocked by J-02's `BarStore.root`, but deliberately not bundled with J-03 (rule 5 — never bundle two risky frozen-foundation-file changes in one diff); the next candidate after J-03.
- Any change to `edge_report.py`, `edge_report_cache.py`, or `routes.py` (J-01's owned files, and the route layer generally) — unaffected, zero diff expected.
- Any change to `bars.py`, `datasets.py`, or `dataset_index.py` (J-02's owned files) — unaffected, zero diff expected.
- Any change to `compute_levels`'s, `compute_confluence_zones`'s, or `compute_tradability`'s EXISTING function bodies, or to `_resolve_basis` — all stay byte-unchanged; only new standalone functions are added beside them.
- Any change to `v1`'s (or any non-structure strategy's) arming path — `_strategy_trades`'s v1 branch is untouched; the memo is read ONLY by the two structure branches.
- Any new `Config` field or new runtime dependency — the memo, `level_change_points`, and `basis_day_key` are pure functions/objects over EXISTING inputs; `bisect` is stdlib (already the interlude-wide constraint: stdlib only, fingerprint frozen).
- Persisting the memo anywhere — it stays in-memory, one instance per run, discarded when `run()` returns; never a durable accelerator, never listed as one.
- Any UI change to `/structure` — no dedicated panel this iteration.
- Any real-corpus recording, credential work, or new dataset/bar-series registration.
- Deleting or weakening any existing test in `test_levels.py`, `test_tradability.py`, or `test_backtests.py`.

## DEFINITION OF DONE

- [ ] J-03 passes via automated backend verification (no browser-qa dispatch — `Frontend Present: no`): TC-1 through TC-13 all hold.
- [ ] TC-1 and TC-2 hold: `level_change_points`'s union/superset contract and the resulting "`compute_levels` is constant between two consecutive change points" property are both mechanically proven.
- [ ] TC-3 and TC-4 hold: `basis_day_key`'s same-UTC-date stability and cross-date-boundary distinctness are both mechanically proven.
- [ ] TC-5 and TC-6 hold: a memoized `structure_tape` run and a memoized `structure_tape_map` run are each byte-identical (`json.dumps(result, sort_keys=True)` equality) to the SAME run with `memo=None` (today's direct-call path).
- [ ] TC-7 and TC-8 hold: BOTH goal.md-named memo-bust legs are proven — a daily period closing between bar epochs, and a run spanning a UTC date boundary — each still byte-identical memoized vs. unmemoized, and each fixture proven non-vacuous (the arming decision or the resolved basis genuinely differs between the two sides of the boundary).
- [ ] TC-9 and TC-10 hold: counting spies prove `compute_levels` is called once per distinct `level_change_points` interval actually visited (never once per confirming tick), and `compute_tradability` is called once per distinct `basis_day_key` actually visited (never once per confirming tick).
- [ ] TC-11 holds: the committed multi-interval structure-backtest fixture completes within the interactive test budget (see NOTES for the specific bound chosen and why).
- [ ] TC-12 holds: every EXISTING `structure_tape`/`structure_tape_map` pinned-value and arming test in `test_backtests.py` (roughly lines 349–970) passes with byte-unmodified source (git-confirmed) — proving `memo=None` preserves today's exact behavior for every caller that doesn't opt in.
- [ ] TC-13 holds: both source-introspection guard tests (`test_backtests.py:1500-1508`, `:932-943`) pass with byte-unmodified source and their assertions hold against the new code.
- [ ] TC-14 holds: the full backend unit/integration suite passes with zero failures and zero newly-skipped/deleted tests, and `config.config_fingerprint()` still equals `4d665603569b9dbf`.
- [ ] TC-15 holds (Required-still-passing J-01/J-02/J-07 mechanical gate): every EXISTING pinned-value test in `test_levels.py` and `test_tradability.py` passes with byte-unmodified source — proving `compute_levels`'s and `compute_tradability`'s served bytes are unchanged for every existing reader, including `/structure`'s Tradable Map and Case Studies sections.
- [ ] Required-still-passing journeys J-01, J-02, J-07 remain green (mechanical non-regression: TC-14/TC-15 plus zero git diff to `edge_report.py`, `edge_report_cache.py`, `bars.py`, `datasets.py`, `dataset_index.py`, `routes.py`, `config.py` — no fresh browser pass exists this `Frontend Present: no` iteration; logged as the applied lesson above, not a fresh assumption).
- [ ] No anti-goal violation introduced — specifically the critical "Frozen foundations" (rail 3) and "No divergent accelerator output" (interlude-specific) anti-goals, mechanically proven by TC-5 through TC-8 and TC-15; the coherence-auditor confirms `_StructureArmMemo` stays a rebuildable, single-owner, never-persisted accelerator matching `blueprint.md`'s pre-registered row exactly, and that `levels.py`/`tradability.py` gain no second computation path.
- [ ] Dev handoff written at `docs/handoffs/goal-fast_wall-iter-3-dev.md`.

## TESTING REQUIREMENTS

- Browser: none. J-03 ships no UI surface (goal.md tags it "(Keyless; automated.)"; `blueprint.md`'s IA table registers no dedicated panel for it). Required-still-passing J-01 (`/structure` not-computed panel), J-02 (no UI surface), and J-07 (regression sentinel screenshots, including `/structure`'s Tradable Map/Case Studies sections) are covered this iteration by the mechanical byte-identity gate (TC-14/TC-15) rather than a fresh browser-qa dispatch, since neither journey's UI surface or served bytes change.
- Unit/integration: `levels.py`'s new `level_change_points` (union/superset construction, the constant-between-change-points property); `tradability.py`'s new `basis_day_key` (same-date stability, cross-boundary distinctness); `backtests.py`'s new `_StructureArmMemo` (`levels_at`/`tradability_at` hit and miss paths, one-instance-per-run scope) threaded into `_structure_tape_arm` and `_structure_tape_map_arm`; both memo-bust legs; both counting spies; the interactive-budget fixture; every existing structure-strategy pinned-value/arming test in `test_backtests.py` re-run unmodified; both source-introspection guard tests re-run unmodified; every existing pinned-value test in `test_levels.py`/`test_tradability.py` re-run unmodified.
- Error cases: a symbol with no recorded (or only corrupted) bar series must still honestly alias to the SAME `no_bar_series_for_symbol: true` / zero-arm outcome through the memo as it does unmemoized (covered by TC-12 re-running the existing `test_structure_tape_no_arm_when_symbol_has_no_classified_levels` / `test_structure_tape_map_no_arm_when_symbol_has_no_recorded_bands` tests unmodified — the memo introduces no new fabricated or silently-different empty state); a `memo=None` call must never raise or change behavior versus today (TC-12's full unmodified-test-pass is the mechanical proof).

Test-first contract:

- TC-1: given a `BarStore` with multiple healthy bar series for a symbol across timeframes, including at least one `PRIOR_PERIOD_TIMEFRAMES` (`1d`/`1w`/`1mo`) series, when `level_change_points(store, symbol)` is called, then the returned tuple is sorted, contains no duplicate values, includes every one of those healthy series' own bar epochs, and includes, for each prior-period-timeframe bar, its `epoch + period_seconds` close instant.
- TC-2: given two `as_of` instants that both fall strictly between the same two consecutive entries of `level_change_points(store, symbol)`'s returned tuple, when `compute_levels(store, symbol, as_of, config)` is called at each of the two instants, then the two results are byte-identical (`json.dumps(..., sort_keys=True)` equality) — the "constant function of `as_of` between two consecutive change points" contract, mechanically proven.
- TC-3: given two `as_of_epoch` values that fall on the SAME UTC calendar date, when `basis_day_key(as_of_epoch)` is called for each, then both calls return the identical string key.
- TC-4: given two `as_of_epoch` values on DIFFERENT UTC calendar dates (one strictly before, one strictly after a UTC midnight boundary), when `basis_day_key(as_of_epoch)` is called for each, then the two returned keys are different strings.
- TC-5: given the `confluence_bar_store` fixture and a recorded `structure_tape` (`STRATEGY_TAPE_ID`) dataset, when the backtest runs once with a per-run `_StructureArmMemo` built and threaded through `_structure_tape_arm`, and once more with `memo=None` (today's direct-call path), then `json.dumps(result, sort_keys=True)` is identical between the two runs.
- TC-6: given the same setup with a `structure_tape_map` (`STRATEGY_TAPE_MAP_ID`) dataset, when the backtest runs once memoized through `_structure_tape_map_arm` and once with `memo=None`, then `json.dumps(result, sort_keys=True)` is identical between the two runs.
- TC-7 (memo-bust leg 1): given a bar-store fixture where a `1d` period's close instant (`epoch + period_seconds`) falls strictly between two intraday bar epochs — so that close instant is itself a `level_change_points` entry with no bar recorded exactly at it — when a `structure_tape` backtest runs memoized across that boundary, then the memoized `result` is byte-identical to the `memo=None` result, AND at least one arming decision (armed vs. not-armed, or which classified level qualifies) differs between an event strictly before the change point and an event strictly after it — proving the fixture exercises a genuine regime change, not a vacuous boundary.
- TC-8 (memo-bust leg 2): given a bar-store/dataset fixture whose recorded tick stream spans a UTC calendar-date boundary (so `basis_day_key` returns two distinct keys across the run), when a `structure_tape_map` backtest runs memoized across that boundary, then the memoized `result` is byte-identical to the `memo=None` result, AND the tradability basis (`basis_as_of`) used before the boundary differs from the one used after it, matching what an unmemoized `compute_tradability` call would resolve at each side.
- TC-9: given a `structure_tape` backtest whose recorded tick stream crosses at least 3 distinct `level_change_points` intervals with more than one flat-arming-eligible tick inside each interval, when the backtest runs with the memo, then a call-counting spy wrapping `levels.compute_levels` records exactly one call per distinct change-point interval actually visited by the run — never one call per confirming tick.
- TC-10: given a `structure_tape_map` backtest whose recorded tick stream crosses at least 2 distinct `basis_day_key` values with more than one flat-arming-eligible tick inside each, when the backtest runs with the memo, then a call-counting spy wrapping `tradability.compute_tradability` records exactly one call per distinct day key actually visited by the run — never one call per confirming tick.
- TC-11: given a newly-added committed fixture whose recorded tick stream crosses at least 5 distinct `level_change_points` intervals, when the memoized `structure_tape` backtest runs to completion inside the test suite, then the test completes in under 10 seconds of wall-clock time (pytest's own reported per-test duration) and its `trades` list is non-empty.
- TC-12: given the full `test_backtests.py` module, when the suite runs after this iteration's diff, then every EXISTING `structure_tape`/`structure_tape_map` pinned-value and arming test (the tests currently spanning roughly lines 349–970, e.g. `test_structure_tape_breakthrough_long_arms_at_the_class_a_resistance_level`, `test_structure_tape_map_rejection_long_arms_at_the_class_b_support_band`, `test_v1_and_structure_tape_byte_identical_after_structure_tape_map_added`) passes, and a `git diff` of each pre-existing test function's own body shows zero edits (additions-only diff).
- TC-13: given `tests/test_backtests.py:1500-1508` (`test_structure_tape_reads_levels_from_the_one_canonical_compute_levels_owner`) and `:932-943` (`test_structure_tape_map_reads_tradability_never_recomputes_levels_or_zones`), when the suite runs after this iteration's diff, then both tests pass, `git diff` shows zero edits to either test's source, and their assertions hold against the new `backtests.py` source (no forbidden substring introduced).
- TC-14: given the full backend unit/integration suite runs after this iteration's changes, when it completes, then 0 tests fail, 0 pre-existing tests are newly skipped or deleted, and `config.config_fingerprint()` still equals `4d665603569b9dbf`.
- TC-15: given `tests/test_levels.py`'s and `tests/test_tradability.py`'s existing pinned-value tests (e.g. `test_byte_identical_determinism_across_independent_runs`, `test_committed_fixture_confluence_zones_exact_values_keyless`, `test_aapl_frozen_levels_output_is_byte_identical_to_before`, `test_aapl_repeat_call_determinism`), when the suite runs after this iteration's diff, then every one of those pre-existing tests passes, and `git diff` shows zero edits to any of their bodies (additions-only diff) — proving `compute_levels`'s and `compute_tradability`'s served values are byte-unchanged.

## NOTES

- **Codebase probe (this iteration, decompose time):** confirmed `level_change_points`, `basis_day_key`, and `_StructureArmMemo`/`StructureArmMemo` do not exist anywhere in `apps/backend/app` or `apps/backend/tests` yet (fresh build; matches `journey-history.json`'s `J-03 | failing`). Read the exact current call sites this iteration must thread through: `backtests.py:670-709` (`_structure_tape_arm`, calling `compute_levels(bar_store, symbol, as_of_epoch, config)` at line 695) and `backtests.py:763-823` (`_structure_tape_map_arm`, calling `compute_tradability(bar_store, symbol, as_of_epoch, config)` at line 807), both invoked from their respective `_structure_tape_trades` (line 607) / `_structure_tape_map_trades` (line 714) loops, themselves dispatched once per run from `_strategy_trades` (line 537, called once per `BacktestRunner.run()` at line 453) — confirming "build one memo per run" naturally scopes to inside the two `_..._trades` methods, with `run()`'s own public signature untouched. `levels.py:279-324` (`compute_levels`) and its `_select_one_series_per_timeframe`/`PRIOR_PERIOD_TIMEFRAMES`(`levels.py:81`)/`_PERIOD_SECONDS`(`levels.py:89`) are the exact healthy-series enumeration `level_change_points` must mirror. `tradability.py:126` (`_session_date`) is the existing date-resolution helper `basis_day_key` should reuse rather than re-deriving.
- **Lessons applied:** see BACKGROUND (`lessons.md` iter-2's `Frontend Present: no` byte-identity-required-gate entry, which names J-03 by ID).
- **Assumption logged this iteration:** goal.md's J-03 acceptance says "the committed tick-fixture structure backtests complete within an interactive test budget" but names no concrete number — see the newly-appended entry in `runs/goal-session-fast_wall/state/assumptions.md` (`## iter-3 — goal-decomposer`) recording the reading TC-11 above pins (a 10-second wall-clock ceiling on a newly-added multi-interval fixture), chosen to be generous/non-flaky yet clearly diagnostic of a regression to per-tick recomputation.
- **No blueprint edit:** `blueprint.md`'s existing "Rebuildable accelerators" list and its Feature/journey homes table already register the per-run `_StructureArmMemo` and J-03's "no dedicated UI panel — cross-cutting" home verbatim (drafted at baseline from the interlude's own Product Shape section); verified field-for-field against this iteration's plan — unchanged, matching iter-2's identical precedent.
- **Scope discipline:** this diff should touch exactly three product files (`levels.py`, `tradability.py`, `backtests.py`) plus their three test files — no route, no store, no config, no frontend file. Any wider diff is a signal the memo has leaked outside its intended per-run, in-memory scope.
