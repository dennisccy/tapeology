# Goal Iteration 4 — The fingerprint epoch bump, §0.4 Path B (J-04)

<!-- machine-readable goal-mode metadata -->
## Goal Mode Metadata

- **Session ID:** clean_slate
- **Iteration:** 4
- **Mode:** next
- **Depth:** full
- **Frontend Present:** no
- **Target journeys:** J-04
- **Required-still-passing journeys:** J-01, J-02, J-03, J-05 (all scoped to backend/keyless checks this iteration — the I-9 kept-route re-capture + full suite + guard tests; no browser walk this iteration, matching iter-1's precedent for full-depth backend-only work and goal.md's own `(Keyless; automated.)` tag on J-04 itself. Widened to all four passing/tracked journeys — not just one or two — because this iteration's blast radius is unique among the era: it moves the ONE Data-Contract value every kept route embeds (`config_fingerprint`), so every kept surface's evidence needs a fresh look, not just the journeys nearest the diff. J-05's browser-walk closure stays reserved for its own iteration.)
- **Anti-goal reminders:**
  - **No execution path, ever** — no brokerage/trading API, no order tickets, no live OR paper trading, no "just to test" exceptions. (`apps/backend/tests/test_no_execution_path.py` is the tier-1 guard; new research code adds matching guard tests, never weakens them.) *(critical)*
  - **No profit claims and no advice** — every $ figure is a simulated measurement carrying R, n, fee/slippage assumptions, and its train/hold-out/forward basis. No prediction language, no imperative trading cues. *(critical)*
  - **Frozen foundations** — the `v1` strategy, the `default` profile, the tape engine's five states and thresholds, the frozen structure computations, the JSON `BarStore`, and every KEPT surface's behaviour stay byte-identical. New work is additive and versioned beside them, never a mutation of them. *(This era's one sanctioned exception, operator-approved 2026-07-23: the journal/studies/performance product surfaces are REMOVED outright — never mutated-in-place — and their historical records stay readable; nothing else moves.)* *(critical)*
  - **Hold-out-only promotion** — the champion pointer moves only on a genuine hold-out survival through the sweep gate (plus the era-6 statistical gates once they exist). Train-only wins are labeled overfit. Never lower a minimum sample size, widen a gate, or pool across feeds/fingerprints to manufacture a survivor. *(critical)*
  - **No lookahead** — every value computed as-of T uses only events/bars fully completed at T. *(critical)*
  - **Single source of truth** — each shared value is computed once, owned by one canonical endpoint, and read verbatim by REST/WS/UI/MCP/reports. The coherence-auditor hard-fails violations. *(critical)*
  - **Deterministic and seeded** — every random draw uses a config-owned recorded seed; identical requests reproduce byte-identical results; no wall-clock, no unseeded randomness in any research artifact.
  - **Read-only MCP** — MCP tools remain byte-identical proxies of GET endpoints; nothing on the MCP surface can change state. *(critical)*
  - **Immutable data** — registered datasets and bar series are append-only, checksummed, never re-tagged, never deleted, never content-perturbed. Splits are frozen at registration. *(critical)*
  - **Persistence stays scoped** — no ambient recording of live streams; recording/fetching is an explicit, logged act. *(critical)*
  - **No research-value change beyond the documented epoch bump.** Every number a KEPT surface serves (levels, bands, touch events, edge cells, pnl rows) stays byte-identical on identical inputs; the ONLY sanctioned change is the `config_fingerprint` value itself, moved once via the J-04 Path B journey; cross-epoch pooling is forbidden forever. *(critical)*
  - **Deletion is complete, never cosmetic.** No orphaned imports, dead components, unreachable routes, dangling MCP tools, or skipped tests survive; a deleted surface is gone from code, routes, nav, MCP, types, and tests alike — grep-provably. *(critical)*
  - **No new features.** This era ships zero new product capabilities, pages, endpoints, strategies, or Config fields; anything new belongs to the next eras. *(critical)*
  - **Relocations are moves, not rewrites.** `r_basis` and the dataset-source constants keep byte-identical behaviour at their new homes; every kept caller's output is proven unchanged. *(critical)*
  - **Never modify the charts beyond the one named edit.** No commit in this era may edit `StructureChart.tsx` at all, or edit `PriceChart.tsx` beyond removing its thesis-geometry overlay build (I-7 chart clause); the three chart guard suites must pass byte-unmodified; any other chart diff — visual or behavioral — is a veto-class defect. *(critical)*
  - **Never touch a historical record.** No commit in this era may delete, rewrite, truncate, or re-stamp journal.db's existing rows or tables, any PnL-ledger row, anything under `docs/goal-archive/` or `runs/goal-session-*`, or any `reports/goal-session-*-delivered.md` — a diff touching any of these is a veto-class defect (deleting CODE is the mandate; deleting RECORDS is forbidden). *(critical)*
  - **No guard weakening.** `test_no_execution_path.py`, the source-introspection guards, and every kept test stay as written; the fingerprint pins change ONLY inside J-04 per Path B, never to make a red test green. *(critical)*
  - **The enhancement loop stays inside its box.** The goal-proposer may append journeys ONLY inside the `AUTO:journeys` marker block above — it MUST NOT edit human-authored journeys, this Anti-goals section, or any other part of this file; proposed journeys MUST carry a single-source-of-truth (or PnL-ledger) acceptance criterion, keep the `default` profile and `v1` byte-identical, and include a `[NEW]`-flagged walkthrough. Manufacturing a low-value journey just to keep the loop alive is a failure. *(critical)*

## GOAL

Execute the §0.4 Path B fingerprint epoch bump exactly as goal.md's I-4/I-9 direct — delete every orphaned journal-era Config field (a corrected, grep-verified list; see NOTES), update `config_fingerprint`'s literal at all 13 verified pin sites, and append a new-epoch PnL founding row beside the untouched old one — so every kept research value keeps serving byte-identical VALUES under a new, honest fingerprint stamp, closing the era's last non-sentinel journey before J-05.

## BACKGROUND

Iteration 3 (lean) landed J-03, independently re-verified by the evaluator (MCP contract = exactly 15 tools, fingerprint unmoved, coherence COHERENCE-PASS). Its Next-Step Recommendation explicitly targets **J-04 at full depth** — the last journey in goal.md's own J-01→J-02→J-03→J-04→J-05 order before the sentinel, and the era's single most delicate operation. **Depth full is justified** per the Picking-depth rubric: this crosses the "touches data model" trigger (23 `Config` field deletions, grep-corrected this planning pass from goal.md's 18 — see NOTES), the "structural/large blast radius" trigger (13 test-file pin sites + `config.py` + `pnl_baseline.py` + `pnl_history.py` + `reports/pnl/pnl-history.md`, independently confirmed this planning pass), and carries the densest stack of critical-anti-goal adjacency in the era (T-3 pin discipline, "never touch a historical record," "no guard weakening," "no research-value change beyond the documented epoch bump," "no new Config fields") — exactly what the audit/coherence/closure lanes exist to trace. Coherence was `COHERENCE-PASS` (not `COHERENCE-FAIL`), so this is normal next-scope work, not a consolidation pass.

This planning pass independently re-verified goal.md's I-4 "Confirmed DELETE list" against the live codebase (grep every named field's readers, not just trust the list) and found **two load-bearing corrections that change what must be deleted**, plus a **third, separate discovery about the PnL re-seed mechanism itself** — all documented in full in NOTES and logged to `assumptions.md` per T-14 ("inventory contradictions stop the line... the fix is a documented inventory correction, never a silent improvisation"). Two prior lessons generalize directly and were both re-applied this pass: iter-1's lesson ("a demolition's 'grep the sole consumer' step must move EVERY symbol that consumer imports, not just the named one") generalizes from relocated *symbols* to deleted *Config fields* — I grepped every field's live readers, not just the goal doc's named list, and found 4 fields wrongly listed for deletion plus 9 more that qualify but weren't listed. Iter-2's lesson ("grep `apps/backend/tests` for `read_text()`/`open(` referencing the deletion target BEFORE deleting, so uncatalogued source-introspection guards are found up front") was re-run against `config.py` this pass (`grep -rn "read_text\|\.open(" tests/*.py | grep -i config` → zero hits) — no hidden source-introspection guard exists for this deletion.

## IN SCOPE

### Backend

- [ ] `apps/backend/app/config.py` — delete these **14** confirmed-list fields (each independently re-verified this planning pass to have zero readers outside `config.py`): `verdict_dwell_seconds`, `invalidation_epsilon_spread_multiple`, `verdict_timeline_cap`, `management_stance_dwell_seconds`, `checklist_stance_dwell_seconds`, `delivery_lag_ok_bound_seconds`, `excursion_horizons_seconds`, `excursion_target_r`, `study_null_arm_count`, `study_null_baseline_seed`, `study_list_max`, `hint_sustain_dwell_seconds`, `hint_cooldown_seconds`, `hint_log_max`.
- [ ] `apps/backend/app/config.py` — ALSO delete these **9** additional fields (not on goal.md's confirmed list, but verified this planning pass to qualify under I-4's own closure rule — zero readers outside `config.py`, sole historical reader already deleted in J-01/J-02): `invalidation_k_consecutive`, `journal_list_default_limit`, `journal_list_max_limit`, `chase_return_threshold`, `invalidation_too_tight_spread_multiple`, `process_outcome_grade_map`, `process_violated_min_failed_checks`, `process_flagged_min_risk_flags`, `sound_cue_cooldown_seconds`. (23 fields deleted in total. Re-verify each via grep before deleting — T-12 — this list reflects the codebase as of this planning pass.)
- [ ] `apps/backend/app/config.py` — do **NOT** delete 4 of goal.md's confirmed-list names: `study_arm_sustain_seconds`, `study_arm_cooldown_seconds`, `study_occurrence_r_spread_multiple`, `study_occurrence_r_floor`. All four are read live by `Config.strategy_definition()` to build the KEPT `v1`/`structure_tape`/`structure_tape_map` strategy grammar (served via `GET /research/strategies`) and directly by `backtests.py:225`'s R-stop formula — deleting them crashes every backtest/edge-report compute. Also do NOT delete `analytics_min_sample_size` (not on the confirmed list but plausible by name) — still read by `pnl_ledger.py`. See NOTES for the full grep trail.
- [ ] `apps/backend/app/config.py`'s `config_fingerprint()` method — prune exactly these **8** now-orphaned entries (string literal + its explanatory comment block) from the `excluded = {...}` set: `checklist_stance_dwell_seconds`, `delivery_lag_ok_bound_seconds`, `hint_log_max`, `journal_list_default_limit`, `journal_list_max_limit`, `management_stance_dwell_seconds`, `sound_cue_cooldown_seconds`, `study_list_max`. Touch no other entry (the other 15 deleted fields were never exclusion-listed; the 40 remaining entries belong to unrelated, still-live era-3/4/5B/5C capabilities — confirmed this planning pass by diffing the full exclusion-set literal against the delete list).
- [ ] `apps/backend/app/config.py` — bump `pnl_founding_enhancement_id` and `pnl_founding_enhancement_title`'s literal DEFAULT VALUES to a new, distinct, self-documenting pair (suggested: id `"founding-baseline-strategy-v1-default-clean-slate"`, title `"founding baseline — strategy v1 on default (post-clean-slate epoch)"`; any clearly-distinct, honest alternative is acceptable — the exact bytes are not load-bearing). This is a VALUE edit to two EXISTING era-3 J-04 fields, not a new field (the "no new Config fields" non-goal holds). Required because `app/research/pnl_baseline.py:seed_founding_row` looks up the ledger by this exact id, and the `pnl_ledger` table's `enhancement_id` column is the store's SQL PRIMARY KEY (`store.py:append_pnl_ledger_row`) — re-running the seeding CLI under the UNCHANGED id would hit the existing row and silently no-op ("already present"), never appending the required new-epoch row. See NOTES/assumptions.md for the full trace. Do this edit in the SAME commit as the field deletions above — both fields are fingerprint-INCLUDED (not exclusion-listed), so the ONE new pin must be computed only after all of config.py's edits (deletions + this value bump) have landed together (T-3: compute and pin exactly once, never twice).
- [ ] Compute the new pin ONCE (`python -c "from app.config import Config; print(Config().config_fingerprint())"`) after the two bullets above both land, then update the fingerprint literal at exactly these **13** verified assertion sites to the new value (re-verify each by locating the `CONFIG.config_fingerprint()` / `Config().config_fingerprint()` assertion by symbol, not by the line numbers below, which may have drifted since this planning pass): `test_timeframe_history_api.py:194`, `test_levels.py:718`, `test_tradability.py:370`, `test_backtests.py:416`, `test_backtests.py:1485`, `test_profile_equivalence.py:121`, `test_pnl_scan.py:193`, `test_pnl_scan.py:266`, `test_pnl_scan.py:569`, `test_pnl_scan.py:646`, `test_edge_report.py:213`, `test_setups.py:409`, `test_setups.py:779`. Touch ONLY these 13 lines in these files — nothing else in them (T-3).
- [ ] Add one new test (any kept test module, or a new small file — no existing home) asserting the string `4d665603569b9dbf` appears in zero files under `apps/` (code + tests) — `reports/**`, `runs/**`, `docs/goal-archive/**` are exempt, read-only history (T-11). This is J-04 Step 4's first clause.
- [ ] Run `python -m app.research.pnl_baseline` for real, against the operator's real journal DB / dataset dir (keyless, deterministic — reuses the already-registered founding datasets via the existing `DatasetAlreadyRegistered` REUSE path; never re-records or re-tags them, rail 9). Expect stdout `founding baseline row appended: '<new-id>' — ...` (created=True), never `already present`.
- [ ] Regenerate the committed markdown: `python -m app.research.pnl_history` (the plain render-only path, no `--append-report`). `reports/pnl/pnl-history.md` must show section 1 (the untouched OLD founding row: old id, old fingerprint `4d665603569b9dbf`, unchanged numbers) AND a new section (the NEW-epoch founding row: new id, new fingerprint), with train/holdout never pooled across the two.
- [ ] Re-run the I-9 kept-route byte-comparison capture (`sha256(curl -s <base><route>)` for every kept `/research`+`/tape`+`/meta` GET) against `runs/goal-session-clean_slate/iter-3/kept-route-after.txt`; write the new capture to `runs/goal-session-clean_slate/iter-4/kept-route-after.txt`. Unlike J-01/J-02/J-03 (where the expected diff was "zero, or 1-2 sanctioned rows"), THIS iteration's expected diff is different in kind: every route whose payload embeds `config_fingerprint` (bars/levels/tradability/setups/backtests/pnl-ledger/edge-report/taxonomy) shows a diff, but it must be attributable EXCLUSIVELY to the fingerprint-stamp substring (old pin → new pin) — zero other byte differs. J-04 Step 4's second clause (identical VALUES) is satisfied by this capture PLUS the existing content-hash-cache-busting unit tests (`test_edge_report_cache.py`, `test_edge_report_backtest_cache.py`, `test_tradability_cache.py`, `test_setups.py`) continuing to pass unmodified — they already prove the general "a fingerprint-affecting config change busts a content-hash cache and recomputes with fresh, correct values" mechanism (see `test_edge_report_cache.py::test_config_fingerprint_affecting_field_busts_the_cache` as the existing template); no new cache test is required beyond this re-capture, but the developer may add one if it clarifies the diff.

### Frontend

None. Zero `apps/frontend/` files in scope — J-04 is backend/config/reports only, matching goal.md's own `(Keyless; automated.)` tag.

### New user-facing capability

None — no page, route, or REST response SHAPE changes. Every kept payload keeps the exact same fields; only the embedded `config_fingerprint` stamp value and the PnL ledger's row COUNT change (2 rows instead of 1, both already visible through the existing `/research/pnl/ledger` route and `reports/pnl/pnl-history.md`).

### New information displayed

None new in kind. The SECOND PnL-ledger row is a new instance of the EXISTING "PnL ledger rows" Data Contract entity, served through its existing single owner/endpoint — not a new value type.

### New user actions

None.

### UI surface changes

None. J-04 has no page (`blueprint.md`'s Information Architecture already lists its home as `*(backend/config + reports/pnl/pnl-history.md; no page)* | —`).

### Product surface delta

None visible to a browser user this iteration. Every kept REST/MCP payload keeps serving the identical shape with identical VALUES (only the invisible `config_fingerprint` stamp and the PnL ledger's row count change) — J-05's own iteration is where a human/browser would next observe the kept product end to end under the new pin.

### Blueprint conformance

No new surfaces; no edit to `blueprint.md` required. Both Data-Contract rows this iteration touches already anticipate the exact transition verbatim: `config_fingerprint`'s row already reads "stays `4d665603569b9dbf` through J-01–J-03; moves to ONE new pin in J-04 only (§0.4 Path B) — never touched by any other commit" (still accurate as a permanent description of the one sanctioned move); "PnL ledger rows" already reads "J-04 appends one new-epoch founding row beside old rows (old fingerprint stamps never rewritten)". Record the actual new pin value and the new enhancement id/title chosen in the dev handoff so iteration 5's planner can reference them without re-deriving.

### Data-contract additions

None. J-04 introduces no new displayed value or entity — it changes the VALUE of two already-cataloged Data Contract rows exactly as `blueprint.md` already describes (see Blueprint conformance above), through their existing single canonical computing module + serving endpoint (`Config.config_fingerprint()`; `pnl_ledger.py` / `GET /research/pnl/ledger`). No second computing module or endpoint is introduced for either.

## OUT OF SCOPE

- J-05's full sentinel closure (browser walk of both charts + `/structure` Load + Case Studies drill-in decision + the cumulative diff-vs-inventory cross-check) — reserved for its own iteration (iteration 5); this iteration only advances J-05's backend/keyless sub-clauses (full suite green under the new pin, guard tests pass byte-unmodified, engine equivalence holds).
- Any `Config` field NOT on the corrected 23-field delete list above — every `sr_*`/`tradability_*`/`setups_*`/edge-report/engine/classifier field, and every I-4 KEEP-DANGER field (`journal_db_path`, `journal_busy_timeout_ms`, `journal_schema_version`, `history_marker_states`, `recent_trades_limit`, `event_log_limit`, `market_closed_status_code`, `vendor_call_timeout_seconds`), plus the corrected additions (`analytics_min_sample_size`, `study_arm_sustain_seconds`, `study_arm_cooldown_seconds`, `study_occurrence_r_spread_multiple`, `study_occurrence_r_floor`) — all untouched.
- `pnl_ledger.py`'s writer (`append_validation_row`, `append_pnl_ledger_row`) and `store.py`'s `enhancement_id` PRIMARY KEY / `DuplicateEnhancementError` discipline — untouched. The PnL-reseed fix is scoped entirely to two `config.py` literal values plus running the existing, unmodified CLI; the "one honest row per enhancement" guarantee stays exactly as strict as it is today.
- Any store schema change (`_migrate`, `_create_schema`, no v9, no table drop — T-4) — J-01's already-completed domain; not touched here.
- `app/mcp/`, `app/main.py`, `app/research/routes.py`, any frontend file — zero touch; J-01/J-02/J-03 already closed these surfaces.
- Restoring `SHOW_CASE_STUDIES` on `/structure` — unrelated pre-existing flag; still pending for whoever plans J-05.
- Any second field-name reorder or cosmetic cleanup beyond the corrected delete list — surgical, in-place deletion only (core.md's Surgical Changes principle).
- Re-tagging, re-recording, or content-perturbing the founding reference datasets — the re-seed reuses them via the existing `DatasetAlreadyRegistered` REUSE path only (rail 9).
- Any edit to the engine (`app/engine/`) or any `sr_*`/`tradability_*`/`setups_*`/edge-report computation module — none of these fields' deletions touch a single line of computation code, only `config.py`'s field declarations + exclusion set + the two founding-row literals.

## DEFINITION OF DONE

- [ ] J-04 passes: `Config().config_fingerprint()` returns ONE new pin (≠ `4d665603569b9dbf`), computed only after both the 23-field deletion and the enhancement-id/title value bump have landed; all 13 I-9 assertion sites updated to it and passing
- [ ] The corrected 23-field DELETE list is removed from `Config`; the 4 study_* fields goal.md's I-4 wrongly lists are explicitly NOT deleted (still present, still read live by `Config.strategy_definition()`); `analytics_min_sample_size` is also explicitly NOT deleted (still read by `pnl_ledger.py`)
- [ ] The fingerprint EXCLUSION set is pruned of exactly the 8 now-deleted names listed above and nothing else
- [ ] `pnl_founding_enhancement_id` / `pnl_founding_enhancement_title` bumped to a new distinct literal pair (recorded in the dev handoff); `python -m app.research.pnl_baseline` appends the new-epoch founding row (created=True, stdout is NOT "already present"); the old founding row is byte-untouched
- [ ] `reports/pnl/pnl-history.md` regenerated via `python -m app.research.pnl_history`; renders both epochs honestly, train/holdout never pooled across them
- [ ] New test proves the OLD literal `4d665603569b9dbf` appears nowhere under `apps/` (T-11 exemption for history dirs)
- [ ] The I-9 kept-route re-capture (vs `iter-3/kept-route-after.txt`) shows every fingerprint-embedding route differing ONLY in the stamp substring, zero other byte; the existing content-hash-cache-busting unit tests pass unmodified
- [ ] The engine-equivalence test still proves byte-identical `default` outputs on identical inputs
- [ ] Required-still-passing: J-01/J-02/J-03's kept-route evidence (this iteration's re-capture) and full suite hold with no non-stamp diff; J-05's backend/keyless sub-clauses (full suite green, guard tests byte-unmodified, engine equivalence) hold
- [ ] No anti-goal violation introduced (rails 3/6/8/9 plus every interlude-specific one — especially "no research-value change beyond the documented epoch bump," "never touch a historical record," "no guard weakening," and "no new features"/Config fields — the enhancement-id/title bump is a VALUE edit of 2 EXISTING fields, not a new field, and is logged to `assumptions.md`)
- [ ] Full backend suite passes: 0 failed, 0 errors
- [ ] The three chart guard suites (`test_cockpit_chart_upgrade.py`, `test_structure_chart_viewport.py`, `test_price_chart_confluence.py`), `test_no_execution_path.py`, and `test_no_credential_in_artifacts.py` all pass byte-unmodified
- [ ] Unit tests pass; no regressions
- [ ] Dev handoff written at `docs/handoffs/goal-clean_slate-iter-4-dev.md`, explicitly recording the new fingerprint pin value and the new enhancement-id/title chosen

## TESTING REQUIREMENTS

- Browser: none. J-04 is backend/config/keyless per goal.md's own journey marking (`(Keyless; automated.)`); no page is touched. J-01/J-02/J-03/J-05's regression checks this iteration are all backend/keyless (I-9 re-capture + full suite + guard tests) — the full browser walk stays reserved for J-05's own iteration.
- Unit/integration: the full backend suite (TC-16); the 13-pin-site update (TC-5); the old-literal-retired test (TC-6); `test_backtests.py`, `test_pnl_ledger.py`, `test_pnl_ledger_api.py` in full (TC-2, TC-10); the content-hash-cache-busting suites unmodified (TC-13); the chart guard suites + execution/credential guards unmodified (TC-14, TC-15).
- Error cases: re-running `python -m app.research.pnl_baseline` a SECOND time (same new epoch) must print "already present" and exit 0 with nothing appended — the idempotency guarantee must still hold for the NEW id, exactly as it always held for the old one (only a genuinely new epoch, never a repeat run, produces a new row).

Test-first contract:

- TC-1: given the corrected 23-field DELETE list is removed from `Config` and the 4 wrongly-listed study_* fields are left untouched, when `python -c "from app.config import Config; import dataclasses; print(sorted(f.name for f in dataclasses.fields(Config)))"` runs, then none of the 23 deleted names appear in the output AND `study_arm_sustain_seconds`, `study_arm_cooldown_seconds`, `study_occurrence_r_spread_multiple`, `study_occurrence_r_floor`, and `analytics_min_sample_size` all still do.
- TC-2: given `Config.strategy_definition(STRATEGY_V1_ID)` is unmodified, when `pytest tests/test_backtests.py -q` runs after the field deletions land, then it reports 0 failed — specifically the assertions at (current) lines 194-198/360/1074 comparing `d["entries"]["arm_sustain_seconds"]`, `d["entries"]["arm_cooldown_seconds"]`, `d["exits"]["r_stop"]["spread_multiple"]`, and `d["exits"]["r_stop"]["floor"]` against `CONFIG.study_arm_sustain_seconds`/`CONFIG.study_arm_cooldown_seconds`/`CONFIG.study_occurrence_r_spread_multiple`/`CONFIG.study_occurrence_r_floor` respectively keep passing unmodified — proof the 4-field correction was respected.
- TC-3: given the fingerprint EXCLUSION set is pruned of exactly the 8 now-deleted names (`checklist_stance_dwell_seconds`, `delivery_lag_ok_bound_seconds`, `hint_log_max`, `journal_list_default_limit`, `journal_list_max_limit`, `management_stance_dwell_seconds`, `sound_cue_cooldown_seconds`, `study_list_max`) and no other entry, when the `excluded` set literal inside `config_fingerprint()` is inspected after the diff, then it contains exactly 40 entries (48 today minus these 8) and every remaining entry names a field still present on `Config`.
- TC-4: given `Config.pnl_founding_enhancement_id`/`pnl_founding_enhancement_title` are bumped to a new, distinct literal pair in the SAME commit as the 23 field deletions and the 8-entry exclusion-set prune, when `python -c "from app.config import Config; print(Config().config_fingerprint())"` runs, then it prints exactly ONE new value that is not `4d665603569b9dbf` — this is the single new pin used for every remaining step.
- TC-5: given the new pin from TC-4, when all 13 I-9 assertion sites (`test_timeframe_history_api.py`, `test_levels.py`, `test_tradability.py`, `test_backtests.py` ×2, `test_profile_equivalence.py`, `test_pnl_scan.py` ×4, `test_edge_report.py`, `test_setups.py` ×2 — 13 total) are updated to it, then `grep -rn "4d665603569b9dbf" apps/backend/tests/*.py` returns zero hits (all 13 moved together; nothing else in those files changed vs the pre-iteration snapshot).
- TC-6: given a new test asserts the OLD literal is absent repo-wide under `apps/`, when it runs, then it passes — `4d665603569b9dbf` appears in zero files under `apps/` (code + tests; `reports/**`/`runs/**`/`docs/goal-archive/**` exempt per T-11).
- TC-7: given `python -m app.research.pnl_baseline` is run against the real operator journal DB after the new pin lands, when it executes, then stdout starts with `founding baseline row appended: '<new-id>'` (created=True) — not `already present` — and a subsequent `GET /research/pnl/ledger` request lists 2 rows: the original (old id, old fingerprint `4d665603569b9dbf`, byte-unchanged payload) and the new one (new id, the new fingerprint from TC-4).
- TC-8: given the founding datasets were already registered by the original pre-demolition seeding run, when `seed_founding_row`'s `_obtain_dataset` runs for the SAME two frozen windows (`pnl_founding_train_window`, `pnl_founding_holdout_window`, both untouched), then it hits the `DatasetAlreadyRegistered` REUSE path for both splits — the dataset store's registered-dataset count does not increase, and neither dataset is re-recorded or re-tagged (rail 9).
- TC-9: given `python -m app.research.pnl_history` (no `--append-report`) is run after TC-7, when `reports/pnl/pnl-history.md` is opened, then section 1 is byte-unchanged (old id, `config fingerprint 4d665603569b9dbf`, unchanged numbers) AND a new section renders the new-epoch row (new id, the new fingerprint), with the two sections' train/holdout figures never pooled or compared against each other.
- TC-10: given `pytest tests/test_pnl_ledger.py tests/test_pnl_ledger_api.py -q` runs (all 4 of that suite's `pnl_founding_enhancement_id`-referencing assertions read `CONFIG.pnl_founding_enhancement_id` dynamically, never a hardcoded string — confirmed this planning pass), then 0 failed — the id/title value bump does not break either file.
- TC-11: given re-running `python -m app.research.pnl_baseline` a second time (same new epoch, unchanged config) after TC-7, when it executes, then stdout reads `already present — the founding baseline row '<new-id>' exists; nothing was appended` and exit code is 0 — the idempotency guarantee holds for the NEW id exactly as it always held for the old one.
- TC-12: given the I-9 kept-route re-capture baseline is `runs/goal-session-clean_slate/iter-3/kept-route-after.txt`, when every kept `/research`+`/tape`+`/meta` GET is re-captured after this iteration's full diff and diffed against it, then every route whose payload embeds `config_fingerprint` shows a diff attributable ONLY to the stamp substring (old pin → new pin) — zero other byte differs — and any route that does not embed the stamp (if any) stays fully byte-identical.
- TC-13: given the existing content-hash-cache-busting unit tests (`test_edge_report_cache.py::test_config_fingerprint_affecting_field_busts_the_cache` and its neighbors, `test_edge_report_backtest_cache.py`, `test_tradability_cache.py`, `test_setups.py`'s equivalent coverage) are unmodified, when `pytest tests/test_edge_report_cache.py tests/test_edge_report_backtest_cache.py tests/test_tradability_cache.py tests/test_setups.py -q` runs, then 0 failed — the general "a fingerprint-affecting config change busts a content-hash cache and recomputes with fresh, correct values" mechanism still holds under the new pin.
- TC-14: given none of the 3 chart guard suites (`test_cockpit_chart_upgrade.py`, `test_structure_chart_viewport.py`, `test_price_chart_confluence.py`) are touched, when `pytest tests/test_cockpit_chart_upgrade.py tests/test_structure_chart_viewport.py tests/test_price_chart_confluence.py -q` runs, then 0 failed (T-8 — zero `apps/frontend/` files in this iteration's diff).
- TC-15: given `test_no_execution_path.py` and `test_no_credential_in_artifacts.py` are unmodified, when both run, then 0 failed.
- TC-16: given the full backend suite runs after this iteration's complete diff, when `pytest tests/ -v` executes, then it reports 0 failed, 0 errors — literally, with no residual pre-authorized failure remaining anywhere in the era from this point forward.
- TC-17: given `grep -rn "read_text\|\.open(" apps/backend/tests/*.py | grep -i config` returns zero hits (re-verified this planning pass, iter-2's lesson applied), when the full suite (TC-16) is inspected for any NEW failure beyond the 13 intentionally-updated pin assertions, then none exists — confirming no uncatalogued source-introspection guard broke.

## NOTES

- **Correction 1 — the Config-field DELETE list (logged to `assumptions.md`, `iter-4 — goal-decomposer`, entry 1).** goal.md's I-4 names 18 fields as the "Confirmed DELETE list." This planning pass grepped every one of them (plus every neighboring field in the same journal-era block, `config.py` lines ~508-885) against all of `apps/` for readers outside `config.py`. Full trail:
  - **Wrongly listed (do NOT delete):** `study_arm_sustain_seconds` / `study_arm_cooldown_seconds` / `study_occurrence_r_spread_multiple` / `study_occurrence_r_floor` — all four are read live inside `Config.strategy_definition()` (`config.py`, the method building the `v1`/`structure_tape`/`structure_tape_map` grammar for `GET /research/strategies`) via `self.study_arm_sustain_seconds` / `self.study_arm_cooldown_seconds` / `self.study_occurrence_r_spread_multiple` / `self.study_occurrence_r_floor`, and `study_occurrence_r_spread_multiple`/`study_occurrence_r_floor` are ALSO read directly at `backtests.py:225` (`band = max(s * config.study_occurrence_r_spread_multiple, config.study_occurrence_r_floor)`, the R-stop synthetic-invalidation formula). `tests/test_backtests.py` asserts against all four dynamically at (current) lines 194, 195, 197, 198, 360, 1074. Config's own exclusion-set comment (near `"study_list_max"`) already names these as fields that "shape the persisted study results" and were "DELIBERATELY NOT excluded" — written when the (now-demolished) study runner ALSO read them; the goal author's I-4 list appears to have conflated "the demolished study runner's own use" with "these Config fields," missing that `backtests.py`/`strategy_definition()` independently REUSE the identical fields (the code's own docstring calls this out: "the REUSED studies constants... no new indicator, no second copy of any existing threshold"). Deleting them would raise `AttributeError` inside `strategy_definition()`, breaking `GET /research/strategies`, every backtest, and every edge-report compute — a severe kept-value regression.
  - **Also do NOT delete (not on the confirmed list, but plausible by name):** `analytics_min_sample_size` — its ORIGINAL purpose (`GET /research/analytics`) is deleted, but `pnl_ledger.py` independently reads it (confirmed: `grep -rln analytics_min_sample_size app/ tests/` → `app/research/pnl_ledger.py`, `app/config.py`, `tests/test_pnl_ledger.py`).
  - **Additional closure-rule finds (delete, though NOT on goal.md's confirmed list):** `invalidation_k_consecutive` (config.py:528, sibling of the confirmed `invalidation_epsilon_spread_multiple`; sole historical reader was the deleted `verdict.py`), `journal_list_default_limit` / `journal_list_max_limit` (config.py:614-615; sole reader was the deleted `GET /research/journal` route), `chase_return_threshold` / `invalidation_too_tight_spread_multiple` (config.py:656/664; the two "genuinely new" entry-risk-flag thresholds, sole reader was the deleted thesis-declaration path), `process_outcome_grade_map` / `process_violated_min_failed_checks` / `process_flagged_min_risk_flags` (config.py:678/702/703; the deleted grades.py's own config), `sound_cue_cooldown_seconds` (config.py:885; served via the now-slimmed taxonomy payload + the deleted `SoundCue` component). Each verified this planning pass via `grep -rln <field> app/ tests/` returning ONLY `app/config.py`.
  - Net: 18 confirmed − 4 wrongly-listed = 14, + 9 closure-rule finds = **23 fields to delete**, and **5 fields to explicitly leave alone** despite superficial journal-era association (`study_arm_sustain_seconds`, `study_arm_cooldown_seconds`, `study_occurrence_r_spread_multiple`, `study_occurrence_r_floor`, `analytics_min_sample_size`).
- **Correction 2 — the PnL founding-row re-seed mechanism (logged to `assumptions.md`, `iter-4 — goal-decomposer`, entry 2).** Traced `seed_founding_row` (`pnl_baseline.py:105`): it looks up the ledger by `config.pnl_founding_enhancement_id`, a FIXED literal today. `store.py`'s `pnl_ledger` table declares `enhancement_id` as PRIMARY KEY (`append_pnl_ledger_row` catches `sqlite3.IntegrityError` and re-raises `DuplicateEnhancementError` on any collision). Since the ORIGINAL founding row (visible today in `reports/pnl/pnl-history.md` §1) already occupies that exact id, re-running the seeding CLI unchanged would hit the idempotency short-circuit at line 106 and print "already present" — the new-epoch row would silently never append. The fix (see IN SCOPE) bumps the id/title's literal DEFAULT to a new value in the same commit as the field deletions; this is not a new Config field (both already exist, era-3 J-04), so it does not violate the "no new Config fields" non-goal, and it does not touch `pnl_ledger.py`'s writer or `store.py`'s uniqueness discipline (both stay exactly as strict as today).
- **Ordering discipline recap:** land the 23 field deletions + the 8-entry exclusion-set prune + the enhancement-id/title bump ALL in `config.py` first; compute the new pin exactly once from that final state; update the 13 pin sites to it; THEN run the seeding CLI and the renderer. Computing/pinning twice (once after deletions, again after the id/title bump) would touch the 13 sites twice, a T-3 violation — avoid it by sequencing config.py's edits together before the first pin computation.
- **Lessons applied** (both re-checked live this planning pass, per the goal-decomposer's standing instruction to surface matching `lessons.md` entries): iter-1's "grep the full import list of the surviving consumer, not just the named symbol" generalized from relocated symbols to deleted Config fields — directly produced Correction 1 above. Iter-2's "grep `apps/backend/tests` for `read_text()`/`open(` referencing the deletion target BEFORE deleting" was re-run against `config.py` (`grep -rn "read_text\|\.open(" tests/*.py | grep -i config` → zero hits) — no uncatalogued source-introspection guard exists for this deletion (TC-17).
- **Carried forward, unrelated to J-04:** `SHOW_CASE_STUDIES = false` (`apps/frontend/app/structure/page.tsx:335`) — still unresolved; restore vs. operator-rescope J-05's "Case Study drill-in" acceptance clause before J-05 can close. Not touched this iteration.
- **`blueprint.md`:** read this planning pass; no edit made. Both Data-Contract rows this iteration's diff affects (`config_fingerprint`, "PnL ledger rows") already describe the exact transition verbatim (see Blueprint conformance above) — confirmed no drift between the blueprint's existing text and this iteration's actual scope.
