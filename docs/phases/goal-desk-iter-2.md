# Goal Iteration 2 — Coverage read + resumable bar top-up over the universe (J-02)

<!-- machine-readable goal-mode metadata -->
## Goal Mode Metadata

- **Session ID:** desk
- **Iteration:** 2
- **Mode:** next
- **Depth:** full
- **Frontend Present:** no
- **Target journeys:** J-02
- **Required-still-passing journeys:** J-01 (coverage/top-up's own member-list input — shares the "Universe snapshots + membership" Data-Contract value), J-07 (backend/keyless subset only — see NOTES)
- **Anti-goal reminders (verbatim from `docs/goal.md`):**
  - **No execution path, ever** — no brokerage/trading API, no order tickets, no live OR paper trading, no "just to test" exceptions. (`apps/backend/tests/test_no_execution_path.py` is the tier-1 guard; new research code adds matching guard tests, never weakens them.) *(critical)*
  - **No profit claims and no advice** — every $ figure is a simulated measurement carrying R, n, fee/slippage assumptions, and its train/hold-out/forward basis. No prediction language, no imperative trading cues. *(critical)*
  - **Frozen foundations** — the `v1` strategy, the `default` profile, the tape engine's five states and thresholds, the frozen structure computations, the JSON `BarStore`, and every KEPT surface's behaviour stay byte-identical. New work is additive and versioned beside them, never a mutation of them. (The 5D demolition's removals are final history; this era builds `/desk` BESIDE the kept two pages — the one sanctioned kept-surface edit is J-05's additive `/structure` prefill.) *(critical)*
  - **Hold-out-only promotion** — the champion pointer moves only on a genuine hold-out survival through the sweep gate (plus the era-6 statistical gates once they exist). Train-only wins are labeled overfit. Never lower a minimum sample size, widen a gate, or pool across feeds/fingerprints to manufacture a survivor. *(critical)*
  - **No lookahead** — every value computed as-of T uses only events/bars fully completed at T. *(critical)*
  - **Single source of truth** — each shared value is computed once, owned by one canonical endpoint, and read verbatim by REST/WS/UI/MCP/reports. The coherence-auditor hard-fails violations. *(critical)*
  - **Deterministic and seeded** — every random draw uses a config-owned recorded seed; identical requests reproduce byte-identical results; no wall-clock, no unseeded randomness in any research artifact.
  - **Read-only MCP** — MCP tools remain byte-identical proxies of GET endpoints; nothing on the MCP surface can change state. *(critical)*
  - **Immutable data** — registered datasets and bar series are append-only, checksummed, never re-tagged, never deleted, never content-perturbed. Splits are frozen at registration. *(critical)*
  - **Persistence stays scoped** — no ambient recording of live streams; recording/fetching is an explicit, logged act. *(critical)*
  - **Membership is never a signal.** Universe membership (and any constituents metadata) selects WHAT to screen; it never enters a computation, rank formula beyond selection, feature, or report as an input value. *(critical)*
  - **Snapshots are append-only and pinned.** Universe and screen snapshots are dated, checksummed, append-only; every screen pins (universe snapshot id, screen date, as_of, fingerprint, bar-store signature); nothing is silently refetched, backfilled, recomputed in place, or rewritten — a new run is a new snapshot. *(critical)*
  - **Every run is an explicit operator act.** No scheduler, cron, daemon, auto-refresh, or market-hours trigger anywhere; page-load GETs never trigger fetches or computes. *(critical)*
  - **The briefing describes, never advises.** Desk copy is descriptive measurement only — no advice, imperative, prediction, or ranking language implying action ("buy", "watch this", "opportunity"); the copy-discipline lint stays green unmodified. *(critical)*
  - **No new statistics, gates, or strategies.** No probability/expectancy/edge claims on any desk surface; champion, `v1`, `default`, gates, and minimum-n floors untouched (the Referee is a future era). *(critical)*
  - **The demolition stays demolished.** No journal-era machinery returns; the desk ledger records machine output only — zero manual-input write paths on desk records this era (dispositions/annotations are Era C's design space). *(critical)*
  - **The ledger never holds orders.** No sizes, tickets, entries/exits, or account concepts in any desk record — rail 1 in desk terms. *(critical)*
  - **The suite stays keyless and hermetic.** Committed fixtures cover every test path; no test fetches the network; live fetch/top-up/screen runs are operator-run verifications reported honestly (run-or-not-run), never CI gates. *(critical)*
  - **The fingerprint pin does not move.** All new Config fields take Path A (exclusion + stability test + counter-test + payload provenance, same commit); `08e471b10130e1e2` is asserted unchanged by the sentinel every iteration. *(critical)*
  - **The enhancement loop stays inside its box.** The goal-proposer may append journeys ONLY inside the `AUTO:journeys` marker block of `docs/goal.md` — it MUST NOT edit human-authored journeys, the Anti-goals section, or any other part of that file; proposed journeys MUST carry a single-source-of-truth (or PnL-ledger) acceptance criterion, keep the `default` profile and `v1` byte-identical, and include a `[NEW]`-flagged walkthrough. Manufacturing a low-value journey just to keep the loop alive is a failure. *(critical)*

## GOAL

Ship the era's first desk compute manager: an honest per-member × per-timeframe bar coverage read off `bar_index`, plus an operator-run, resumable, cancellable bar top-up over the pinned universe snapshot's members that walks store-first through the existing `POST /research/bars` path — closing J-02 and unblocking J-03's screen compute.

## BACKGROUND

Iteration 1 (full depth) moved J-01 `failing → passing` on the evaluator's own independently re-executed evidence; J-02–J-06 stayed `failing` (untargeted, now unblocked) and J-07 stayed `partial`. Per the target-selection rubric: rule 1 (no regressed journey exists), rule 2 (`iter-1/coherence.md` is `COHERENCE-PASS`, not `COHERENCE-FAIL` — no consolidation mandate), rule 3 (J-02 is `docs/goal.md`'s own next link in the stated chain "J-01 → J-02 → J-03 → J-04 → J-05 → J-06", and its coverage/bar-freshness output is a direct input to J-03's screen compute), rule 5 (J-02 alone — it is already this iteration's one risky/full journey: a brand-new compute-manager pattern plus a correctness contract against the frozen `levels.py`/`compute_tradability` timeframe set; no second risky journey rides along), and rule 6 (no human-owned blocker exists) all point at J-02 alone — matching the iter-1 evaluator's explicit Next-Step Recommendation ("Target J-02 alone... Run it at full depth") and the binding "Do not redo" entry in `iteration-state.md` ("Next target settled: J-02 alone, at full depth").

**Depth is `full`**, citing two independent triggers from the Picking-depth rubric: (1) **Structural/cross-cutting** — this is the era's FIRST desk compute manager, instantiating the single-flight/progress/cancel/resumable pattern outside `edge_report_compute.py` for the first time, spanning a new coverage-read module, a new top-up compute manager + three routes + a CLI warmer, and a reuse (never a duplicate) of the existing `POST /research/bars` fetch-and-record path — interactions (store-first skip correctness, resumability-after-cancel, single-flight, GET-never-computes) that no single existing unit test covers; (2) **Data model** — this iteration FIXES the computing module + serving endpoint for two `blueprint.md`-registered-but-placeholder Data-Contract rows ("Per-member bar coverage + freshness" and the top-up half of "Top-up / screen compute progress"), resolving `blueprint.md`'s own explicitly-deferred build-time decision (see Blueprint conformance below). Both independently confirm the iter-1 evaluator's own `full` recommendation; the hardening-cadence trigger (4) does not apply (0 consecutive lean iterations dispatched — iter-1 was full) and is not the reason for this call.

**Frontend Present is `no`.** J-02's own `docs/goal.md` acceptance is explicitly tagged `(Keyless core; the real ~100-symbol Yahoo top-up is an operator-run act with its outcome — including partial coverage — reported honestly.)` with no browser step, and `blueprint.md`'s Feature/journey-homes table states J-02 has "no standalone page" — its coverage/tick-evidence badges surface on `/desk` only once J-04 ships. This mirrors the `goal-desk-iter-1`/J-01 precedent exactly.

**Lessons applied (from `lessons.md` and the iter-1 eval's carry-forwards):**
1. (iter-1) `edge_report_cache._config_content_hash` (`edge_report_cache.py:165-169`) is a SECOND, unnamed whole-config hash with no exclusion set, keying the `setups`/`tradability`/`edge-report`/`edge-report-backtest` caches — the iter-1 diff already moved it once (to `dc0271c15a26…`), stranding those caches (real-data `GET /research/setups` cold ~9–11 min, `/structure` Load ~21.6 s). **Decided policy this iteration:** accept (latency-only — served values are unaffected, per iter-1's own audit) and defer any warm to whichever iteration next dispatches browser QA (expected J-04); fixing the hash's own exclusion discipline is explicitly OUT OF SCOPE this iteration (it would bundle a second cross-cutting change into an already-full J-02, rule 5). If this iteration adds its own new `Config` field, the hash will move again — expected, not a defect.
2. (iter-0, restated — still not actionable) `journey-scripts/J-07.json` step 8 asserts async, cache-warmth-dependent text on a 15s timeout (a documented false-negative shape), and the scoped QA backend's first `GET /research/setups` call runs cold at ~9–11 min. Neither is exercised this iteration (`Frontend Present: no` — no browser QA dispatches) — carried forward again for J-04.
3. (iter-1 audit, finding T2) The kept-route byte-comparison capture (TC-11 in iter-1) covered only 14 of the app's 24 kept GET route templates, against a near-empty data dir. Applied this iteration: widened to all 24 templates, captured against a data dir populated by this iteration's own fixture top-up run (TC-13 below).

**Coverage endpoint placement decided this iteration.** `blueprint.md` explicitly deferred the `GET /research/desk/coverage` vs. universe-payload-block choice to "whichever iteration builds J-02." This spec decides: a **dedicated endpoint**, `GET /research/desk/coverage` — it keeps `GET /research/desk/universe`'s already-passing J-01 response shape untouched, and matches the era's one-concept-per-endpoint convention (`/universe`, `/coverage`, `/screen`). `blueprint.md` is updated to match (see Blueprint conformance / Data-contract additions).

## IN SCOPE

### Backend
- [ ] Pin the desk top-up timeframe set as a plain structural constant (the `PRIOR_PERIOD_TIMEFRAMES` precedent, `levels.py:106` — NOT a `Config` field, since it is derived from the existing frozen contract rather than a new tunable): `{"1h", "4h", "1d", "1w"}`. Verified this iteration against the live tree (re-verify at build time per J-02 step 2, but the answer is already checked): `config.py:770` (`bar_timeframes`, the full 9-entry set), `config.py:821-832` (`sr_timeframe_weights`, which weights all 9 but only these 4 are both Yahoo-servable and non-intraday-microscope), `levels.py:106` (`PRIOR_PERIOD_TIMEFRAMES = ("1d","1w","1mo")`, the long-term bucket `compute_levels`/confluence-classing reads), and `yahoo.py:66-72`/`:158` (`_INTERVAL_MAP` maps `1d/1w/1h/5m/1m` directly; `4h` is the local resample of `1h`; `8h`/`15m`/`1mo` raise `UnsupportedTimeframe`). `5m`/`1m` are excluded per J-02's own explicit "no 5m/1m in the desk top-up" acceptance text.
- [ ] New desk-coverage module (e.g. `app/research/desk_coverage.py`, name at build discretion, mirroring `desk_universe.py`'s module-naming freedom): a pure read function that, for the latest universe snapshot's members × the pinned timeframe set, reports `has_bars` (any `bar_index` row exists for that `(symbol, timeframe)`) and `latest_window_end_utc` (the most recent `window_end_utc` among that pair's indexed rows) — reading `bar_index` only, never re-hashing `BarStore` (T-4).
- [ ] A minimal, ADDITIVE extension to `bar_index.py`'s public read API (e.g. a new field on `BarIndexHit`, or an equivalent new accessor) exposing the already-existing `window_end_utc` SQLite column — never a DB-schema change, never touching `.lookup()`/`.insert()`'s existing contract or any current caller's behavior (logged in `assumptions.md`, iter-2).
- [ ] `GET /research/desk/coverage` route (new, dedicated endpoint — see BACKGROUND): serves the latest universe snapshot's coverage; an honest empty payload (`universe_snapshot_id: null`, `members: []`) before any universe snapshot exists — HTTP 200, never a 404 (mirrors `GET /research/desk/universe`'s own convention).
- [ ] A desk top-up compute manager mirroring `EdgeReportComputeManager` (`edge_report_compute.py:108`) verbatim in shape: one in-flight job slot, an in-memory process-scoped snapshot (`id`/`state`/`started_utc`/`finished_utc`/`error`/`progress`), cooperative cancel, atomic snapshot publish under a lock.
- [ ] Top-up work function: walks the latest universe snapshot's members × the pinned timeframe set; for each `(symbol, timeframe)` pair, calls the SAME existing bar-fetch-and-record logic `POST /research/bars` already uses (in-process reuse — never a second fetch-and-record implementation), which is itself already store-first (`bar_index`-backed); records one honest per-pair outcome — `"reused"` when the store-first hit answered it, `"fetched"` when a real vendor call ran, `"failed"` with the surfaced error detail (never swallowed) — into the job's `progress.outcomes` list.
- [ ] `POST /research/desk/topup/compute` (trigger; single-flight, returns `{"started": bool, "compute": <snapshot>}`), `GET /research/desk/topup/compute` (poll; snapshot or `null`), `POST /research/desk/topup/compute/cancel` (409 when idle) — mirrors `routes.py:1268/1293/1302` exactly.
- [ ] CLI warmer (e.g. `python -m app.research.desk_topup_compute`, mirrors `edge_report_compute.py`'s `main()`): runs the top-up to completion synchronously, in-process, for the operator's real ~100-symbol run; one progress line per completed pair.
- [ ] IF a genuinely new semantic `Config` field is needed (e.g. a per-timeframe fetch-window/lookback for the top-up's vendor calls), it takes Path A in the SAME commit — exclusion-set entry, stability test, counter-test, payload provenance — mirroring the 4 shipped `desk_universe_*` fields. Prefer reusing an existing pattern over inventing a new one. Operational knobs (a top-up worker/concurrency cap, if any) are env vars, never `Config` fields (the `TAPEOLOGY_EDGE_SWEEP_WORKERS` precedent), per Constraints.
- [ ] Widen the kept-route byte-comparison regression capture (the `goal-desk-iter-1` I-9-style protocol) from the 14 previously-probed templates to all 24 kept GET route templates (per iter-1 audit finding T2, `docs/handoffs/goal-desk-iter-1-audit.md:178-183`), captured before and after this iteration's diff, against a data dir populated by this iteration's own fixture top-up run (not near-empty).
- [ ] Unit/integration-test: the coverage truth-table (including the honest-empty state), top-up resumability + store-first-reuse + single-flight + cancel, the GET-never-computes rule on both new GET routes, the pinned-timeframe-set assertion against `levels.py`'s live constants, the `bar_index.py` read-API extension (additive only — existing `test_bar_index.py` behavior byte-unmodified), and (if added) the new Config field's Path-A stability + counter-tests.

### Frontend
None. `Frontend Present: no`. J-02's coverage/top-up data surfaces on `/desk` in J-04 only (per `blueprint.md`'s Feature/journey-homes table); no `/desk` page, nav row, or any frontend file changes this iteration.

### New user-facing capability
Backend/operator/CLI capability only: an operator (or the goal-mode test suite, or — once J-06 ships — a Claude+MCP `get_endpoint` call) can read per-member bar coverage for the latest universe snapshot and run a resumable, cancellable, store-first top-up over its members. No on-screen control yet.

### New information displayed
None on-screen this iteration (no `/desk` page exists yet). Coverage and top-up-progress become real, readable JSON via the new REST endpoints; their on-screen rendering (coverage badges, a Top-up button + live progress) is J-04's job.

### New user actions
None in the UI. `POST /research/desk/topup/compute` (+ poll + cancel) and `GET /research/desk/coverage` exist only as REST/CLI surfaces this iteration.

### UI surface changes
None. `/` and `/structure` are unchanged; `/desk` does not exist yet (ships in J-04); `UI_ROUTES` stays at exactly 2 rows.

### Product surface delta
None visible in the browser this iteration. The backend gains an honest coverage read plus a resumable top-up compute job, both reachable only via REST/CLI/tests; the visible product change (coverage badges + Top-up button on `/desk`) arrives in J-04.

### Blueprint conformance
No new page ships this iteration (J-04 builds `/desk`); `UI_ROUTES` stays at 2 rows — no nav-skeleton change, so no `blueprint.reapproval-requested` entry is written. J-02's backend modules + routes are the canonical home already registered in `blueprint.md`'s Feature/journey-homes table (row "J-02 Coverage + explicit bar top-up" → Desk section, "no standalone page"). `blueprint.md`'s Data Contract IS edited this iteration (additive, no human approval needed): the "Per-member bar coverage + freshness" row's endpoint placeholder is resolved to the dedicated `GET /research/desk/coverage`, with exact field-level shape added; the "Top-up / screen compute progress" row is split into its now-concrete top-up half (this iteration) and an explicit open placeholder for the screen half (J-03).

### Data-contract additions
1. **Per-member bar coverage + freshness** — computed by the new desk-coverage module (reads `bar_index` only, T-4, plus the additive `BarIndexHit`/read-API extension above), served by `GET /research/desk/coverage` (dedicated endpoint — this iteration's build-time decision, replacing `blueprint.md`'s "ONE home, to be fixed by whichever iteration builds J-02" placeholder). Exact shape: `{"universe_snapshot_id": str | null, "timeframes": ["1h","4h","1d","1w"], "members": [{"symbol": str, "per_timeframe": {"<tf>": {"has_bars": bool, "latest_window_end_utc": str | null}}}]}`. Honest-empty (`universe_snapshot_id: null`, `members: []`) before any universe snapshot exists — HTTP 200, never 404. Registered in `blueprint.md` this iteration.
2. **Top-up compute progress** — computed by the new desk top-up compute manager (mirrors `EdgeReportComputeManager`'s bookkeeping shape verbatim), served by `POST/GET /research/desk/topup/compute` + `POST /research/desk/topup/compute/cancel`. Exact shape: `{"id": str, "state": "running"|"done"|"cancelled"|"failed", "started_utc": str, "finished_utc": str | null, "error": str | null, "progress": {"pairs_total": int, "pairs_done": int, "outcomes": [{"symbol": str, "timeframe": str, "outcome": "reused"|"fetched"|"failed", "detail": str | null}]}}`. Process-scoped bookkeeping — never a research value (the `EdgeReportComputeManager` precedent). Registered in `blueprint.md` this iteration (specializing the top-up half of the pre-registered generic row; the screen half stays an explicit open placeholder for J-03).

## OUT OF SCOPE

- J-03 (screen compute + append-only ledger), J-04 (`/desk` page — `UI_ROUTES` stays 2 rows, nav unchanged), J-05 (screen history + `/structure` prefill), J-06 (MCP `desk_universe`/`desk_screen` tools — MCP suite stays 15 tools) — all explicitly deferred to their own iterations per `docs/goal.md`'s stated dependency order.
- Tick-evidence badges (dataset-registration presence) — J-03's screen row summary concern per `docs/goal.md` Key Capability 3, not J-02's coverage payload.
- A "freshness threshold" / `is_fresh` boolean — coverage serves the raw `latest_window_end_utc` only; deriving a staleness judgment from it is a display-layer concern for J-04.
- Modifying `edge_report_cache._config_content_hash`'s exclusion discipline — accepted as a known, documented, latency-only gap (iter-1 lesson); decided policy is accept + warm-before-next-browser-pass (J-04), not a code fix this iteration (rule 5: avoids bundling a second cross-cutting change into an already-full iteration).
- J-01 hardening items from the iter-1 recommendation (parser `skipped_rows` count; making audit finding B3's corrupt-file replacement loud) — J-01 is DONE and binding "Do not redo" per `iteration-state.md`; both are audit-noted `PASS_WITH_GAPS` gaps, not journey acceptance criteria, and touching `desk_universe.py` again would bundle a second risky change into this already-full J-02 iteration (rule 5).
- The real ~100-symbol Yahoo top-up run itself — an operator-run act reported honestly (including partial coverage), not a CI/test gate, per `docs/goal.md`'s own framing.
- Re-pointing `journey-scripts/J-07.json` step 8's async-text assertion, or warming the `/research/setups` scan cache — no browser QA dispatches this iteration to exercise either; carried forward again for J-04 (see NOTES).
- Any change to `tradability.py`, `levels.py`, `bars.py`'s store format, or `EdgeReportComputeManager` itself — J-02 reads/reuses these verbatim, never modifies them (single source of truth). Any change to `bar_index.py`'s DB SCHEMA (the `_SCHEMA` table definition, primary key) or `.lookup()`/`.insert()`'s existing semantics — the one sanctioned exception is the minimal additive read-accessor named in IN SCOPE.
- Any change to the fingerprint pin `08e471b10130e1e2` itself, or to any of the 13 existing pin-assertion sites — Path A only, forever this era.
- Re-probing already-verified J-01 acceptance clauses or re-shooting prior browser-walk screenshots — both already DONE and binding "Do not redo" per `iteration-state.md`.

## DEFINITION OF DONE

- [ ] J-02 passes: coverage truth-table (bars-present vs missing, per-member, including the honest-empty pre-universe state), top-up resumability (cancel-then-resume skips already-covered pairs), store-first proven (a second run reports all-reused with zero vendor calls), single-flight, GET-never-computes, index-read latency (no store re-hash), honest failure handling — all verified directly (TC-1 – TC-10, TC-14, TC-15)
- [ ] Required-still-passing: J-01 stays passing — `GET /research/desk/universe` byte-identical to iter-1's baseline, unperturbed (TC-12); J-07's backend/keyless subset stays green — suite green, pin unchanged, all 24 kept GET route templates byte-identical (TC-11, TC-13)
- [ ] No anti-goal violation introduced — immutable data / store-first (TC-7, TC-8), every run an explicit operator act (TC-10), single source of truth (coverage/top-up reuse the existing `bar_index`/`POST /research/bars` owners verbatim, never a second implementation), fingerprint pin unchanged (TC-11)
- [ ] Unit tests pass; no regressions — suite pass count ≥ 1210 (the iter-1 floor), skip count ≥ 8 (non-decreasing) (TC-11)
- [ ] Dev handoff written at `docs/handoffs/goal-desk-iter-2-dev.md`

## TESTING REQUIREMENTS

- Browser: none. `Frontend Present: no` — J-02's own `docs/goal.md` acceptance is tagged `(Keyless core...)` with no browser step; no Chrome MCP dispatch is expected or required this iteration. J-07's browser-walked portion is not re-shot (zero frontend diff this iteration); its regression coverage this iteration is the widened 24-template kept-route byte-comparison (TC-13) plus the live fingerprint print (TC-11), mirroring iter-1's own precedent.
- Unit/integration: the coverage truth-table over a fixture bar store + fixture universe (including the honest-empty pre-universe and empty-bar-store states); top-up single-flight/cancel/resume/store-first-reuse over a fixture-scoped `POST /research/bars` seam with an instrumented vendor-fetch call counter; a GET-never-computes call-counting guard on both new GET routes; the pinned-timeframe-set assertion against `levels.py`'s live constants; the `bar_index.py` read-API extension proven additive (existing `test_bar_index.py` assertions byte-unmodified and still passing); Path-A stability + counter-test for any new Config field; the widened 24-template kept-route capture against a populated data dir.
- Error cases: a top-up pair whose vendor call fails (the existing `NoDataForWindow`/`VendorTimeout`/`UnsupportedTimeframe` taxonomy) surfaces as an honest `"failed"` outcome with the detail preserved, never swallowed and never silently dropped from the outcomes list, and the run continues to the remaining pairs rather than aborting the whole job; `GET /research/desk/coverage` before any universe snapshot exists returns an honest empty payload, never a 500 or a fabricated row; `POST /research/desk/topup/compute/cancel` on an idle manager returns 409.

Test-first contract: every DEFINITION OF DONE checkbox and every Data-contract addition above maps to at least one concrete scenario line below.

- TC-1: given no universe snapshot has ever been registered (a fresh test-scoped universe dir), when `GET /research/desk/coverage` is called, then it returns HTTP 200 with `universe_snapshot_id: null` and `members: []` — honest empty, never a 404 or 500.
- TC-2: given a universe snapshot with N members and an empty bar store, when `GET /research/desk/coverage` is called, then `coverage.members` lists all N members with `has_bars == false` for all 4 pinned timeframes, for every member.
- TC-3: given a bar-store fixture with bars recorded across all 4 pinned timeframes (`1h`, `4h`, `1d`, `1w`) for exactly 2 of a 5-member fixture universe's symbols and none for the other 3, when `GET /research/desk/coverage` is called, then those 2 symbols report `has_bars == true` for all 4 timeframes and the other 3 report `has_bars == false` for all 4 timeframes — a per-member truth-table, asserted individually for every member.
- TC-4: given the fixture from TC-3 with a bar series recorded for a covered symbol at a known `window_end_utc`, when `GET /research/desk/coverage` is called, then that symbol's `latest_window_end_utc` for that timeframe equals the exact recorded value (freshness reads the real `bar_index` column verbatim, never fabricated or rounded).
- TC-5: given `bar_index` instrumented for call-count and populated for a fixture universe, when `GET /research/desk/coverage` is called, then it issues only `bar_index` reads — zero `BarStore.list()`/full-store-hash calls (the T-4 latency contract).
- TC-6: given a fixture universe with N members and an empty bar store, when `POST /research/desk/topup/compute` is triggered, then the response's `progress.pairs_total` equals `N * 4` and polling `GET /research/desk/topup/compute` shows `state` progressing to `"done"` with exactly one outcome entry per `(member, timeframe)` pair, each `outcome` in `{"fetched","reused","failed"}`.
- TC-7: given the completed top-up run from TC-6, when `POST /research/desk/topup/compute` is triggered a second time over the same universe snapshot with the vendor-fetch seam instrumented for call-count, then every outcome entry reports `outcome == "reused"` and zero vendor calls are made — store-first proven.
- TC-8: given an in-flight top-up run cancelled (via `POST /research/desk/topup/compute/cancel`) after M of N*4 pairs completed, when the run is re-triggered, then the resumed run's outcomes report `"reused"` for those same M pairs (no re-fetch of frozen series) and only the remaining pairs are freshly attempted — resumability.
- TC-9: given an in-flight top-up run (`state == "running"`), when `POST /research/desk/topup/compute` is triggered again concurrently, then the response reports `started: false` and the SAME job `id` as the in-flight run — single-flight.
- TC-10: given no explicit trigger call, when either new GET route (`GET /research/desk/coverage`, `GET /research/desk/topup/compute`) handles a request, then no bar fetch or compute job starts as a side effect — asserted via a call-counting fixture on the vendor-fetch seam / compute-manager `trigger` (GET-never-computes).
- TC-11: given the full backend suite run after this iteration's diff, when it completes, then it reports a pass count ≥ 1210 (the iter-1 floor) and a skip count ≥ 8 (non-decreasing), and `Config().config_fingerprint()` prints `08e471b10130e1e2` unchanged, including under any new desk `Config` field's non-default override (Path A stability), if one is added this iteration.
- TC-12: given the fixture universe snapshot from J-01 unperturbed by this iteration's diff, when `GET /research/desk/universe` is re-called, then its response is byte-identical to iter-1's captured baseline — J-01 stays passing.
- TC-13: given pre- and post-iteration captures of all 24 kept GET route templates (widened from iter-1's 14, per audit finding T2) against a data dir populated by this iteration's own fixture top-up run, when the two captures are diffed, then every row is byte-identical except label/port comment lines.
- TC-14: given a top-up pair whose underlying vendor call raises the existing `NoDataForWindow`/`VendorTimeout`/`UnsupportedTimeframe` taxonomy, when the top-up processes that pair, then its outcome entry reports `outcome == "failed"` with the error detail preserved verbatim, and the run continues to the remaining pairs rather than aborting the whole job.
- TC-15: given an idle top-up compute manager (no job has ever run, or the last job is terminal), when `POST /research/desk/topup/compute/cancel` is called, then it returns HTTP 409.

## NOTES

- **Lessons applied** — see BACKGROUND for the full reasoning; summary: (1) the `_config_content_hash` second-hash landmine is accepted, not fixed, this iteration; (2) the J-07 golden-script / cold-setups-cache lessons are carried forward again for J-04 (not actionable while `Frontend Present: no`); (3) the kept-route capture widens from 14 to 24 templates (audit T2) and runs against a populated data dir.
- **J-01 hardening deferred, not forgotten.** The iter-1 recommendation's two J-01 hardening items (parser `skipped_rows` count; loud B3 corrupt-file replacement) are excluded this iteration per rule 5 and the binding "Do not redo" — see OUT OF SCOPE. Revisit only if a later journey's acceptance genuinely depends on them.
- **Verified timeframe set — do not re-derive.** The desk top-up's pinned set `{1h, 4h, 1d, 1w}` was checked directly against the live tree before writing this spec: `config.py:770` (`bar_timeframes`), `config.py:821-832` (`sr_timeframe_weights`), `levels.py:106` (`PRIOR_PERIOD_TIMEFRAMES`), `yahoo.py:66-72`/`:158` (`_INTERVAL_MAP` + the `4h` local resample). This satisfies J-02 step 2's "verify against `levels.py` at build time" — the developer should re-confirm quickly, not re-derive from scratch.
- **`bar_index.py` extension is additive-only, and is logged as an interpretation call.** See `assumptions.md` (iter-2 entry): permitting a minimal, additive read-API extension (exposing the already-existing `window_end_utc` column) rather than resolving every coverage hit through `BarStore.get()`. No DB-schema change; `.lookup()`/`.insert()`'s contract and all current callers (`POST /research/bars`'s store-first coordinator) stay byte-unmodified.
- **Hermetic-test discipline, explicit reminder.** `apps/backend/.data/universe/` already holds a real registered snapshot from iter-1 QA (gitignored, uncommitted, operator-initiated). This iteration's tests must use their OWN temp-scoped universe + bar dirs (the `TAPEOLOGY_DESK_UNIVERSE_DIR`/`TAPEOLOGY_BAR_DIR` env-override pattern) — never the ambient real dir — to stay hermetic and reproducible.
- **References:** iter-1 eval `runs/goal-session-desk/iter-1/eval.md` (Next-Step Recommendation); iter-1 audit `docs/handoffs/goal-desk-iter-1-audit.md` (finding T2, the 24-template count); blueprint `runs/goal-session-desk/state/blueprint.md` (Data Contract rows "Per-member bar coverage + freshness" and "Top-up compute progress", edited this iteration); goal `docs/goal.md` (J-02, Key Capability 2, Constraints, Build anchors T-4/T-5/T-6/T-7); local precedent `docs/phases/goal-desk-iter-1.md` (identical shape: backend-only, full depth, `Frontend Present: no`) and `edge_report_compute.py` / `routes.py:1268-1302` (the compute-manager pattern this iteration copies).
