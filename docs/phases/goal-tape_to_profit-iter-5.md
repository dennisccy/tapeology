# Goal Iteration 5 — The /performance page: PnL per enhancement, honestly (J-05)

<!-- machine-readable goal-mode metadata -->
## Goal Mode Metadata

- **Session ID:** tape_to_profit
- **Iteration:** 5
- **Mode:** next
- **Depth:** lean

Frontend Present: yes

- **Target journeys:** J-05
- **Required-still-passing journeys:** J-01, J-02, J-03, J-04, J-08
- **Dispatch framing:** verify-and-complete resume — a prior dispatch of THIS iteration completed the developer step and was interrupted before review; the full implementation already exists uncommitted (see BACKGROUND → Resume context). Scope is IDENTICAL to the interrupted dispatch.
- **Anti-goal reminders:**
  - **No live execution path.** Tapeology MUST NOT place, route, or transmit orders anywhere — no brokerage integration, no trading API, **no paper-trading API**, no order tickets, no recommendation to execute. The ONLY permitted "fill" is the offline backtester's simulated fill computed against recorded historical tape, clearly labeled simulated and sent nowhere. *(critical)*
  - **No profit claims and no advice.** Simulated PnL is a caveated measurement: it MUST always appear with its R counterpart, its n, its fee/slippage assumptions, its train-or-hold-out basis, and its null baseline — and MUST never be presented as expected live results, an edge claim, or a reason to trade. No imperative cues, no prediction language. *(critical)*
  - **Default engine outputs are frozen.** Indicator evolution is additive and versioned only: candidate profiles may add feature keys or alternate thresholds, but the `default` profile's outputs stay byte-identical (equivalence-tested), the live cockpit uses `default` only, and no enhancement may mutate an archived-era behavior to pass. *(critical)*
  - **No train-only promotion.** Nothing becomes the champion, a proposed journey, or a claimed improvement on the strength of train data alone: hold-out survival (net R AND net $, with the configured minimum n) is the only promotion gate; overfit results are labeled overfit. *(critical)*
  - **No ML, no online tuning.** Candidate search is bounded, config-enumerated, offline, and deterministic; no fitted models, no optimizer loops inside the engine, no thresholds that move at runtime.
  - **No fabricated data — honest failure states.** No synthesized trades, quotes, fills, datasets, or PnL to force a green journey; every failure mode (backend down, corrupt dataset, empty window, missing credentials, insufficient n) surfaces an explicit, distinct state. *(critical)*
  - **Single source of truth.** Every canonical value in the Data Contract is computed once and read verbatim by every surface — REST, WebSocket, UI, markdown reports, and MCP. A second computation path or a diverging number across surfaces is a defect. *(critical)*
  - **MCP is read-only.** The MCP server exposes no mutating tools, proxies only the canonical GET surface (plus the allowlisted `get_endpoint`), and MUST NOT become a second implementation of any computation. *(critical)*
  - **Persistence stays scoped.** SQLite holds research records (now including backtests and the PnL ledger); the dataset store holds explicitly recorded historical tape for research replay. The live cockpit's tape remains unpersisted; no ambient recording. *(critical)*
  - **The enhancement loop stays inside its box.** The goal-proposer may append journeys ONLY inside the AUTO:journeys marker block above — it MUST NOT edit human-authored journeys, this Anti-goals section, or any other part of this file; proposed journeys MUST carry a PnL-ledger acceptance criterion, keep the default profile byte-identical, and include a [NEW]-flagged walkthrough. Manufacturing a low-value journey just to keep the loop alive is a failure. *(critical)*

## GOAL

The researcher can open the fourth top-level page, `/performance`, from a Performance link on the persistent nav (rendered from `GET /meta/ui-routes`) and read the PnL ledger and the current champion (strategy + profile) verbatim from the canonical endpoints — every $ beside its R and its n, train and hold-out separate, insufficient-sample labels live on the real n=1 founding row, the simulated register visible — in the existing dark cockpit design language.

## BACKGROUND

Iter-4 delivered J-04 (append-only PnL ledger + founding baseline row; 983 passed / 1 skipped; COHERENCE-PASS) and the evaluator recommended J-05 next at lean depth — the last link of the J-02 → J-03 → J-04 → J-05 chain, and **the first frontend iteration of era-3**. Everything the page needs is live: `GET /research/pnl/ledger` serves the founding row (train net R −0.16000000000001136 / net $ −16.000000000001137, holdout net R 0.3334000000001356 / net $ 33.34000000001356, n=1 per split, both labeled insufficient sample, baseline side explicitly `null`, register verbatim), and the NavBar already renders `GET /meta/ui-routes` with NO hardcoded list — adding the `/performance` entry to `UI_ROUTES` in `app/meta.py` makes the fourth link appear with zero nav-component edits (the no-dead-link rule: entry and page ship together, exactly as `meta.py`'s own docstring anticipates). One gap: J-05's champion summary must read the champion from its **registered** canonical endpoint — Data Contract row 33 assigns the champion pointer to `GET /research/profiles`, which today is an honest 404 (J-06 not yet landed). This iteration therefore lands that endpoint **minimally** (serving side only: the frozen `default` profile + the founding champion pointer, all from existing config constants); J-06's candidate-profile substance stays out of scope. Inferring the champion from ledger provenance or hardcoding it in the page would be exactly the second-computation-path drift the coherence-auditor fails.

**Resume context (this dispatch is verify-and-complete):** a prior dispatch of THIS iteration completed the developer step and was interrupted before the reviewer ran (the engine paused on an in-flight timeout during an idle gap; since resumed — `runs/goal-tape_to_profit-iter-5/status.json` reads `current_step: dev_complete`, `next_action: review`). The full J-05 implementation already exists UNCOMMITTED on `goal/tape_to_profit`, matching this spec item-for-item: `apps/frontend/app/performance/page.tsx`; the `/performance` entry in `app/meta.py` `UI_ROUTES` (+ docstring tense fix); the minimal `GET /research/profiles` (`app/research/profiles.py` + one route in `app/research/routes.py`); the documentation-strings-only `app/mcp/__init__.py` diff; updated `test_meta_routes.py` / `test_mcp_server.py` (honest-404 legs relocated to a permanently-unknown path, live-200 byte-identity added) and new `test_profiles_api.py`; frontend `lib/types.ts` + `lib/api.ts` helpers; both golden scripts (`J-01.json` evolved 3→4 destinations with the honest not-found step still final; `J-05.json` recorded, 11 steps). Dev + frontend handoffs exist (`docs/handoffs/goal-tape_to_profit-iter-5-dev.md`, `-frontend.md`) claiming 988 passed / 1 skipped, equivalence 7/7, `npm run build` clean, replay 2/2, 19/19 page-equals-API browser checks. **Those claims are UNVERIFIED until this dispatch's reviewer, browser-qa, and evaluator independently confirm them.** The developer step therefore re-runs the suites/build/replays against the existing working tree and fixes only what re-verification exposes — it MUST NOT rebuild, rewrite, or churn the existing implementation (surgical-changes rule), and MUST NOT re-record golden scripts that replay green. If everything re-verifies green, "no code changes — verified as-is" is the correct and successful developer outcome.

**Lessons applied:**
- *(iter-4, applies to J-05 explicitly)* The committed fixture pair arms n=1 per split — below `pnl_min_sample_size` 5 — so the page's "insufficient sample" rendering is exercised by real data from day one, not by a synthetic test row.
- *(iter-1, applies to every iteration)* The merged UI results MUST contain one explicit result row per required-still-passing browser journey (J-01, J-08) — a missing row is NOT a pass.
- *(iter-2, applies to J-02/J-03/J-04)* Machine-surface journeys have no golden replay script; their required-still-passing coverage routes through the automated backend suite (plus in-page `fetch()` from a backend-origin page where browser-originated evidence is wanted).
- *(iter-3, applies to every browser lane)* Before diagnosing "flaky browser" or sqlite I/O errors, check `/tmp` tmpfs pressure (fresh check at THIS dispatch's planning: pytest dir 2.3G, /tmp at 48% — headroom below the ~80% quota crisis point, but grown from the prior dispatch's 1.2G/30%; the pump reaper is active — re-check `du -sh /tmp/pytest-of-dennis-chan` first if browser/replay/pytest failures look like resource exhaustion).

## IN SCOPE

### Backend
- [ ] **`/performance` enters the canonical route map (Data Contract row 35).** Add `{"path": "/performance", "label": "Performance", "nav": True}` to `UI_ROUTES` in `app/meta.py` — shipped in the same iteration as the page, per the blueprint's no-dead-link rule (and `meta.py`'s own docstring). This is the ONLY route-list edit anywhere; the nav picks it up with no frontend nav change. Update the route-map unit tests' pinned expectations (5 entries, 4 with `nav: true`, `/performance` present with label `Performance`) and any MCP `ui_route_map` pinned payloads.
- [ ] **Minimal `GET /research/profiles` (Data Contract row 33 — serving side only).** On the existing research router, exactly this one GET route: serves the config-owned profile registry — today exactly one profile, `default`, marked frozen/default — plus the champion pointer (strategy `v1` + profile `default`, the founding champion; no promotions exist yet, so this is the config-owned initial value row 33 declares). Reuse the existing single-copy constants (`PROFILE_DEFAULT`, `backtests.py:125`; `STRATEGY_V1_ID`, `config.py:22`) — NO second copy of any id string, no new magic values. No candidate registration, no profile-selection mechanics, no champion-movement mechanics (J-06/J-07). Non-GET verbs → 405 (no write surface). Module placement is developer latitude (e.g. `app/research/profiles.py`), but there is ONE serving read.
- [ ] **MCP: zero proxy/transport/handler logic changes.** `/research/profiles` becomes reachable through the EXISTING `get_endpoint` allowlist (`/research/*`) by construction. The only permitted `app/mcp/__init__.py` diff is documentation strings made stale by this iteration (the module docstring's honest-404 example at `__init__.py:19` names `/research/profiles` before J-06). In `tests/test_mcp_server.py`: the honest-404 passthrough leg that currently exercises `/research/profiles` MUST move to a permanently-unknown `/research/*` path (the honest-error behavior stays covered — do not delete it), and add byte-identity coverage for `get_endpoint` on `/research/profiles` returning the live 200.

### Frontend
- [ ] **New page `apps/frontend/app/performance/page.tsx`** — the fourth top-level page, dark cockpit design language (slate-950 background, slate-900/60 panels, slate-800 borders, `font-mono` for ALL numerics, emerald/rose/amber semantics; density and honesty over decoration; matches the established style of `/journal` and `/studies`):
  - **Ledger table rendering `GET /research/pnl/ledger` verbatim.** One entry per enhancement: enhancement id + title; per split (train and hold-out in SEPARATE columns/sections — never pooled, no combined figure anywhere): candidate net R AND net $ side by side with n; the baseline side — the founding row's baseline is explicitly `null`, so render an explicit founding/absence marker ("no prior incumbent" per the served payload), NEVER a fabricated 0; the "insufficient sample" labels rendered from the API's labels (the real founding row exercises this on both splits); provenance visible (strategy id, profile id, config fingerprint, dataset ids + checksums — compact is fine); the timestamp displayed dd-MM-yyyy (foundation invariant 12, following the existing frontend date-display precedent).
  - **Numeric values render exactly as served** — full precision, no client-side rounding, no reformatting, no arithmetic of any kind (no re-deriving $, R, deltas, win rates, or totals). The committed `reports/pnl/pnl-history.md` full-precision render is the precedent; a value shown on the page equals the API value exactly. If display-rounding is ever wanted it must come from the serving side — out of scope here.
  - **The register string** "simulated — assumed fees/slippage — not indicative of live results" visibly rendered ON the page from the API payload's register field — never a second frontend copy of the string.
  - **Champion summary panel rendering `GET /research/profiles` verbatim**: the current champion strategy + profile (strategy `v1`, profile `default`) and the registry's frozen-default status, as served. Purely declarative — no advisory language, no imperative cues.
  - **States:** loading; backend unreachable → explicit unavailable state (mirroring the NavBar's honest degraded-state pattern — never fabricated or cached rows); empty ledger → honest explicit empty state.
- [ ] **NO NavBar edit and NO hardcoded route/link addition anywhere in the frontend** — the Performance link appears solely because the backend route map gains the entry (row 35 single source). A hardcoded `/performance` link would be the duplicate list this contract retired.

### Golden replay scripts
- [ ] **Evolve `runs/goal-session-tape_to_profit/journey-scripts/J-01.json`**: the stored nav expectation grows from 3 to 4 destinations — add a `goto /performance` step expecting text `Performance`, keeping the honest not-found step as the final end state (evaluator instruction from iter-4).
- [ ] **Record a new golden script `runs/goal-session-tape_to_profit/journey-scripts/J-05.json`** once J-05 verifies — J-05 is a real frontend journey (goto-based steps: `/performance` renders the ledger table, register text, and champion summary), so it gets a deterministic replay lane for future required-still-passing coverage.

### New user-facing capability
The researcher can, for the first time, SEE the era's scorekeeping in the browser: a Performance page listing every enhancement's PnL-ledger row (net R and net $ per split with n, insufficient-sample labels, provenance, the simulated register) and the current champion — reached from a fourth top-bar link on every page.

### New information displayed
The PnL ledger (Data Contract row 32) and the champion pointer + profile registry (row 33), both rendered verbatim from their canonical endpoints; the route map (row 35) now lists `/performance`.

### New user actions
One: navigate to `/performance` via the new Performance top-bar link (rendered from the route map). The page is read-only — no buttons, forms, or controls beyond navigation; the ledger has no write surface anywhere.

### UI surface changes
One new page (`/performance`) and the fourth nav entry appearing on EVERY page (Cockpit, Journal, Studies, Performance). Cockpit, Journal, and Studies pages themselves are untouched.

### Product surface delta
The era's honesty record becomes a human surface, not just a machine one: REST, markdown, and MCP already served the ledger identically; now the browser shows the same stored rows verbatim. This closes the J-02 → J-03 → J-04 → J-05 chain; J-06 (candidate profiles) and J-07 (sweep) remain.

### Blueprint conformance
`/performance` lands exactly where the blueprint's IA pre-declared it at baseline: the ONE new nav entry of the era ("Cockpit · Journal · Studies · Performance"), page and entry shipping together per the no-dead-link rule, home of the J-05 row in the feature-homes table. This EXECUTES the approved skeleton — it is not a nav-skeleton change, so no blueprint edit and no reapproval request.

### Data-contract additions
None new — every displayed value is already registered: ledger rows are row 32 (served by `GET /research/pnl/ledger`; the page is one more verbatim reader), champion pointer + profile registry are row 33 (this iteration lands its registered serving endpoint `GET /research/profiles` minimally; config-owned as declared), the route map is row 35 (one entry added at its single owner `app/meta.py`), and the register string remains the single `REGISTER` constant surfaced through the row-32 payload. No second computation or fetch path for any value: the page reads the two registered endpoints and computes nothing.

## OUT OF SCOPE

- J-06's substance: candidate profile registration or definition, additive feature keys / alternate threshold sets, backtests under a non-default profile, the registry-consultation refactor of the backtest route's profile refusal, pinned pre-profile equivalence work beyond keeping the existing suite green, and any dedicated MCP `profiles` tool (the blueprint routes profiles through `get_endpoint`)
- J-07 entirely: no sweep harness, no candidate evaluation, no promotion logic, no champion-movement mechanics (the champion pointer serves the config-owned founding value only)
- Any change to the ledger store/writer/render, the backtest runner, the dataset store, studies, engine, classifier, or serializers (`app/research/pnl_ledger.py`, `pnl_baseline.py`, `pnl_history.py`, `backtests.py`, `datasets.py`, `app/engine/`, `app/serializers.py` — zero diff); `reports/pnl/pnl-history.md` unchanged (no rows change)
- Any `app/mcp/__init__.py` change beyond the stale documentation strings named above
- Charts, graphs, sparklines, sorting/filtering/pagination controls on `/performance` (one founding row exists; the honest table is the spec — no general-purpose charting per Non-Goals)
- Client-side number formatting, rounding, or derived figures of any kind
- Any change to the Cockpit, Journal, or Studies pages or their components beyond the nav's automatic fourth link
- Real-scale or credentialed dataset flows — J-05 is keyless on the seeded founding row
- **(resume)** Rebuilding, restyling, or refactoring the already-implemented iter-5 changes when re-verification finds them conforming — verify first, change only what a failed check requires

## DEFINITION OF DONE

- [ ] Target journey J-05 passes via browser-qa-agent: `/performance` reached from the top-bar Performance link; ledger values on the page equal `GET /research/pnl/ledger` EXACTLY; every $ beside its R and its n; register visible; train/hold-out separate; both founding-row splits show "insufficient sample"; baseline side shows the explicit founding marker (never 0); champion summary equals `GET /research/profiles`; nav reads Cockpit / Journal / Studies / Performance on every page; `GET /meta/ui-routes` includes `/performance`; dark cockpit design language
- [ ] Required-still-passing journeys remain green: J-01 (replay of the UPDATED 4-destination golden script — one explicit result row) and J-08 (replay — one explicit result row) in the merged UI results; J-02, J-03, J-04 via their full automated suites staying green
- [ ] No anti-goal violation introduced (no execution path; no profit claims — register + R + n + split basis on every $ surface including the new page; no advisory language anywhere on `/performance`; engine untouched — equivalence 7/7; single source of truth — the page computes nothing; honest failure states)
- [ ] Full backend suite green (all ≥983 existing tests intact — none deleted or weakened; the relocated honest-404 MCP leg is a move, not a removal) plus the new route-map/profiles/MCP tests; engine equivalence suite 7/7; frontend `npm run build` passes — **re-run and independently confirmed in THIS dispatch, not carried forward from the interrupted one**
- [ ] Dev handoff current at `docs/handoffs/goal-tape_to_profit-iter-5-dev.md` (it already exists from the interrupted dispatch — refresh it ONLY where this dispatch's re-verification changes a result or fixes a gap; if all checks re-verify green, note that and leave the substance intact)

## TESTING REQUIREMENTS

- Browser (services running; **QA pre-flight:** run the idempotent founding-baseline seeding CLI against the harness backend's journal DB first, so the page renders the real founding row — J-05's acceptance requires "the founding ledger row present"; assert VALUES, not backtest/dataset UUIDs, since a fresh harness DB deterministically reproduces values but mints new record ids):
  - **Nav + reach leg:** from `/`, the top bar shows four links (Cockpit, Journal, Studies, Performance); click Performance → `/performance` renders with the Performance link active; spot-check the four-link bar on `/journal` and `/studies` too — capture
  - **Page-equals-API leg:** capture `GET /research/pnl/ledger` (in-page `fetch()` or backend-origin tab) and compare the displayed founding row EXACTLY: net R −0.16000000000001136 / net $ −16.000000000001137 / n 1 (train), net R 0.3334000000001356 / net $ 33.34000000001356 / n 1 (holdout), enhancement id `founding-baseline-strategy-v1-default`, "insufficient sample" on both splits, baseline side an explicit founding marker (not 0), register string verbatim on the page — capture
  - **Champion leg:** the champion summary shows strategy `v1` + profile `default` and equals `GET /research/profiles` (in-page `fetch()` comparison) — capture
  - **Route-map leg:** `GET /meta/ui-routes` now includes `/performance` (nav: true) and the rendered links match the map exactly — capture
  - **J-01, J-08 regression:** deterministic replay of the stored golden scripts (J-01 AFTER its 3→4 evolution); the merged results file MUST contain one result row per required journey; if the replay lane yields no rows, browser-qa executes those legs itself — lesson iter-1
  - **Record the J-05 golden script** for future replay coverage (already recorded in the interrupted dispatch — re-record ONLY if a fix in this dispatch changes verified page behavior; a green verify-mode replay of the existing script is the preferred evidence)
- Unit/integration (all keyless; run the FULL suite, not just new tests):
  - **Route map:** `UI_ROUTES` has 5 entries, 4 nav-true, `/performance` labeled `Performance`; `GET /meta/ui-routes` serves it verbatim; MCP `ui_route_map` byte-identity holds with the new entry
  - **Profiles endpoint:** `GET /research/profiles` → 200 with exactly one profile (`default`, marked frozen/default) and the champion pointer (strategy `v1`, profile `default`); values assert against the existing constants (no literal id strings duplicated in the new module); non-GET → 405
  - **MCP:** `get_endpoint` on `/research/profiles` byte-identical to REST on a live 200; the honest-404 passthrough leg relocated to a permanently-unknown `/research/*` path and still green; full MCP suite green; `app/mcp/__init__.py` diff is exactly the stale documentation strings
  - **No-execution gate:** `test_no_execution_path.py` green over any new modules
  - **Full backend suite green (≥983 intact); engine equivalence suite 7/7** (zero engine/compute-module diffs by construction)
  - **Frontend:** `npm run build` passes (type-check + compile; the page compiles with no new dependencies)
- Error cases: backend down → `/performance` shows an explicit unavailable state and the nav shows its existing degraded state (no fabricated rows, no cached ledger); empty ledger → honest explicit empty state on the page (exercised at dev time against an unseeded DB — the browser lane sees the seeded row); non-GET on `/research/profiles` → 405; the relocated MCP honest-404 leg proves unknown `/research/*` paths still surface the backend's honest error

## NOTES

- **Resume protocol (verify-and-complete) — read this first, developer:** the working tree on `goal/tape_to_profit` already contains the complete implementation (git status: 8 modified tracked files + `profiles.py`, `test_profiles_api.py`, `app/performance/`, `J-05.json` untracked — matching the dev handoff's Files Changed list exactly). Your pass is verification-first: (1) re-run the full backend suite, the equivalence suite, and `npm run build`; (2) replay J-01 + J-05 golden scripts in verify mode; (3) spot-check the handoff's page-equals-API and honest-failure-state claims; (4) fix ONLY what a failed check exposes, surgically; (5) refresh the existing dev handoff only where a result changed. Reviewer and browser-qa: treat every dev-handoff claim (988 passed / 1 skipped, equivalence 7/7, build clean, replay 2/2, 19/19 page-equals-API) as unverified input to check against the spec — the interrupted dispatch's claims carry no evidentiary weight until reproduced here.
- **Depth rationale:** lean, per the iter-4 evaluator recommendation. One route-map entry, one minimal config-serving GET route, one new page rendering two endpoints verbatim, zero compute-module diffs — narrow, additive, and the riskiest element (nav evolution) is structurally pre-solved by the route-map architecture. The resume changes nothing about depth: the work is the same scope, now mostly verification.
- **Why `/research/profiles` lands now (minimally):** J-05's acceptance includes the champion summary, and Data Contract row 33 assigns the champion pointer to `GET /research/profiles` — the ONLY coherent source. Serving `default` + the founding champion from existing config constants is a partial landing of row 33's serving side, NOT J-06 scope creep: J-06's substance (a registered candidate, backtests under it, equivalence pinning) remains fully open, and J-06's fresh-failing evidence naturally evolves from "404" to "registry lists no candidate yet".
- **Coherence watchpoints for the reviewer:** (1) the page performs NO arithmetic and NO number formatting — values render as served (full-precision markdown precedent); (2) the champion is read ONLY from `/research/profiles`, never inferred from ledger provenance or hardcoded; (3) the register string on the page comes from the API payload, not a frontend string literal; (4) no hardcoded route list or `/performance` link anywhere in the frontend — the nav entry exists solely in `app/meta.py` (row 35); (5) the new profiles module reuses `PROFILE_DEFAULT` and `STRATEGY_V1_ID` — grep for duplicated id literals (the handoff claims a source-scan test enforces this — verify it); (6) zero diff to `pnl_ledger.py` / `backtests.py` / `datasets.py` / `app/engine/` / `pnl-history.md`; (7) `app/mcp/__init__.py` diff is documentation strings only (planning-time `git diff` confirms one docstring hunk today — re-confirm at review time).
- **Lessons surfaced:** (iter-4) the founding row's n=1 splits make "insufficient sample" a live-data rendering requirement, not a mock; (iter-1) one explicit result row per required-still-passing browser journey; (iter-2) J-02/J-03/J-04 regression rides the automated suite; (iter-3) tmpfs pre-flight before diagnosing browser flakes (fresh reading at THIS dispatch's planning: /tmp 48%, pytest dir 2.3G — headroom but grown since the prior dispatch's 30%/1.2G; re-check at QA time).
- **Golden-script evolution is in scope this iteration** (contrast iter-4, where it was excluded): J-01.json gains the `/performance` step per the evaluator's explicit instruction, and J-05.json is recorded fresh. The updated J-01 script must still end on the honest not-found step. **(resume)** Both edits already exist and were dev-verified 2/2 in the interrupted dispatch; this dispatch re-verifies them rather than re-recording. Note the handoff's own caveat: J-05.json pins `insufficient sample (n < 5)` where `5` is the served `pnl_min_sample_size` — a config change would require re-recording (acceptable by design; no action now).
- **dd-MM-yyyy discipline (foundation invariant 12):** the page displays the appended date dd-MM-yyyy in the user's local timezone, following the existing frontend date-display precedent — date presentation is not value recomputation.
- **Demo framing (non-gating):** J-05's [NEW] walkthrough narrates an enhancement's before/after hold-out PnL delta — with only the founding row, the honest narration is the founding measurement itself: baseline explicitly absent (no prior incumbent), candidate holdout +0.3334 R / +$33.34 at n=1, labeled insufficient sample. Do not manufacture a fake "before".
- **`Frontend Present` parsing (carried from iters 2–4):** written above as a plain unbolded line so `detect_frontend_in_plan()` matches it — genuinely `yes` this iteration, the era's first.
- **Next natural targets after J-05:** J-06 (candidate profile + equivalence pinning) at lean, then J-07 (sweep harness) — J-07's promotion-gate tests must control the configured minimum-n both ways, since the fixture pair arms only n=1 per split (lessons iter-4).
