# Goal Iteration 1 — Universe ingestion: vendor seam, honest parser, append-only store (J-01)

<!-- machine-readable goal-mode metadata -->
## Goal Mode Metadata

- **Session ID:** desk
- **Iteration:** 1
- **Mode:** next
- **Depth:** full
- **Frontend Present:** no
- **Target journeys:** J-01
- **Required-still-passing journeys:** J-07 (backend/keyless subset only — see NOTES)
- **Anti-goal reminders (verbatim from `docs/goal.md`):**
  - **No execution path, ever** — no brokerage/trading API, no order tickets, no live OR paper trading, no "just to test" exceptions. (`apps/backend/tests/test_no_execution_path.py` is the tier-1 guard; new research code adds matching guard tests, never weakens them.) *(critical)*
  - **No profit claims and no advice** — every $ figure is a simulated measurement carrying R, n, fee/slippage assumptions, and its train/hold-out/forward basis. No prediction language, no imperative trading cues. *(critical)*
  - **Frozen foundations** — the `v1` strategy, the `default` profile, the tape engine's five states and thresholds, the frozen structure computations, the JSON `BarStore`, and every KEPT surface's behaviour stay byte-identical. New work is additive and versioned beside them, never a mutation of them. (The 5D demolition's removals are final history; this era builds `/desk` BESIDE the kept two pages — the one sanctioned kept-surface edit is J-05's additive `/structure` prefill.) *(critical)*
  - **Hold-out-only promotion** — the champion pointer moves only on a genuine hold-out survival through the sweep gate (plus the era-6 statistical gates once they exist). Train-only wins are labeled overfit. Never lower a minimum sample size, widen a gate, or pool across feeds/fingerprints to manufacture a survivor. *(critical)*
  - **No lookahead** — every value computed as-of T uses only events/bars fully completed at T. *(critical)*
  - **Single source of truth** — each shared value is computed once, owned by one canonical endpoint, and read verbatim by REST/WS/UI/MCP/reports. The coherence-auditor hard-fails violations. *(critical)*
  - **Deterministic and seeded** — every random draw uses a config-owned recorded seed; identical requests reproduce byte-identical results; no wall-clock, no unseeded randomness in any research artifact.
  - **Read-only MCP** — MCP tools remain byte-identical proxies of GET endpoints; nothing on the MCP surface can change state. *(critical)*
  - **Immutable data** — registered datasets and bar series are append-only, checksummed, never re-tagged, never deleted, never content-perturbed. Splits are frozen at registration. *(critical)*
  - **Persistence stays scoped** — no ambient recording of live streams; recording/fetching is an explicit, logged act. *(critical)*
  - **Membership is never a signal.** Universe membership (and any constituents metadata) selects WHAT to screen; it never enters a computation, rank formula beyond selection, feature, or report as an input value. *(critical)*
  - **Snapshots are append-only and pinned.** Universe and screen snapshots are dated, checksummed, append-only; every screen pins (universe snapshot id, screen date, as_of, fingerprint, bar-store signature); nothing is silently refetched, backfilled, recomputed in place, or rewritten — a new run is a new snapshot. *(critical)*
  - **Every run is an explicit operator act.** No scheduler, cron, daemon, auto-refresh, or market-hours trigger anywhere; page-load GETs never trigger fetches or computes. *(critical)*
  - **The briefing describes, never advises.** Desk copy is descriptive measurement only — no advice, imperative, prediction, or ranking language implying action ("buy", "watch this", "opportunity"); the copy-discipline lint stays green unmodified. *(critical)*
  - **No new statistics, gates, or strategies.** No probability/expectancy/edge claims on any desk surface; champion, `v1`, `default`, gates, and minimum-n floors untouched (the Referee is a future era). *(critical)*
  - **The demolition stays demolished.** No journal-era machinery returns; the desk ledger records machine output only — zero manual-input write paths on desk records this era (dispositions/annotations are Era C's design space). *(critical)*
  - **The ledger never holds orders.** No sizes, tickets, entries/exits, or account concepts in any desk record — rail 1 in desk terms. *(critical)*
  - **The suite stays keyless and hermetic.** Committed fixtures cover every test path; no test fetches the network; live fetch/top-up/screen runs are operator-run verifications reported honestly (run-or-not-run), never CI gates. *(critical)*
  - **The fingerprint pin does not move.** All new Config fields take Path A (exclusion + stability test + counter-test + payload provenance, same commit); `08e471b10130e1e2` is asserted unchanged by the sentinel every iteration. *(critical)*
  - **The enhancement loop stays inside its box.** The goal-proposer may append journeys ONLY inside the `AUTO:journeys` marker block of `docs/goal.md` — it MUST NOT edit human-authored journeys, the Anti-goals section, or any other part of that file; proposed journeys MUST carry a single-source-of-truth (or PnL-ledger) acceptance criterion, keep the `default` profile and `v1` byte-identical, and include a `[NEW]`-flagged walkthrough. Manufacturing a low-value journey just to keep the loop alive is a failure. *(critical)*

## GOAL

Add a backend-only, currently UI-invisible universe subsystem — a Wikipedia vendor seam, an honest parser, and an append-only checksummed snapshot store — so `POST /research/desk/universe/fetch` registers a validated S&P 100 membership snapshot (or refuses honestly) and `GET /research/desk/universe` serves it, unblocking J-02 through J-06.

## BACKGROUND

Iteration 0 (baseline, zero code diff) recorded J-01–J-06 `failing` and J-07 `partial`. Per the target-selection rubric, J-01 is the only defensible pick: rule 1 (no regressed journey exists), rule 2 (no `COHERENCE-FAIL` exists — `iter-0/coherence.md` is simply absent, since baseline never runs a coherence step), rule 3 (J-01 is `docs/goal.md`'s own stated hard unblocker: "J-01 → J-02 → J-03 → J-04 → J-05 → J-06"), and rule 5 (J-01 is already a large, risky, data-model change — a new store format plus the era's first Path-A Config fields plus a network-facing parser — so no second risky journey may ride along) all point at it alone, matching the iter-0 evaluator's explicit Next-Step Recommendation and the binding "Do not redo" entry in `iteration-state.md` ("Next target settled: J-01 alone, at full depth"). **Depth is `full`**, citing two independent triggers from the Picking-depth rubric: (1) **Structural/cross-cutting** — the vendor seam, parser, Config-exclusion-set edit, store, fixtures, and two new routes land together in one commit, with interactions (parser-honesty → store-immutability → fingerprint-stability) no single unit test alone covers; (2) **Data model** — this adds a brand-new persisted store format (frozen JSON + checksum) AND a blueprint Data-Contract value's computing module + serving endpoint ("Universe snapshots + membership" → `app/research/desk_universe.py` → `GET /research/desk/universe`). Both independently confirm the iter-0 evaluator's own `full` recommendation; the hardening-cadence trigger (4) does not apply (0 consecutive lean iterations dispatched so far) and is not the reason for this call.

**Frontend Present is `no`.** J-01's own `docs/goal.md` acceptance text is explicitly tagged `(Keyless; automated. The LIVE Wikipedia fetch is an operator-run verification, reported honestly as run-or-not-run.)`, it touches zero frontend files, and iter-0 already produced a full, screenshot-evidenced kept-product browser walk (`reports/qa/goal-desk-iter-0-evidence/`, binding "Do not redo" in `iteration-state.md`) that nothing in this diff can regress. This mirrors the `goal-clean_slate-iter-1` precedent exactly (an identical shape of iteration: a large backend-only foundation change with an existing evidenced sentinel and no frontend diff) — regression coverage for the kept product is scoped to a backend byte-comparison + live fingerprint-print protocol (TC-11) rather than a browser re-walk.

**Lessons applied (from `lessons.md`, not directly triggered this iteration but carried forward — see NOTES):** the J-07 golden script (`journey-scripts/J-07.json` step 8) asserts async, cache-warmth-dependent text on a 15s timeout — a documented false-negative shape — and the cold `/research/setups` scan takes ~9–11 min unwarmed. Neither applies to this iteration's execution (no browser QA dispatches when `Frontend Present: no`), but both must be re-surfaced for whichever iteration next dispatches browser QA (expected around J-04).

## IN SCOPE

### Backend
- [ ] Build the universe vendor seam (mirrors the Yahoo/Alpaca `MarketDataAdapter` "bars-vendor pattern" per `docs/goal.md` Key Capability 1) that fetches the Wikipedia S&P 100 constituents page over HTTP using the already-pinned `httpx` client (`>=0.27`, no new dependency) and hands the raw HTML to the parser below. Make it swappable via a `dependency_overrides`-style seam (the `get_bar_fetch_adapter`/`FakeAdapter` precedent in `research/routes.py`) so hermetic tests inject fixture HTML with zero network calls.
- [ ] Build the parser contract using **stdlib-only** HTML handling (`html.parser.HTMLParser` or an equivalent targeted extraction of the constituents table) — `lxml` and `html5lib` are **not installed** in this venv, so `pandas.read_html()` raises `ImportError` without them; `beautifulsoup4` is present only as an **undeclared transitive dependency** of `yfinance` and must NOT be imported or pinned (`docs/goal.md` Constraints: "No new runtime dependency (the universe fetch uses the stdlib/HTTP client patterns the Yahoo adapter already uses)"). Validate ticker charset `[A-Z.-]{1,6}`, member-count bounds `[90, 110]`, normalize `BRK.B → BRK-B` / `BF.B → BF-B` (retain the raw form in metadata), dedupe, and sort. ANY validation failure (charset, bounds, table shape) raises an honest, specific error — never a partial or guessed list (T-1).
- [ ] Add the Path-A Config fields — `desk_universe_source_url` (default: the live Wikipedia S&P 100 constituents URL), `desk_universe_min_members` (default `90`), `desk_universe_max_members` (default `110`) — plus a storage-location field/resolver if the store needs a configurable root (mirrors `bar_dir`/`dataset_dir` + their `_dir_resolved()` + `TAPEOLOGY_*_DIR` env-override pattern; a storage location is excluded from the fingerprint the same way, since it shapes no served value). Add every one to the `config_fingerprint()` exclusion set (`app/config.py:1312`, re-locate by grep, not line arithmetic) **in the same commit**, each with its own rationale comment following the `sr_pivot_lookback` / `tradability_band_cap_per_side` precedent (T-5).
- [ ] Build the universe store: one frozen JSON file per snapshot at `apps/backend/.data/universe/universe-<YYYY-MM-DD>-<checksum12>.json` (content-checksummed, structurally immutable — no update/delete function anywhere in the module, mirrors `bars.py`/`datasets.py`); registering content byte-identical to an existing snapshot is a no-op/refusal (mirrors `BarSeriesAlreadyRegistered`), never a rewrite. A directory-scan listing is sufficient for J-01's snapshot volume — a derived SQLite index (the `bar_index.py` pattern) is **not required this iteration** (J-02's own coverage-speed requirement is the one that actually needs an index, and it reads `bar_index`, not a universe index).
- [ ] Embed each Path-A Config field's value used at registration into the stored/served snapshot payload (provenance duty, the `structure_tape_*` precedent): `desk_universe_source_url`, `desk_universe_min_members`, `desk_universe_max_members`.
- [ ] Wire `POST /research/desk/universe/fetch` (explicit operator act; runs vendor-fetch → parse → validate → register; an honest 4xx body naming the specific validation failure on any parser/bounds error; a 409-style refusal naming the existing snapshot on duplicate content) and `GET /research/desk/universe` (snapshot list + latest membership; an explicit HTTP 200 empty payload before any registration — never a 404) under the existing `/research` prefix. New handlers in `routes.py`, or (preferred, given its size) a new dedicated desk-routes module mounted alongside it — build discretion; `get_endpoint`'s existing `/research/` allowlist already covers both paths with no MCP code change needed this iteration.
- [ ] Commit fixtures under `apps/backend/tests/fixtures/universe/` (new dir, mirrors `bars/`/`datasets/`/`yahoo/`): (a) a realistic constituents-table HTML fixture (90–110 tickers, including at least one dual-class ticker), (b) a deliberately corrupted variant (bad charset and/or an out-of-bounds count) for the honest-failure test, and (c) the resulting REGISTERED snapshot JSON produced by running the real registration path against fixture (a) once — this becomes "the fixture universe" that J-02–J-05 name by that exact phrase in their own `docs/goal.md` acceptance text.
- [ ] Wire the hermetic test path: an env-scoped universe directory (the `TAPEOLOGY_DATASET_DIR`/`TAPEOLOGY_BAR_DIR` env-override pattern) so tests never touch the real `.data/universe/` dir.
- [ ] Unit-test the parser contract (fixture HTML → exact expected member list; each validation-failure fixture → its specific honest error, zero partial registration), the store's immutability/no-op-on-duplicate behavior, both new routes' four states (empty / registered / corrupted-input / duplicate-input), the Path-A stability + counter-tests, and a T-3 store-separation guard (grep/import check proving the universe module never imports `research/datasets.py`'s registration surface or `DatasetStore`).
- [ ] Add one `@pytest.mark.integration`-gated test performing a REAL fetch against the live Wikipedia page (network-enabled, outside the default suite) — per `.claude/core.md` External Integration Testing. Run it at least once this iteration and record the outcome honestly in the dev handoff, whether it succeeded or hit a known failure (bot detection, layout change, no network in this sandbox).
- [ ] Capture a pre-iteration kept-route baseline (sha256 of every existing `/research`, `/tape`, `/meta` GET response — mirrors the `goal-clean_slate-iter-1` I-9 protocol) and re-capture/diff it after this iteration's changes land — zero deltas expected, since nothing existing is touched and only new routes are added.

### Frontend (if applicable)
None. J-01 is backend/REST-only; `/desk`, its nav row, and any UI rendering of universe data are J-04's job.

### New user-facing capability
Backend/operator capability only: an operator (or the goal-mode test suite, or a Claude+MCP `get_endpoint` call) can register a validated S&P 100 universe snapshot from the documented Wikipedia source and read it back via `GET /research/desk/universe`. No on-screen control yet — that arrives with J-04's `/desk` page.

### New information displayed
None on-screen this iteration (no `/desk` page exists yet). The snapshot's fields (checksum, member count, normalized/raw ticker forms, Path-A provenance) become real, readable JSON via `GET /research/desk/universe` and the `POST /research/desk/universe/fetch` response; their on-screen rendering is J-04's job.

### New user actions
None in the UI this iteration. `POST /research/desk/universe/fetch` exists only as a REST endpoint (curl/CLI/agent-callable); a UI "Fetch" button is out of scope until J-04.

### UI surface changes
None. `/` and `/structure` are unchanged; `/desk` does not exist yet (ships in J-04); `UI_ROUTES` stays at exactly 2 rows.

### Product surface delta
None visible in the browser this iteration. The backend gains a new, honestly-empty-by-default universe subsystem reachable only via REST/tests; the visible product change (the `/desk` provenance line) arrives in J-04.

### Blueprint conformance
No new page ships this iteration (J-04 builds `/desk`); `UI_ROUTES` stays at exactly 2 rows (`Cockpit`, `Structure`) — no nav-skeleton change, so no `blueprint.reapproval-requested` entry is needed. J-01's backend module + routes are the canonical home already registered in `blueprint.md`'s Feature/journey-homes table (row "J-01 Universe ingestion" → Desk section: *"surfaced as the provenance line + universe metadata on `/desk` — no standalone page"*). No blueprint edit is made this iteration — reviewed and confirmed current.

### Data-contract additions
**None requiring a blueprint edit.** The "Universe snapshots + membership" row this iteration makes real is already registered in `blueprint.md`'s Data Contract ("New rows this era" table): owner `app/research/desk_universe.py`, served by `GET /research/desk/universe` (fetch via `POST /research/desk/universe/fetch`), source-of-truth `.data/universe/universe-<date>-<checksum12>.json`. This iteration introduces no second computation or second endpoint for it and no other new displayed value — concrete field-level shape (checksum, member count in [90, 110], normalized/raw ticker forms, Path-A provenance) is pinned by the TC- lines below rather than by a new blueprint row, since the row's owner/endpoint were already fixed at baseline.

## OUT OF SCOPE

- J-02 (coverage + explicit bar top-up over `bar_index`), J-03 (screen compute + append-only ledger), J-04 (`/desk` page — `UI_ROUTES` stays 2 rows, nav unchanged), J-05 (screen history + `/structure` prefill), J-06 (MCP `desk_universe`/`desk_screen` tools — MCP suite stays at 15 tools) — all explicitly deferred to their own iterations per `docs/goal.md`'s stated dependency order.
- Any change to `app/meta.py`'s `UI_ROUTES`, `NavBar.tsx`, or any frontend file.
- Any change to `tradability.py`, `levels.py`, `bar_index.py`'s schema, or `EdgeReportComputeManager` — J-01's fetch is a single synchronous call, not a long-running sweep, so it needs no compute-manager (that pattern lands with J-02/J-03).
- Any new runtime dependency — no `lxml`, `beautifulsoup4`, `html5lib`, or `pandas.read_html()`; HTML parsing is stdlib-only (see IN SCOPE and NOTES).
- Writing universe data through `research/datasets.py`, or registering a screen snapshot as a dataset (T-3).
- A CLI wrapper for the universe fetch — not named in J-01's steps (only J-02/J-03 explicitly require "POST + CLI"); revisit if a later journey needs one.
- Any change to the fingerprint pin `08e471b10130e1e2` itself, or to any of the 13 existing pin-assertion sites — Path A only, forever this era.
- Re-probing the J-01–J-06 baseline absence checks or re-shooting iter-0's kept-product browser-walk screenshots — both already DONE and binding "Do not redo" per `iteration-state.md`.
- Fixing `journey-scripts/J-07.json` step 8's async-text assertion, or warming the `/research/setups` scan cache — carried forward for the next iteration that dispatches browser QA (no browser QA runs this iteration to exercise either); see NOTES.

## DEFINITION OF DONE

- [ ] J-01 passes: `GET /research/desk/universe` serves an honest empty payload before any registration; a valid fixture fetch registers a snapshot with a 12-character checksum, member count in [90, 110], and normalized/sorted membership (`BRK.B`→`BRK-B`, raw form retained, no duplicate entries); a corrupted fixture fails with an explicit error and registers nothing; re-fetching identical already-registered content is refused, never rewritten (TC-1 – TC-7)
- [ ] Every new `desk_universe_*` Config field lands with its exclusion-set entry, stability test, counter-test, and payload-provenance embedding in the same commit; `Config().config_fingerprint()` stays `08e471b10130e1e2` (TC-8 – TC-10)
- [ ] Required-still-passing: J-07's backend/keyless subset stays green — every kept `/research`/`/tape`/`/meta` GET route is byte-identical to its pre-iteration baseline (TC-11)
- [ ] No anti-goal violation introduced — snapshots are append-only and never rewritten (TC-5), the universe store never routes through `datasets.py` (TC-7), the suite stays keyless/hermetic with zero network calls in the default run (TC-13), and the fingerprint pin does not move (TC-8)
- [ ] Unit tests pass; no regressions — full suite reports at least 1169 passed, exactly 7 skipped, 0 failed (TC-12)
- [ ] The live Wikipedia fetch is attempted at least once under an integration marker and its outcome is reported honestly in the dev handoff (TC-14)
- [ ] Dev handoff written at `docs/handoffs/goal-desk-iter-1-dev.md`

## TESTING REQUIREMENTS

- Browser: none. J-01's own `docs/goal.md` acceptance is tagged `(Keyless; automated...)` and touches zero frontend files; `Frontend Present: no`, so no Chrome MCP dispatch is expected or required this iteration. J-07's kept-product browser walk was already fully evidenced at iter-0 (`reports/qa/goal-desk-iter-0-evidence/`, binding "Do not redo") and nothing in this diff touches a frontend file, so it is not re-shot; its regression coverage this iteration is the backend byte-comparison protocol (TC-11) instead.
- Unit/integration: parser contract (valid fixture + each failure mode), store immutability/no-op-on-duplicate, both new routes' four states (empty / registered / corrupted-input / duplicate-input), Path-A stability + counter-tests for every new Config field, the T-3 store-separation guard, the kept-route byte-comparison protocol, one `@pytest.mark.integration` live-Wikipedia test.
- Error cases: a charset-violating ticker, an out-of-bounds member count (too few and too many), a malformed/unexpected table shape, and a duplicate-content re-fetch — each must produce its own explicit, non-partial, non-guessed error state.

Test-first contract: every DEFINITION OF DONE checkbox and every Data-contract addition above maps to at least one concrete scenario line below.

- TC-1: given no universe snapshot has ever been registered in a fresh test-scoped universe directory, when `GET /research/desk/universe` is called, then it returns HTTP 200 with an explicitly empty payload (no snapshots listed, no latest membership) — not a 404.
- TC-2: given the committed valid constituents-table HTML fixture (90–110 tickers, at least one dual-class ticker) and a test-injected fake vendor returning it, when `POST /research/desk/universe/fetch` is called, then it registers a new snapshot and returns a body containing a 12-character content checksum, a member count between 90 and 110 inclusive, and the normalized ticker list sorted alphabetically with `BRK.B` normalized to `BRK-B` (and `BF.B` to `BF-B` if present in the fixture).
- TC-3: given the snapshot registered in TC-2, when `GET /research/desk/universe` is called again, then it lists that snapshot (date, checksum, member count) in the snapshot list and returns its membership as `latest`.
- TC-4: given a deliberately corrupted fixture (a ticker outside the `[A-Z.-]{1,6}` charset, or a total row count well outside 90–110), when `POST /research/desk/universe/fetch` is called with a fake vendor returning that fixture, then it returns an explicit 4xx error naming the specific validation failure, and a subsequent `GET /research/desk/universe` shows the store unchanged from before the call (no new snapshot registered).
- TC-5: given the snapshot registered in TC-2 already exists, when `POST /research/desk/universe/fetch` is called again with the fake vendor returning the IDENTICAL fixture content, then it returns an explicit 409-style refusal naming the already-registered snapshot, and the on-disk snapshot file is byte-unchanged (no rewrite).
- TC-6: given the raw fixture table contains `BRK.B`, when the registered snapshot is inspected, then its normalized membership contains `BRK-B` (never `BRK.B`), its raw/original form `BRK.B` is preserved in the snapshot's metadata, and the normalized list contains no duplicate entries introduced by the normalization step.
- TC-7: given the new universe-store module's source code, when it is grepped for any import of `research/datasets.py`'s registration function or `DatasetStore`, then zero hits are found (T-3: the universe store never routes through the dataset store).
- TC-8: given `Config().config_fingerprint()` computed live after this iteration's changes, when compared to the pre-iteration value, then it equals `08e471b10130e1e2` (unchanged) — the fingerprint-stability test for every new `desk_universe_*` field.
- TC-9: given a counter-test config override (e.g., `desk_universe_min_members` raised above the fixture's actual member count), when the SAME valid fixture from TC-2 is fetched under that override, then the registration is refused with the count-out-of-bounds validation error — proving the field is live-wired into the NEW path's output while the fingerprint (TC-8) stays unaffected.
- TC-10: given a snapshot registered under a specific `desk_universe_source_url` / `desk_universe_min_members` / `desk_universe_max_members` configuration, when its `GET /research/desk/universe` payload is inspected, then it embeds the exact three Config field values used at registration (the provenance duty).
- TC-11: given the backend running on committed fixtures BEFORE this iteration's changes and again AFTER, when every kept `/research`, `/tape`, `/meta` GET route (the era-open two-page surface) is curled and sha256-compared, then every hash is identical to its pre-iteration baseline capture.
- TC-12: given the full backend suite, when it is run after this iteration's changes, then it reports at least 1169 passed, exactly 7 skipped, 0 failed, 0 errors.
- TC-13: given the default `pytest` run (no `integration` marker selected), when it completes, then zero test performed a live network call to Wikipedia.
- TC-14: given a manual or `@pytest.mark.integration`-gated run against the REAL Wikipedia constituents page (network-enabled, outside the default suite), when it is attempted at least once this iteration, then its outcome (succeeded with N members parsed, or a specific failure reason such as no network / blocked / layout changed) is stated explicitly in `docs/handoffs/goal-desk-iter-1-dev.md`.

## NOTES

- **Stdlib-only parsing trap (verified live in this venv before writing this spec).** `lxml` and `html5lib` are NOT installed (`ModuleNotFoundError` confirmed); `pandas.read_html()` therefore raises `ImportError` with no parser backend available. `beautifulsoup4` IS importable (`bs4` 4.15.0) but only as an **undeclared transitive dependency** of `yfinance` — it is not in `requirements.txt` or `config/install-security-policy.json`'s allowlist, so importing it would be exactly the kind of accidental new-dependency drift the "no new runtime dependency" anti-goal and the supply-chain install gate both exist to catch. Use `html.parser.HTMLParser` (stdlib) or a targeted string/regex extraction instead.
- **Required-still-passing scoping.** `journey-history.json` currently has zero `passing` journeys (J-01–J-06 `failing`, J-07 `partial`), so there is no stable-passing regression set to protect via browser replay this iteration. J-07 is listed as Required-still-passing in its backend/keyless subset only: the kept-route byte-comparison protocol (TC-11) plus the live fingerprint print (TC-8) IS the mechanism that catches "kept surface regressed," which is exactly the risk this iteration's `config.py`/`routes.py` edits could introduce. J-07's browser-walked portion (cockpit screenshots, `/structure` Load, Case Studies, Edge Report state) is not re-verified this iteration since zero frontend files change in this diff — it stays at its iter-0 evidenced state and will be re-confirmed the next time browser QA dispatches (expected around J-04).
- **Carried forward for the next browser-QA iteration (from `lessons.md`, not actionable here since no browser QA dispatches this iteration):** (1) `journey-scripts/J-07.json` step 8 asserts async, cache-warmth-dependent text (`300.11`) on a 15s timeout — a documented false-negative shape; re-point it at a statically-rendered `/structure` shell string before the replay lane next guards J-07. (2) The scoped QA backend's first `GET /research/setups` call runs cold at ~9–11 min (0.84s warm) — warm the cache before any browser pass that touches Case Studies.
- **No new assumption-ledger entry logged this iteration.** Target/depth selection followed the rubric mechanically against the evaluator's own explicit recommendation; the Wikipedia source, the `desk_universe.py`/`GET /research/desk/universe`/`POST /research/desk/universe/fetch` naming, and the 90–110/`BRK.B→BRK-B` parser contract are all stated directly in `docs/goal.md` and `blueprint.md`, not interpreted. The one genuinely open implementation choice (directory-scan listing vs. a derived SQLite index for J-01's own snapshot list) is resolved above with stated reasoning rather than logged as an ambiguity, since `docs/goal.md`'s "any index over it is derived/rebuildable" describes a property an index must have IF one exists — it does not mandate that J-01 itself ship one, and no acceptance clause of J-01 (unlike J-02's explicit index-speed requirement) depends on it.
- **References:** iter-0 eval `runs/goal-session-desk/iter-0/eval.md`; blueprint `runs/goal-session-desk/state/blueprint.md` (Data Contract "New rows this era" table, row 1); goal `docs/goal.md` (J-01, Key Capability 1, Constraints, Build anchors T-1/T-2/T-3/T-5, Product Shape Data Contract); local precedent `docs/phases/goal-clean_slate-iter-1.md` (an iteration of the identical shape: backend-only, full depth, `Frontend Present: no`, a reduced-scope Required-still-passing sentinel).
