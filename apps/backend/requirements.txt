fastapi>=0.115
uvicorn[standard]>=0.30
pytest>=8.0
httpx>=0.27
websockets>=13.0
# Real market-data vendor SDK (Alpaca) — pinned, cleared through the supply-chain gate and
# allowlisted in config/install-security-policy.json. Confined to app/providers/adapters/alpaca.py.
alpaca-py==0.43.4
